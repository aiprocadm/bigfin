# Bigfin

Система управленческого учёта и финансовой отчётности для предпринимателей.

Bigfin помогает собственникам бизнеса видеть реальную картину денег и принимать
обоснованные решения — без бухгалтерского образования. Российский аналог
Fintablo и PlanFact с возможностью развёртывания на собственных серверах.

## Возможности

- Управленческий учёт (метод начисления и кассовый)
- Финансовая отчётность: P&L, ДДС, баланс
- Учёт по проектам, направлениям, юр. лицам
- Многовалютность с поддержкой RUB
- Импорт банковских выписок
- API для интеграций

## Установка

### Окружение в Docker

`docker-compose.yml` поднимает то, без чего продукт не работает: базу
(MariaDB), Redis и службу печати Gotenberg. Самого продукта в этом файле
нет — он запускается отдельно, следующим шагом.

```bash
cp .env.example .env
docker compose up -d
```

### Запуск продукта

Требования: Node.js 18.16.1, pnpm, а также поднятое окружение из
предыдущего шага (MariaDB, Redis, Gotenberg).

```bash
pnpm install
pnpm build
pnpm dev
```

После старта витрина открывается на http://localhost:4000, сервер — на
http://localhost:3000.

Настройки продукт берёт из `.env`. Переменные из этого файла читаются при
запуске приложения, поэтому менять его нужно до `pnpm dev`.

Подробнее в [CONTRIBUTING.md](CONTRIBUTING.md).

## Стек

- **Бэкенд:** Node.js, TypeScript, NestJS, MySQL/MariaDB, Redis, Knex
- **Фронтенд:** React, TypeScript, Vite
- **Монорепо:** lerna + pnpm workspaces

## Лицензия

Проприетарное программное обеспечение. См. [LICENSE](LICENSE).
