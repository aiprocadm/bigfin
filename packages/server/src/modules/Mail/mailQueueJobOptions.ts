// © 2026 Bigfin
import { JobsOptions } from 'bullmq';

/**
 * Повторы почтовых задач (шаг Ф2 карты v10).
 *
 * Без попыток временный сбой почты превращает письмо в потерю навсегда с
 * первого раза — именно так в очередях стенда молча скопились 22 упавшие
 * почтовые задачи. Пять попыток с удвоением паузы от минуты: 1 → 2 → 4 → 8
 * минут, всего ~15 минут. Минутный сбой почты покрыт с запасом, а
 * окончательное падение наступает быстро — чтобы уведомление о нём (шаг Ф1)
 * не приходило через сутки.
 *
 * Политика очистки истории задаётся глобально в App.module и здесь не
 * повторяется.
 */
export const MAIL_QUEUE_JOB_OPTIONS: JobsOptions = {
  attempts: 5,
  backoff: {
    type: 'exponential',
    delay: 60_000,
  },
};
