import { Meta } from '@storybook/blocks';

<Meta title="Brand/Colors" />

# Цвет в Bigfin

**Единственный источник значений — `src/styles/tokens.css`.** Здесь описано не
«какие цвета», а **почему они такие** и когда какой брать. Значения повторены
только там, где без них не объяснить правило; за их совпадением с кодом следит
проверка `colorsDocMatchesTokens.spec.ts`.

Светлая тема — основная. Тёмная включается классом `.dark` и повторяет те же
имена с другими значениями: в коде цвет берут **по имени**, а не по значению.

---

## Четыре правила

### 1. Холст почти белый, а не серый

`--c-background` (#FCFCFD) и `--c-surface` (#FFFFFF) различаются едва заметно.
Привычный приём — серый фон и белые карточки поверх; здесь наоборот: светлый
лист, а структура держится на волосяных линиях `--c-border` (#E6E8EC) и
отступах.

Учётная программа так похожа на документ, в котором работают, — а это и есть
журнал операций. Разницы хватает, чтобы карточка читалась без тени, и не
хватает, чтобы страница рассыпалась на плитки.

### 2. Расход — не красный

**Главное правило цвета в этом продукте.** В управленческом учёте расход —
норма работы, а не авария. Покрасьте каждый расход красным — и здоровый бизнес
выглядит стеной тревоги, а настоящая беда (просрочка, кассовый разрыв) теряется
среди неё.

- Расход рисуется обычными чернилами (`--c-text-primary`) со знаком минус.
- `--c-danger` (#D92D20) — **только проблема**: ошибка, просрочка, разрыв.
- `--c-success` (#0E7C5A) — приход.

Из того же правила следует второе: **рост показателя не всегда зелёный**. Рост
расходов — не «хорошо». Сторона смысла важнее знака числа.

### 3. Жёлтый метит, а не заливает

`--c-accent` (#FFD300) — фирменный знак Bigfin. Он метит логотип и **один**
смысловой момент на экране. Жёлтый на большой площади перестаёт быть акцентом и
становится фоном.

Кнопки и ссылки красит не он, а `--c-action` — чернильный (#15181F). В тёмной
теме чернильный на тёмном не читается, поэтому там главное действие светлое
(#F2F3F5), как у Apple, — а не жёлтое: жёлтый и в тёмной теме только метит
(R2, R3 ТЗ-4). Имя токена одно, значение разное.

### 4. Скруглений два, а не одно

- `--radius` — 12px, **оболочки**: карточки, шторки, всплывающие окна.
- `--radius-ctl` — 8px, **органы управления**: кнопки, поля, переключатели.
- `--radius-pill` — полное скругление: сегменты, метки-пилюли.

Было 10/6; 12/8 ближе к «непрерывным» углам Apple (R4 ТЗ-4).

Разное скругление кодирует разный вес. Одинаковое на всём не кодирует ничего и
превращает страницу в однородную кашу.

> **Грабля.** Имя `--radius-ctl` не должно совпадать с токеном Tailwind
> `--radius-control`: тот объявляется через этот, и одинаковые имена дают
> круговую ссылку. Свойство молча становится недействительным — ошибок нет,
> просто углы прямые.

### 5. Заливки и слои

- `--c-fill-1` (#F2F3F5) — фон сегментов, поиска, неактивных пилюль;
  `--c-fill-2` (#E9EBEF) — нажатое и наведённое.
- `--c-material` — «стекло» шапки и нижней панели на телефоне: класс
  `bg-material` и размытие фона; прозрачность — в `--material-alpha` (0.72).
- `--c-scrim` — затемнение под окном и шторкой: класс `bg-scrim`;
  прозрачность — в `--scrim-alpha` (0.32, в тёмной теме 0.56).
  Прозрачность живёт в токене, а не в классе: в тёмной теме она другая, и
  экраны не пишут отдельных классов под тему.

### 6. Высота: тень только у всплывающего

Карточки — без тени, они часть страницы. Тень — у того, что правда лежит
поверх: `--elev-1` (подсказки, меню), `--elev-2` (поповеры, командная строка),
`--elev-3` (окна, шторки). Классы `shadow-elev-1` … `shadow-elev-3`. В тёмной
теме тень не видна, и всплывающее отделяет светлая волосяная обводка.

### 7. Движение

Только в ответ на действие. `--motion-ease-standard` — появление и раскрытие,
`--motion-ease-spring` — шторки и сегменты. Длительности:
`--motion-dur-fast` (150 мс), `--motion-dur-base` (220 мс),
`--motion-dur-slow` (320 мс). При «уменьшить движение» в системе всё
мгновенно.

### 8. Цвета графиков

Первые три ряда — смысловые, остальные — категории:

- `--c-chart-1` (#15181F) — главный ряд: остаток, выручка, факт.
- `--c-chart-2` (#0E7C5A) — приход.
- `--c-chart-3` (#8C95A3) — расход, прошлый период, план. **Не красный.**
- `--c-chart-4`, `--c-chart-5`, `--c-chart-6`, `--c-chart-7`, `--c-chart-8` — категории в структуре (бублик, полосы).
- `--c-chart-problem` (#D92D20) — **только** зона разрыва и просрочки.
- `--c-chart-brand` (#FFD300) — **только** точка «сегодня».


## Все токены светлой темы

Полный список — за его полнотой следит `typographyDocMatchesTheme.spec.ts`.

| Токен | Значение | Роль |
|---|---|---|
| `--c-background` | #FCFCFD | холст приложения |
| `--c-surface` | #FFFFFF | карточки, панели |
| `--c-surface-elevated` | #F6F7F9 | боковая полоса, ховер, неактивное |
| `--c-border` | #E6E8EC | волосяные линии, вся структура |
| `--c-text-primary` | #15181F | основной текст |
| `--c-text-secondary` | #5B6472 | вторичный текст |
| `--c-text-muted` | #8C95A3 | подсказки, плейсхолдеры |
| `--c-action` | #15181F | главное действие: кнопки, ссылки (чернильный) |
| `--c-action-hover` | #262B36 | ховер |
| `--c-action-fg` | #FFFFFF | текст на чернильном фоне |
| `--c-accent` | #FFD300 | бренд: знак и одна метка на экран |
| `--c-accent-hover` | #F5C300 | наведение на бренд |
| `--c-accent-fg` | #15181F | текст на жёлтом |
| `--c-success` | #0E7C5A | приход, рост |
| `--c-danger` | #D92D20 | ТОЛЬКО проблема |
| `--c-warning` | #B57604 | предупреждение, читаемо на белом |
| `--c-info` | #1B5ACA | информация |
| `--c-fill-1` | #F2F3F5 | фон сегментов, поиска, неактивных пилюль |
| `--c-fill-2` | #E9EBEF | нажатое и наведённое |
| `--c-material` | #FFFFFF | брать с прозрачностью: bg-material/72 |
| `--c-scrim` | #0F1116 | брать с прозрачностью: bg-scrim/32 |
| `--c-chart-1` | #15181F | график: главный ряд |
| `--c-chart-2` | #0E7C5A | график: приход |
| `--c-chart-3` | #8C95A3 | график: расход, прошлое, план |
| `--c-chart-4` | #1B5ACA | график: категория 1 |
| `--c-chart-5` | #B57604 | график: категория 2 |
| `--c-chart-6` | #7A5AF8 | график: категория 3 |
| `--c-chart-7` | #0891B2 | график: категория 4 |
| `--c-chart-8` | #C2410C | график: категория 5 |
| `--c-chart-problem` | #D92D20 | график: только разрыв и просрочка |
| `--c-chart-brand` | #FFD300 | график: только точка «сегодня» |

---

## Как брать цвет в коде

Через классы Tailwind, которые Tailwind собирает из этих же токенов:
`bg-surface`, `text-text-secondary`, `border-border`, `text-danger`.

Тройки RGB (а не `#HEX`) в токенах нужны, чтобы работала прозрачность вида
`bg-accent/10`.

**Не пишите цвет значением.** `#15181F` в разметке не переключится в тёмную
тему и не найдётся поиском, когда палитру поправят.
