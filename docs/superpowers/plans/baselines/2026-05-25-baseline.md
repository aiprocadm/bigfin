# Baseline перед D-редизайном

**Дата фиксации:** 2026-05-25
**Branch:** `chore/dev-env-setup`
**Commit:** `621dbcdbe2daf65652ce3961b160c8dcbe091a45`
**Node:** 18.16.1 (через `fnm`)

## Состояние сборки

### `pnpm --filter @bigfin/utils build`

- Exit: 0
- CJS: build success (1910 ms)
- ESM: `dist/index.mjs` 46.42 KB, sourcemap 91.44 KB
- DTS: `dist/index.d.mts` 670 B
- Предупреждения npm про `Unknown env config` (`npm-globalconfig`, `recursive`, `verify-deps-before-run`, `_jsr-registry`) — связаны с конфигом окружения, на сборку не влияют

### `pnpm --filter @bigfin/webapp typecheck`

- Exit: 0
- Команда: `tsc --noEmit`
- **0 ошибок** ✅

### `pnpm --filter @bigfin/webapp lang:check`

- Exit: 0
- English keys: 2390
- Russian keys: 2390
- Missing in ru: 0
- Extra in ru: 0
- Парность en↔ru соблюдена ✅

## Визуальный baseline

**НЕ зафиксирован в этой сессии.** Шаг «открыть `http://localhost:4000`, дойти до `/auth/login`, сделать скриншот» требует ручных действий в браузере. Если потребуется визуальное сравнение после D-Phase-0 — основатель может зафиксировать скриншоты текущего auth-флоу самостоятельно перед запуском Task 0.2.

## Окружение исполнителя

- ОС: Windows
- Менеджер версий Node: `fnm` (нужен `eval "$(fnm env --shell bash)"` перед `fnm use 18.16.1` в неинтерактивных сессиях)
- Пакетный менеджер: `pnpm` (через lerna monorepo)

## Известные проблемы окружения (из памяти)

- `pnpm install` бывает капризен — возможно потребуются обходы (`pnpm install --shamefully-hoist`, либо `rm -rf node_modules && pnpm install`)
- Локальный бэкенд НЕ настроен (нет `.env`, нет Bigfin Docker) — но для пилота этого редизайна это не блокер, потому что Login — pre-auth страница
- `shared/` пакеты должны быть собраны ДО `typecheck` (выполнено)

## Что это значит

Перед началом D-редизайна (Phase 0 → Task 0.2 «Install Tailwind 4») кодовая база в чистом состоянии:
- typecheck без ошибок
- локализация без расхождений
- shared/ пакеты собраны

Любые ошибки `typecheck` или `lang:check`, появляющиеся в процессе Phase 0, — следствие наших изменений, не предсуществующие.
