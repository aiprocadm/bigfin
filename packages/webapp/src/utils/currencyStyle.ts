/**
 * Валюты, которые принято писать по-русски: разряды через пробел, копейки
 * через запятую, знак ПОСЛЕ суммы («100 000,00 ₽»).
 *
 * Лежит отдельным листком без зависимостей: список нужен и печати сумм
 * (`utils/index.tsx`), и разбору напечатанного (`utils/amountInput.ts`),
 * а тянуть одно из них в другое — значит тянуть за собой полстора.
 */
export const RU_STYLE_CURRENCIES = ['RUB'];

/** Неразрывный пробел — им продукт разделяет разряды. */
export const NBSP = '\u00a0';
