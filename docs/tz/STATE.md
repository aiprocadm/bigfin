# ТЗ Bigfin — состояние работ

<!--
  ЭТОТ ФАЙЛ — ЕДИНСТВЕННЫЙ ИСТОЧНИК ПРАВДЫ О ТОМ, ГДЕ МЫ СЕЙЧАС ПО ТЗ.

  Правила ведения:
  1. Файл ПЕРЕЗАПИСЫВАЕТСЯ, а не накапливается. История — в разделе «Журнал»
     внизу и в картах `docs/superpowers/specs/*-roadmap-actualization-vNN.md`.
  2. Обновляется в конце КАЖДОГО шага работы, до коммита.
  3. Блок «Указатель» правится всегда — даже если этап не сменился.
  4. Порядок работы с ним описан в `docs/tz/CONTINUE.md`.
-->

**Само ТЗ:** [MASTER.md](MASTER.md) — блоки Б1–Б3, этапы 1–15 · [MASTER-2.md](MASTER-2.md) — блок Б4 «Вход в продукт», этапы 16–28 · [MASTER-3.md](MASTER-3.md) — блок Б5 «Управленческая глубина», этапы 29–41
**Обновлено:** 2026-09-23
**Обновил:** Claude Code (этап 29 закрыт; дальше этап 30 «ОДДС-матрица»)
**База develop на момент обновления:** `f8c1d1ce`

---

## 1. Указатель — что делать прямо сейчас

| Поле | Значение |
|---|---|
| 2026-09-23 (живая проверка этапа 32) | **УПРАВЛЕНЧЕСКИЙ ОПиУ «ПО ДЕНЬГАМ» НЕ ВИДЕЛ ВЫРУЧКУ СЧЕТОВ, ОПЛАЧЕННЫХ ПОЗЖЕ. ПОЧИНЕНО, PR #550.** Найдено живой проверкой на стенде, а не тестами: по начислению управленческий ОПиУ сошёлся с бухгалтерским до рубля (ЧП 1 370 000 + «Не отнесено» −105 000 = 1 265 000), а по деньгам выручка была 100 000 против 183 333. Причина: у счёта покупателю нет денежной ноги, у оплаты — ноги выручки, и признак «оплачено деньгами» выручку такого счёта не видит. Бухгалтерский ОПиУ это давно решал «достраиванием по оплате». Правило вынесено в общий `ProfitLossSheet/settlementRecognition.ts` и зовётся из обоих отчётов — второй копии нет. **ОСТАТОК:** раскрытие ячейки управленческого ОПиУ по деньгам отключено: достроенной выручки нет в журнале, и панель не сошлась бы с ячейкой. Раскрытие по начислению работает. |
| 2026-09-23 (этап 32) | **ЭТАП 32 «УПРАВЛЕНЧЕСКИЙ ОПиУ: ЯРУСЫ» ЗАКРЫТ ПО КОДУ, PR #549.** FT-010: новый отчёт `GET /reports/managerial-profit-loss` (подмодуль `ManagerialProfitLoss`) — лестница Выручка → МД → ВП1 → ВП2 → ОП (EBITDA) → ЧП с рентабельностью под каждым ярусом; строится от `pl_type` статьи с наследованием, подстатья со своим ярусом — в своём ярусе. «Ниже чистой прибыли» на ЧП не влияет. «Не отнесено к ярусу» — статьи без яруса И счета доходов/расходов без статьи (иначе их суммы пропадали бы молча). Два метода учёта: по начислению — один сгруппированный запрос (счёт × направление × день), по деньгам — кассовая свёртка «Денег». Раскрытие до статей, направлений или обоих. Выручка ≤ 0 — рентабельность «н/о», никогда «0 %»/«100 %». Раскрытие ячейки — по ярусу целиком или по статье в своём ярусе, с методом учёта. Страница «Прибыль (ОПиУ)» — переключатель «Управленческий / Бухгалтерский» (`view`), по умолчанию по режиму интерфейса. Новое право `read-managerial-profit-loss` (у старых ролей его нет — экран говорит об этом прямо). FT-016: «?» с формулой у всех итогов, рентабельностей и расчётных строк «Денег»; сторож `formulaTooltipsExist.spec.ts` читает серверный построитель. **ПОПУТНО:** в словарях сервера не было подписи права на «Деньги по статьям» — добавлена; на «Деньгах» у нулевой ячейки больше нет «0,00 %» (живая проверка этапа 31). Живая проверка этапа 31 ПРОЙДЕНА (d17cb27e): шесть группировок дают одинаковые поток и конец, раскрытие совпадает с ячейками, недели с понедельника, ответы 0,04–0,17 с. |
| 2026-09-23 (этап 31) | **ЭТАП 31 «ОДДС: ГРУППИРОВКИ, ПРОЦЕНТЫ, РАСКРЫТИЕ» ЗАКРЫТ ПО КОДУ, PR #548.** FT-002: шесть группировок строк (статьи — по умолчанию, виды деятельности, контрагенты, счета, направления, направления и статьи). Поток и остатки считаются с денежной стороны и потому ОДИНАКОВЫ во всех режимах; всё, что строки не объяснили, — «Не разнесено». Контрагент и направление берутся с ног «не деньги» (на денежной ноге их на живых данных нет), счета — с денежных. FT-003: доля под суммой от итога своей колонки, «н/о» вместо «0 %». FT-004: щелчок по ячейке статьи — панель операций за период КЛИКНУТОЙ колонки с отбором отчёта, из панели открывается карточка операции. FT-005/006: пустые строки и переводы по умолчанию спрятаны; тумблеры — личные настройки. FT-006b: день начала недели, подсветка выходных, дни недели — настройки организации (страница «Бухгалтерия»), действуют на «Деньги» и платёжный календарь. **ТРИ НАХОДКИ ПОПУТНО:** (1) отбор по направлению в кассовой свёртке (этап 29) стоял ДО признака «оплачено деньгами» — денежная нога без направления отпадала, и выручка направления обнулялась; (2) раскрытие по статье не знало юрлица отчёта и признак «оплачено» считало по дням колонки — итог панели мог не сойтись с ячейкой; (3) ссылка из «Карты статей» (`?articleId=`) вела в отчёт, который параметр молча терял — теперь строка статьи выделена. Плюс: сохранение одной личной галочки сбрасывало в памяти остальные к умолчаниям. |
| 2026-09-23 (этап 30) | **ЭТАП 30 «ОДДС-МАТРИЦА» ЗАКРЫТ ПО КОДУ, PR #547.** «Деньги по статьям» — матрица «статьи × периоды» пяти масштабов + «весь период» и колонка «Итого» (FT-001); у общей таблицы отчётов — закреплённые шапка (без виртуализации) и первая колонка (FT-005b). **КАК ДЕРЖАТСЯ РАВЕНСТВА.** Кассовая свёртка по периодам читает ноги ОДИН раз на весь отрезок, и признак «оплачено деньгами» считается тоже один раз — поэтому «Итого» не зависит от масштаба. Остатки ЦЕПЛЯЮТСЯ (конец колонки = начало следующей), а конец цепочки сверяется с остатком из базы, посчитанным отдельным запросом (`isBalanced`). «Итого» СКЛАДЫВАЕТСЯ из колонок, а не считается заново. **ПРОВЕРКИ:** 5 000 случайных операций (17 колонок за 17 месяцев, сцепка, «Итого» = Σ колонок ±0,001 ₽, кварталы/дни/недели/год дают то же «Итого»), золотой ответ с каждой ячейкой, посчитанной руками, мутация «Итого» ловится шестью проверками. **ПОПУТНО:** (1) дата ноги считалась через moment на каждую ногу — заменено простым чтением, проверки отчёта стали втрое быстрее (ТЗ требует ≤ 3 с на 50 000 операций); (2) график «Денег» красился `var(--c-action)` — это три голых числа без `rgb()`, браузер такой цвет не понимает; исправлено на `--color-*`. **ТА ЖЕ ОШИБКА ЦВЕТА ОСТАЛАСЬ** в `Homepage/TopContractorsSection.tsx` (строки 101 и 107) — вне этапа, записана в остаток раздела 1а. Живая проверка на стенде ПРОЙДЕНА (90a6342f): 14 колонок за год, «Итого» одинаково на месяцах/кварталах/неделях, остатки сцеплены, по дням на 2 года — 400 с понятным текстом, ответ 0,1–0,5 с; на экране закреплённые шапка и колонка работают. |
| 2026-09-23 (этап 29) | **ЭТАП 29 «ФУНДАМЕНТ» ЗАКРЫТ ПО КОДУ, PR #546.** FT-008: три управленческих свода (свёртка ОПиУ по статьям, кассовая свёртка, «Деньги по статьям») принимали номера юрлиц и ВЫБРАСЫВАЛИ — теперь все их запросы к проводкам идут через одно общее место `applyManagementReportScope` (подразделения, юрлица, направления разом). Сторож считает «запросов к проводкам столько же, сколько отборов», проверен мутацией. НАМЕРЕННОЕ изменение: в сводном режиме внутригрупповые обороты убираются, как в бухгалтерском ОПиУ. FT-009: у статьи появился ярус управленческого ОПиУ (`pl_type`, 9 ярусов + «не участвует», наследование от родителя, 422 на недопустимую пару). **ОТСТУПЛЕНИЕ ОТ ТЗ, ОСОЗНАННОЕ:** сид ярусов узнаёт системные статьи не только по `seed_key`, но и по подписи исходного сида — у организаций до 20.09 ключей у доходов и расходов НЕТ, и сид «только по ключу» прошёл бы мимо всех девяти статей, формально выполнив критерий приёмки. **ДВЕ НАХОДКИ ПОПУТНО (PR #545):** (1) автообновление стенда НИКОГДА не накатывало миграции организаций — с 20.09 у стенда не было ни пяти видов статей, ни личных настроек, ни индекса; (2) откат сида балансовых статей читал `article_id` вместо `articleId` и удалил бы ВСЕ балансовые статьи, включая размеченные. |
| 2026-09-21 (ТЗ-3) | **ПРИШЛО ТРЕТЬЕ ТЗ — [MASTER-3.md](MASTER-3.md), блок Б5 «Управленческая глубина», этапы 29–41.** Написано по живому проходу по Финтабло (личный аккаунт + демо-аккаунт с данными за 2021–2022) и по чтению исходников Bigfin на `5fa0e660`. **ГЛАВНЫЙ ВЫВОД: Bigfin не отстаёт от Финтабло по функциям и не отстаёт по входу в них — отстаёт по управленческой глубине трёх вещей.** (1) Отчёт «Деньги (ДДС по статьям)» имеет ДВЕ колонки — Название и Сумма; у конкурента это матрица со строками статей и колонками месяцев, шестью группировками строк, пятью масштабами колонок, процентами и раскрытием любой ячейки. (2) Нет ярусов управленческой прибыли МД → ВП1 → ВП2: `computeProfitTiers` даёт EBITDA/EBIT/EBT/Net от бухгалтерской схемы, а трёх уровней валовой прибыли, по которым сравниваются направления, нет. (3) У статьи нет управленческого типа для ОПиУ: есть `kind`, `cashflow_section`, `cost_behavior` — нет `pl_type`, и поэтому ярусы строить не из чего. **ДВА ДЕФЕКТА В СВОЁМ КОДЕ, НАЙДЕННЫЕ АУДИТОМ:** `applyLegalEntityScope` вызывается только в трёх репозиториях (Баланс, ОПиУ, ДДС косвенный) — `CashFlowArticlesService`, `ArticlesPlRollup` и `ArticlesCashflowRollup` юрлицо НЕ учитывают, то есть ДДС по статьям, план-факт бюджета и рентабельность сделок считаются по всей организации; и токен публичного API `bgf_` выпускается, но `resolveToken` не вызывается НИОТКУДА, а вебхуки не отправляются вовсе. Оба вынесены требованиями FT-008 и FT-091/092 и стоят в начале очереди. **СЛЕДУЮЩИЙ ШАГ: этап 29 «Фундамент: юрлицо в свёртках + управленческий тип статьи»** — он первый, потому что колонки-периоды ОДДС, положенные поверх непочиненного отбора по юрлицу, размножат ошибку по всем колонкам. |
| 2026-09-20 (живой проход, последняя находка) | **ПОМЕТКА «ПРОСРОЧЕНО» НЕ ПОЯВЛЯЛАСЬ НИ РАЗУ, ХОТЯ ОТБОР НАХОДИЛ 18 ПРОСРОЧЕННЫХ СТРОК.** Найдено живым проходом сразу после того, как состояния заработали. Причина: расчёт сравнивает срок оплаты с сегодняшним днём КАК СТРОКИ, а модель отдаёт дату ОБЪЕКТОМ. Сравнение объекта со строкой в JavaScript не падает — оно молча даёт «не просрочено». Починено приведением срока к `ГГГГ-ММ-ДД` при догрузке документов. Спека закрепляет ОБЕ стороны: со строкой расчёт работает, с объектом МОЛЧА ВРЁТ — чтобы следующий, кто передаст сюда `Date`, увидел причину сразу. **ЭТО СЕДЬМОЙ ДЕФЕКТ ЗА ДЕНЬ, НАЙДЕННЫЙ ГЛАЗАМИ, А НЕ ТЕСТАМИ.** |
| 2026-09-20 (список ТЗ-2 закрыт) | **ЗАКРЫТЫ ПОСЛЕДНИЕ ДВЕ ПОЗИЦИИ: T-38 «ТРЕНД» И T-34 «НАСТРОЙКА КОЛОНОК». СПИСОК ЗАДАЧ ТЗ-2 ЗАКРЫТ ЦЕЛИКОМ, кроме осознанно пропущенной T-54.** **ТРЕНД БЕРЁТ ЧИСЛА ИЗ УЖЕ ПРИШЕДШИХ ЯЧЕЕК,** без своего запроса: второй источник тех же чисел однажды разошёлся бы с цифрами над линией. Ради этого пришлось разбирать напечатанную сумму обратно в число — движок отчётов отдаёт ячейки уже в русском виде. **РАЗБОР — САМОЕ ОПАСНОЕ МЕСТО, И ОН ПОЙМАЛ ДВЕ ЛОВУШКИ:** разряды разделены НЕРАЗРЫВНЫМ пробелом, а минус бывает ТИПОГРАФСКИМ — на обоих `Number()` даёт `NaN`, и линия для расходов пропала бы целиком. Правило «копейки отделяет ПОСЛЕДНИЙ из знаков» взято из уже записанного в проекте, а не выдумано заново. Пустая ячейка НЕ становится нулём: «нет значения» и «ноль» на графике выглядят одинаково, а значат разное. **КОЛОНКИ РЕЕСТРА: «ДАТА» И «СУММА» НЕ СНИМАЮТСЯ** — это не про вкусы, список операций без них перестаёт быть списком. Обязательных галочек на экране нет вовсе: заблокированная галочка предлагает то, чего сделать нельзя. Новая колонка, о которой сохранённая настройка не знает, ПОКАЗЫВАЕТСЯ: молчание — это «не выбирал», а не «выключил». |
| 2026-09-20 (сверка задела, главная находка) | **СОСТОЯНИЯ ОПЕРАЦИЙ БЫЛИ НАПИСАНЫ ТРИЖДЫ И НЕ СОЕДИНЕНЫ НИ РАЗУ.** Самая крупная находка сверки. Расчёт `resolveTransactionState` написан и покрыт спекой — и не вызывался НИОТКУДА. Бейджи `TransactionStateBadges` нарисованы — и не показывались НИГДЕ. Отбор по состоянию описан в модели отборов витрины (`states`) — и сервер о нём не знал. Три готовые части, ноль работающих: прогон зелёный, экран пустой. **СОЕДИНЕНО:** хранилище списка догружает документы строк текущей страницы (два запроса на страницу, а не по запросу на строку), строка получает состояния, в таблице появилась колонка «Состояние», в панели — отбор. **ОТБИРАЕТ СЕРВЕР:** список разбит на страницы, и фильтровать загруженную значило бы показать «ничего не найдено» при полной базе просрочки на следующей. **ФОРМУЛА ДОЛГА ОБЩАЯ** (`hasDueSql`) — та самая, по которой долг считает карточка документа: своя копия однажды разошлась бы на налог или скидку, и реестр показал бы «просрочено» там, где документ закрыт. **ЗАОДНО ЗАКРЫТ T-14 (P0): ГРАФИК НАД ОТЧЁТОМ «ДЕНЬГИ».** Его не было вовсе, хотя чек-лист приёмки требует «график показывает те же числа, что таблица». Равенство обеспечено УСТРОЙСТВОМ: ряд собирается из тех же строк, которые рисует таблица, своего запроса у графика нет. **ОСТАЛОСЬ ПО СПИСКУ:** T-38 «колонка Тренд» (P1) и T-34 «настройка колонок реестра» (P2) — обе требуют правки общей таблицы отчётов, это отдельная работа. |
| 2026-09-20 (сверка задела, четвёртая находка) | **ЧЕКБОКСОВ КОЛОНОК ПЛАН-ФАКТА НЕ БЫЛО ВОВСЕ.** Позиция T-43 числилась закрытой этапом 26, но этап 26 сделал «прошло времени» и личные настройки, а выбор колонок — нет: все четыре колонки всегда показывались разом. На телефоне они не помещаются, а нужны вместе редко. **ГЛАВНОЕ ПРАВИЛО — «ФАКТ» УПРАВЛЯЕТ ОСТАЛЬНЫМИ ТРЕМЯ:** выполнение плана, отклонение и отклонение в процентах считаются ОТ ФАКТА, и показывать их без него значит показывать следствие без причины. Снятие «Факта» не прячет выбор, а СНИМАЕТ зависимые колонки: иначе человек вернёт факт и получит колонки, которых уже не ждал. Правило вынесено из экрана отдельным расчётом со спекой — глазами такое не проверяют. Заодно добавлена колонка «Вып. плана, %» с честным «н/о» при нулевом плане. **ИТОГ ДНЯ ПО СВЕРКЕ: ЧЕТЫРЕ ДЕФЕКТА ОДНОГО КЛАССА.** Масштаб календаря, галочка копеек, источник выручки Парето и отсутствующие чекбоксы. Три первых — «орган управления есть, последствий нет», четвёртый — «в отчёте о состоянии стоит галочка, а в коде ничего». Вывод: отмечать позицию закрытой можно только после того, как посмотрел, ЧТО ИМЕННО она изменила на экране. |
| 2026-09-20 (сверка задела, третья находка) | **ГАЛОЧКА «ОТОБРАЖАТЬ КОПЕЙКИ» СОХРАНЯЛАСЬ И НЕ МЕНЯЛА НИЧЕГО.** Третий дефект одного класса за день. Экран настроек сохранял значение, сервер его хранил — и НИКТО его не читал: поиск по коду давал одно упоминание, на самом экране настроек. Человек снимал галочку, получал «сохранено» и не видел ни одного изменения. Приёмка FIN-026 требует прямо: «отключение копеек меняет печать сумм во всём продукте». **ПОЧИНЕНО В ОДНОМ МЕСТЕ:** общая печать денег `formatOrganizationMoney` спрашивает настройку, форматтер научился принимать точность, настройки грузятся при входе и применяются СРАЗУ при сохранении — иначе они заработали бы только со следующего входа. **ОСТАЛОСЬ ЯВНО:** экспорт в XLSX формирует СЕРВЕР своим форматтером, и личную настройку пользователя он пока не знает. Это отдельная правка: пробросить её придётся в каждый отчёт, и делать это заодно с тремя другими починками — плохой обмен. **ОБЩИЙ ВЫВОД ДНЯ: ТРИ ДЕФЕКТА ОДНОГО КЛАССА — «ОРГАН УПРАВЛЕНИЯ ЕСТЬ, ПОСЛЕДСТВИЙ НЕТ».** Переключатель масштаба календаря, галочка копеек и источник выручки для Парето. Ни один не ломает ни типы, ни тесты: код написан, кнопка на месте, прогон зелёный. Ловятся они только двумя способами — живым проходом и сверкой «кто это читает». Оба теперь закреплены сторожами. |
| 2026-09-20 (сверка задела, продолжение) | **ПЕРЕКЛЮЧАТЕЛЬ МАСШТАБА В ПЛАТЁЖНОМ КАЛЕНДАРЕ НИЧЕГО НЕ МЕНЯЛ НА ЭКРАНЕ.** Сверка позиции T-42 показала: на экране есть выбор «по дням / неделям / месяцам / кварталам / годам», сервер честно считает периоды и отдаёт их полем `periods` — а витрина читает только `days` и периоды НЕ ИСПОЛЬЗУЕТ ВОВСЕ (поиск по каталогу экрана: ноль упоминаний). Человек менял масштаб, и не происходило ничего. **ЭТО САМЫЙ ОБИДНЫЙ ВИД ПОЛОМКИ:** и код написан, и кнопка на месте, и прогон зелёный — потому что никто не проверял, что выбор ВИДЕН. **ПОЧИНЕНО И ЗАОДНО ЗАКРЫТО РАСКРЫТИЕ КЛЕТКИ (FIN-020):** клетка крупнее дня разворачивается прямо на месте — в те дни и строки, из которых сложилась, вместе с кнопкой «Записать в учёт». Отдельная шторка добавила бы щелчок и ничего к ответу; у конкурента клик по числу не делает вообще ничего. Пустая клетка не раскрывается: разворачивать нечего. Факт и план в клетке показаны РАЗДЕЛЬНО — иначе план выдавался бы за свершившееся. **СТОРОЖ ПРОТИВ ВОЗВРАТА:** спека проверяет не только разбор дней, но и сам факт, что экран читает `periods`, а разбор ответа их не теряет. |
| 2026-09-20 (сверка задела) | **СВЕРКА ПО СПИСКУ ЗАДАЧ ТЗ-2 НАШЛА ТРИ НЕЗАКРЫТЫЕ ПОЗИЦИИ, ДВЕ ИЗ НИХ — P0.** Этапы шли по разделу 12, а список задач раздела 13 отдельно никто не сверял: T-56 (набор спек сходимости), T-57 (регрессия «золотых» ответов) и T-39 (замер и индекс направлений) оказались ни в одном этапе. **T-56 — НАБОР СХОДИМОСТИ.** Десять инвариантов раздела 11.10, три набора данных (одно юрлицо; два юрлица с внутригрупповыми и переводами; две валюты и операции без статьи), суммы с копейками, «сегодня» зафиксировано. Считают ЖИВЫЕ функции продукта: проверка, переписывающая расчёт своими словами, проверяет саму себя. Отдельный РЕЕСТР связывает каждый инвариант с файлом, который его держит, и требует, чтобы в файле встречалось слово из текста инварианта, — иначе реестр зачёл бы любой существующий файл. **T-57 — «ЗОЛОТЫЕ» ОТВЕТЫ.** Снимки ответов раскрытия и итогов реестра на зафиксированном наборе параметров плюс отдельная проверка состава полей: снимок легко обновить не читая, а список имён заставляет увидеть пропажу. **T-39 — ИНДЕКС «НАПРАВЛЕНИЕ + ДАТА».** Замер на живой базе в комментарии миграции, честно с оговоркой: стенд слишком мал, чтобы доказать выигрыш секундами, и решение опирается на форму запроса и на два УЖЕ существующих индекса той же формы. **ГРАБЛЯ, КОТОРУЮ ПОЙМАЛ ПРОГОН `latest → rollback → latest` НА ЖИВОЙ БАЗЕ:** простой откат ПАДАЕТ с «Cannot drop index … needed in a foreign key constraint». База, увидев составной индекс с той же первой колонкой, убирает одиночный индекс внешнего ключа как лишний — и единственной опорой ключа становится наш. Откат теперь сначала возвращает одиночный индекс. Проверены оба случая. |
| 2026-09-20 (после этапа 28, вторая находка) | **ШАПКА И ОТЧЁТ ПОКАЗЫВАЛИ РАЗНЫЕ ДЕНЬГИ. ТЕПЕРЬ ЭТО ВИДНО.** Живой проход: в шапке 1 749 839,09 ₽, в отчёте о движении денег 1 748 838,59 ₽. Разница 1 000,50 ₽ по одной кассе. Требование §15.1 ТЗ-2 — «сумма в шапке совпадает с итогом на экране счетов» — нарушалось, и заметить это можно было ТОЛЬКО сравнив два экрана глазами. **ПРИЧИНА — ОСТАТОК ХРАНИТСЯ ДВАЖДЫ:** в колонке счёта (быстро, её показывает шапка) и в проводках (правда, по ним отчёты). Пока суммы совпадают, о двойственности никто не думает. **ЗАВЕДЕНА ПРОВЕРКА В «КАЧЕСТВЕ ДАННЫХ»** — вкладка «Остатки разошлись»: счёт, хранимый остаток, остаток по проводкам, разница и итог «на столько врёт общий остаток». **ПРОДУКТ НЕ ЧИНИТ ЭТО САМ, И ЭТО РЕШЕНИЕ, А НЕ ЛЕНЬ:** какая сумма верна, знает только человек — колонка могла отстать от проводок, а могла правильно хранить начальные деньги, которых в проводках нет. Молча переписать деньги в базе — худшее, что здесь можно сделать. Порог — полкопейки: погрешность пересчёта валют не должна приучать не смотреть на этот отчёт. |
| 2026-09-20 (после этапа 28) | **ГЛАВНАЯ ПОКАЗЫВАЛА ЧИСЛА ЧУЖОГО ПЕРИОДА. ПОЧИНЕНО.** Найдено живым проходом по стенду, а не тестами. За август плитка доходов показывала 183 333,33 ₽ — ИЮЛЬСКУЮ выручку, хотя в августе доходов нет вовсе. Причём «за период» и «к прошлому периоду» совпадали до копейки, а изменение всегда выходило пустым, и это читалось как «сравнивать не с чем», а не как ошибка. **ПРИЧИНА — ОБЩЕЕ ХРАНИЛИЩЕ ОТЧЁТА.** Оно держит период в своих полях: сначала `setFilter`, потом `asyncInitialize` читает данные. Хранилище объявлено «своим на каждое место внедрения», но служба отчёта — одна на всё приложение, и внедрено оно в неё ОДИН раз. Главная зовёт отчёт ДВАЖДЫ ОДНОВРЕМЕННО — за текущий период и за прошлый; второй вызов переписывает период раньше, чем первый дочитает данные, и оба получают один отчёт. **ПОЧЕМУ ЭТОГО НИКТО НЕ ВИДЕЛ:** на демо-данных оба периода пустые, ноль равен нулю, и подмена не проявлялась. Спека главной подставляла отчёт заглушкой и гонку воспроизвести не могла в принципе. **ЧИНИТСЯ ОЧЕРЕДЬЮ В ЕДИНСТВЕННОЙ ДВЕРИ К ОТЧЁТУ,** а не переводом хранилища в «своё на запрос»: последнее потянуло бы за собой службу и всех, кто её зовёт, — планировщик, ИИ-аналитика, главную. Цена — отчёты строятся по очереди; неверное число дороже лишней секунды. Сбой одного построения очередь не запирает — проверено спекой. Проверено и обратное: без очереди два одновременных вызова получают один период (мутация). **ПРОВЕРЕНЫ ВСЕ ОСТАЛЬНЫЕ ОТЧЁТЫ:** служба отчёта зовётся дважды ещё в одном месте (`GetFinancialRatios`), но там последовательно — гонки нет. |
| 2026-09-20 (этап 28) | **ЭТАП 28 «Отраслевые демо и приёмка» ЗАКРЫТ ПО КОДУ. ОЧЕРЕДЬ БЛОКА Б4 ПУСТА: все тринадцать этапов ТЗ-2 закрыты.** **ГЛАВНОЕ В ЭТОМ ЗАХОДЕ — НЕ КОД, А ЖИВОЙ ПРОХОД.** Он снова нашёл то, чего не увидели ни типы, ни 3255 серверных проверок: блок «Кто приносит прибыль» был ПУСТ при выручке в полтора миллиона. Причина — КОНТРАГЕНТ ЗАПИСАН НЕ НА ТОЙ СТОРОНЕ ПРОВОДКИ: счёт покупателю кладёт его на сторону расчётов, а на стороне дохода этого поля нет НИ У ОДНОЙ проводки (проверено запросом к базе стенда: девять проводок дохода, контрагент у нуля из них). Запрос был правильный, ответ — пустой. Починено: контрагент берётся ИЗ ДОКУМЕНТА, на который ссылаются обе стороны. Спека воспроизводит ту же раскладку полей и ловит возврат ошибки. **ТРИ ОТРАСЛЕВЫХ НАБОРА, А НЕ ОДИННАДЦАТЬ.** У конкурента их одиннадцать — это верное направление и неверный первый шаг: одиннадцать наборов надо поддерживать в согласии с каждым изменением продукта. Услуги, торговля, строительство — самые частые случаи. **В КАЖДОМ НАБОРЕ ЕСТЬ БУДУЩИЙ КАССОВЫЙ РАЗРЫВ,** и это не пожелание, а проверка: чистая функция `projectDemoMoney` считает ряд остатков по самим данным, и спека падает, если разрыв исчез. При её написании вскрылась собственная ошибка: ПРОСРОЧЕННЫЙ СЧЁТ СЧИТАЛСЯ БУДУЩИМ ПОСТУПЛЕНИЕМ — то есть разрыв закрывался деньгами, которых человек как раз и не может получить. **ДЕМО НАПОЛНЯЕТСЯ ОБЫЧНЫМИ СЛУЖБАМИ ПРОДУКТА,** поэтому наборы лежат в модуле, а не в сидах базы: путь из ТЗ намекал бы на вставку в базу напрямую, чего здесь намеренно нет. **ВОПРОС ОБ ОТРАСЛИ В МАСТЕРЕ НАСТРОЙКИ НЕ СДЕЛАН — ОСОЗНАННО.** Вести ему некуда: реальную организацию демо-данными наполнять запрещает само ТЗ, а страница демо открыта только неавторизованным. Поле, которое никто не читает, — это тот самый дефект «заведено, но ничем не запускается». **`onboarding.spec.ts` НЕ ВКЛЮЧЁН В ПРОГОН.** ТЗ говорит «он уже написан», но написан он под ПРЕЖНИЙ англоязычный экран регистрации: продукт стал русскоязычным, и сценарий ищет подписи, которых нет. Включить — значит завести красный тест, который никто не чинит. **`items.spec.ts` НАПОЛНЕН, А НЕ УДАЛЁН:** три пустых теста с телом `() => {}` проходили зелёными всегда. Новый сторож `routes/e2eCoverage.spec.ts` держит и это, и список сценариев, и отсутствие паролей в исходниках. |
| 2026-09-20 (этап 27) | **ЭТАП 27 «Главная, контрагенты, сделки, справка» ЗАКРЫТ ПО КОДУ.** **ГЛАВНАЯ НАУЧИЛАСЬ ОТВЕЧАТЬ «НА КОМ ДЕРЖИТСЯ БИЗНЕС».** Парето по контрагентам и прибыльность направлений — два новых блока, и оба едут ТЕМ ЖЕ ответом `overview`: правило «пять запросов на главной недопустимы» соблюдено буквально. Ради него состояние переключателя сортировки направлений живёт в `OverviewSection`, а не внутри блока: собственный запрос блока разошёлся бы ключом с обзором, и запросов стало бы два. **ГРАФИК ПОКАЗЫВАЮТ ГЛАЗАМИ, ФРАЗУ ЧИТАЮТ.** У конкурента столбцы и линия — и всё; здесь под ними стоит «более 80 % выручки дают 3 клиента — это высокая зависимость», а единственный клиент назван отдельно: «более 80 % даёт 1 клиент» звучит как статистика, «вся выручка от одного клиента» — как предупреждение, которым это и является. Накопительная доля закрывается РОВНО сотней: сложение округлённых долей давало 99,99, и человек видел, что «чего-то не хватает». **ДОЛГ РАЗДЕЛЁН ПО ПРИРОДЕ, И СЧИТАЕТСЯ ПО САЛЬДО, А НЕ ПО ДОКУМЕНТАМ.** ТЗ требует прямо: зачёт аванса против отгрузки выполняется ДО классификации. Первый вариант расчёта разбирал документы по видам — и клиент, заплативший вперёд и тут же получивший товар, показал бы и аванс, и отгрузку, хотя не должен ничего. Разбор заведён СЧИТАЕМЫМ, без таблиц дебиторки и кредиторки: раздел 10 ТЗ отклоняет их прямо — это производное от проводок, и хранить его значит завести второй источник правды. **АВАНСЫ ЕДУТ В СВОДКЕ ДЕНЕГ, А НЕ ОТДЕЛЬНЫМ ЗАПРОСОМ** — по той же причине: одна строка не стоит второго запроса на самом частом экране. **ОТБОР СПИСКА ДЕЛАЕТ СЕРВЕР ПОДЗАПРОСОМ.** Списки разбиты на страницы: фильтровать загруженную страницу значило бы показать «ничего не найдено» при полной базе должников на следующей. Передавать найденные номера в адрес тоже нельзя — при большой базе это список на тысячи значений. «Без дебиторки» исключает ОБЕ природы сразу. **СПРАВКА ПОЯВИЛАСЬ У ВСЕХ 50 КОРНЕВЫХ ЭКРАНОВ, И КЛЮЧ ВЫВОДИТСЯ ИЗ АДРЕСА.** Проставить пометку на каждом экране руками — значит однажды забыть её на новом, то есть ровно на том, про который никто ничего не знает. Адрес читается из окна, а не хуком маршрутизатора: заголовок рисуется и вне маршрутов. **СТОРОЖ СПРАВКИ ПОЙМАЛ СОБСТВЕННОГО АВТОРА** — и правило оказалось грубее требования: «Вы» в НАЧАЛЕ предложения пишется с большой по обычному правилу русского языка, запрещать это значило бы требовать безграмотности. Проверка сужена до середины предложения и проверена мутацией. Мутацией проверен и сторож наличия: удаление одного ключа роняет его. |
| 2026-09-20 (этап 26) | **ЭТАП 26 «Бюджеты и личные настройки» ЗАКРЫТ ПО КОДУ.** **«ВЫПОЛНЕНО 56 %» САМО ПО СЕБЕ НЕ ЗНАЧИТ НИЧЕГО:** в июле это хорошо, в декабре — беда. Рядом встало «прошло времени» и ВЫВОД СЛОВАМИ («расходы идут быстрее плана: потрачено 90 % при прошедших 72 % времени») — два процента рядом читаются не всеми, фраза — всеми. Этого у конкурента нет. **РАСХОД И ДОХОД ЗЕРКАЛЬНЫ:** потратить больше времени — плохо, заработать больше — хорошо; один и тот же перекос значит разное. Допуск ±5 %, иначе вердикт менялся бы каждый день и его перестали бы читать; граница проверена явно. **НУЛЕВОЙ ПЛАН НЕ ДАЁТ ДОЛИ:** «н/о», а не ноль (читался бы как «ничего не потрачено») и не бесконечность (вообще не число). У завершившегося бюджета прошло ровно 100 %, не больше. Дата в спеке ЗАФИКСИРОВАНА — иначе спека меняла бы ответ каждый день и однажды покраснела бы сама. **ЛИЧНЫЕ НАСТРОЙКИ — В СХЕМЕ ОРГАНИЗАЦИИ:** бухгалтер, ведущий три компании, в одной хочет копейки, в другой нет, и это не противоречие. Хранение «ключ-значение», а не колонка на настройку: миграция на каждую галочку — плохой обмен. **СТОРОЖ РУЧЕК ЗАПИСИ ПОЙМАЛ НОВУЮ РУЧКУ.** Права организации на личную настройку спрашивать НЕЛЬЗЯ: тот, кто просто смотрит отчёты, иначе был бы обязан читать копейки, которых видеть не хочет. Ручка внесена в список открытых С ОБОСНОВАНИЕМ: вход по-прежнему нужен, и пишет она только строку самого вызывающего — `userId` берётся из сессии, а не из тела запроса. Прогон: сервер 3117/423, витрина 1495/183. |
| 2026-09-20 (этап 25) | **ЭТАП 25 «Платёжный календарь» ЗАКРЫТ ПО КОДУ.** Пять масштабов, два источника, «На сегодня». **ГЛАВНАЯ ЛОВУШКА — СЛОЖИТЬ ОСТАТКИ.** Поступления и выплаты за месяц складываются, а остаток нет: это состояние на конец, а не поток. Сложенные остатки дают число, которого нет ни в одном дне и ни в одной выписке. Остаток периода — остаток последнего его дня. **ФАКТ И ПЛАН В ОДНОМ СТОЛБЦЕ:** при масштабе крупнее дня прошедшая часть периода считается фактом, оставшаяся — планом. Смешав их молча, продукт показал бы план как свершившееся. Сегодняшний день — факт: он наступил. **НЕДЕЛЯ С ПОНЕДЕЛЬНИКА:** в JS воскресенье нулевое, и «неделя» разошлась бы с тем, как её считает человек. **ВЫХОДНЫЕ подсвечиваются только при дневном масштабе:** «выходная неделя» — бессмыслица. **НА УЗКОМ ЭКРАНЕ по умолчанию недели:** дневная таблица не помещается, и молча показать её значило бы отдать человеку таблицу, которую он не может прочитать. Дни остались в ответе рядом с укрупнением: на них держатся лента денег на главной, оповещение о разрыве и виджет шапки — ни один нынешний читатель ответа изменения не заметил. Прогон: сервер 3106/422, витрина 1495/183. |
| 2026-09-20 (этап 24) | **ЭТАП 24 «Единая шапка отчёта и ярусы прибыли» ЗАКРЫТ ПО КОДУ ЧАСТИЧНО.** **ШАПКА:** полоса периода доросла до каркаса управления — масштаб, способ построения, метод учёта. Каждый переключатель показывается ТОЛЬКО когда отчёт его поддерживает: неактивная кнопка хуже отсутствующей, она обещает возможность, которой нет. Всё пишется в АДРЕС — отчёт печатают и пересылают, «прибыль по кварталам кассовым методом» должна быть ссылкой, а не инструкцией из четырёх шагов. Опечатка в адресе молча отбрасывается, отчёт строится по умолчанию. Прежнее имя `ReportPeriodBar` оставлено рабочим, рядом добавлен псевдоним `ReportControlBar`: переименование ради переименования сломало бы три отчёта без пользы. **ЯРУСЫ ПРИБЫЛИ:** EBITDA, EBIT, EBT и чистая прибыль с рентабельностью. При НУЛЕВОЙ выручке рентабельность «не определена», а не ноль и не бесконечность: ноль читается как «работали в ноль», хотя работы не было. Ярус без данных НЕ ПРЯЧЕТСЯ — показывается с пояснением «амортизация не учитывается: нет операций этого вида»; исчезнув, он заставил бы думать, что продукт сломался. Порядок строк фиксирован и не зависит от порядка выбора. **ОСТАТОК ЭТАПА:** подключить полосу к каждому из 19 отчётов и вывести колонку «Тренд» в общий каркас таблицы. Это правка чужих экранов, и делать её надо с живым прогоном глазами — иначе сломается то, чего не видно в тестах. Прогон: сервер 3093/421, витрина 1495/183. |
| 2026-09-20 (этап 23) | **ЭТАП 23 «Реестр операций» ЗАКРЫТ ПО КОДУ** — состояния, итоги, отбор по состоянию, планы рядом с фактом. **СОСТОЯНИЯ ВЫВОДЯТСЯ, А НЕ ХРАНЯТСЯ.** Конкурент держит у операции ДВЕ даты и рисует буквы «Д»/«К». Вторую дату Bigfin завести не может — у него двойная запись, и это прямо в отклонённых идеях ТЗ. Те же состояния выведены из остатка документа и срока оплаты. Подписи СЛОВАМИ: «Д» надо заучить, «нам должны» понятно сразу. **ЦВЕТ ТОЛЬКО У БЕДЫ:** красный у просрочки; долг с будущим сроком и план — обычная жизнь, красить их значит приучить не замечать красный. Долг и просрочка — ДВА состояния, а не замена одного другим: первый ждёт, второй требует действия. **ИТОГИ ПОД ТЕМ ЖЕ ОТБОРОМ:** набор условий вынесен в общий помощник `applyTransactionFilters` и используется и списком, и сводкой. Свой запрос для итогов однажды разошёлся бы ТИХО — «87 операций» над списком из 84, и оба числа выглядят правдоподобно. Переводы в итог не входят и показаны отдельно. Отдельная лёгкая ручка, а не поле списка: список идёт бесконечной прокруткой. **ПЛАНЫ БЕЗ ДУБЛЕЙ:** материализованный план выбрасывается до показа — иначе один платёж виден дважды и итоги сходятся вдвое больше. Планы ложатся на ПЕРВУЮ страницу: реестр отсортирован по дате убыванию, а план — это будущее, он всегда впереди факта; на второй странице он выглядел бы ошибкой и повторялся при каждой прокрутке. Поменяется сортировка — место придётся пересмотреть (написано в коде). Опечатка в состоянии в адресе МОЛЧА отбрасывается: опечатался тот, кто прислал ссылку, а смотрит её другой. Прогон: сервер 3079/420, витрина 1488/182. |
| 2026-09-20 (этап 22) | **ЭТАП 22 «Группы счетов» ЗАКРЫТ ПО КОДУ.** Счета складываются в кучки («операционные», «депозиты», «личные»), панель денег показывает остаток по каждой. **ОТДЕЛЬНАЯ ТАБЛИЦА, А НЕ `parent_account_id`:** у счёта уже есть иерархия — план счетов, это бухгалтерское устройство. Пользовательская группировка — другое измерение; смешай их, и человек, перетащив счёт в свою кучку, сломает план счетов и отчёты. Тот же запрет прежнее ТЗ вводило для юрлиц и отделений. **`SET NULL`, А НЕ `CASCADE`:** удаление группы НИКОГДА не удаляет счета. Каскад унёс бы вместе со счётом всю историю операций. Правило продублировано в коде: полагаться только на базу нельзя, ссылки может не быть там, где миграции накатывались частями. **ОТСТУПЛЕНИЕ ОТ ТЗ, ОСОЗНАННОЕ:** ТЗ предлагало колонку и меню в списке счетов `/cashflow-accounts`, но этот экран помечен `@ts-nocheck`, а ЧАСТЬ A2 ТЗ запрещает трогать такие файлы в рамках работ по ТЗ. Управление вынесено на новый экран `/preferences/account-groups` — там же, где ТЗ и требует право `Preferences: mutate`. **УДАЛЕНИЕ СПРАШИВАЕТ ЧИСЛОМ:** «Перенести 4 счёта в Нераспределённые» вместо «Удалить группу» — второе человек читает как «удалить счета». Пустая настроенная группа показывается (человек её завёл), пустые «Нераспределённые» — нет (ничего не сообщают). **СТОРОЖ ПОЙМАЛ:** заголовок группы был прописными — в продукте запрет «подписи не кричат». Починен мой код. Прогон: сервер 3054/418, витрина 1486/182. |
| 2026-09-20 (этап 21) | **ЭТАП 21 «Виджет денег и разрывы по счетам» ЗАКРЫТ ПО КОДУ.** Остаток и ближайший разрыв видны с ЛЮБОГО экрана; в панели у каждого счёта своя дата разрыва и глубина. **ЗАЧЕМ:** остаток жил только на главной — уйдя в отчёты, человек терял из виду главный факт дня. А совокупный разрыв не говорил, НА КАКОМ счёте: деньги между счетами не переезжают мгновенно, и «Т-Банк, разрыв с 15.10 по 16.10» даёт день на переброску, а «разрыв 15 октября» — постфактум. **ГЛУБИНА ВАЖНЕЕ ФАКТА:** отдаётся не только «разрыв есть», но и сколько не хватит в худший день — это и есть число, на которое надо пополнить счёт. Опирается на массив интервалов из этапа 16. **ОДНА РУЧКА НА ВЕСЬ ВИДЖЕТ** и кэш на 60 с: шапка есть на каждом экране, три запроса на переходе = три запроса на каждый щелчок по меню. Кэш В ПАМЯТИ ПРОЦЕССА, а не в Redis: цена промаха — один лишний расчёт, цена общего хранилища — новая связь, которую надо поднимать и чинить; при переезде в несколько процессов место придётся пересмотреть (написано в коде). `staleTime` витрины совпадает с жизнью серверного кэша. **ЦВЕТ — ЭТО СРОЧНОСТЬ, А НЕ УКРАШЕНИЕ:** разрыв дальше 14 дней жёлтый (ещё можно занять, передвинуть платёж, поторопить должника), ближе — красный, начавшийся — красный. Граница проверена явно: сдвинься она на день, счёт с разрывом послезавтра выглядел бы спокойным. Точка НИКОГДА не единственный носитель смысла — рядом текст. **ЧЕСТНОСТЬ ПРИ СБОЯХ:** не построился прогноз по счёту — счёт всё равно показан со своим остатком и подписью «прогноз недоступен» (молча выбросить хуже: человек решит, что счёта нет); выключенный календарь показывает остаток и объясняет причину; сбой ручки виджета НЕ ломает шапку. **ПЛАНОВЫЕ БЕЗ СЧЁТА** считаются и показываются отдельной строкой: они есть в совокупном прогнозе, но в разрез по счетам попасть не могут. Прогон: сервер 3043/417, витрина 1486/182. |
| 2026-09-20 (этап 20) | **ЭТАП 20 «Схема „Куда попадает статья“» ЗАКРЫТ ПО КОДУ.** Три карточки — Деньги, Прибыль, Баланс — с подсветкой строк выбранной статьи и её оборотом рядом. **ЭТО ГЛАВНАЯ БЕДА ПРОДУКТА, названная владельцем:** человек не понимал связи между тем, что выбирает при разноске, и тем, что видит в отчётах. Экрана, объясняющего связь, не было вовсе. **ЧЕМ ЛУЧШЕ КОНКУРЕНТА:** у ПланФакта схема статична и отвечает «как устроен продукт вообще»; здесь она подсвечивает строку ВЫБРАННОЙ статьи и печатает её оборот — то есть отвечает «где окажутся МОИ деньги». **КАРТА ВЫВОДИТСЯ, А НЕ ХРАНИТСЯ** (решение D2): всё из вида статьи, раздела ДДС и наличия привязанных счетов. Таблица «статья → строка отчёта» не заводится — хранить производное значит завести второй источник правды. **ОБОРОТ СЧИТАЕТ ТОТ ЖЕ КОД, ЧТО ОТЧЁТ** (`ArticlesCashflowRollupService`): схема обещает «твоя сумма окажется вот здесь», и посчитай мы её заново — обещание однажды стало бы ложью. **СТАТЬЯ БЕЗ СЧЕТОВ НЕ ПОДСВЕЧИВАЕТ НИЧЕГО** и говорит почему: подсветка была бы обещанием, которое продукт не выполнит. «Ничего не подсветилось» и «эта статья ни на что не влияет» выглядят одинаково, а значат разное. Подсветка ФОНОМ и левой планкой, не цветом текста: цветной текст конфликтует с правилом «расход не красный» и читался бы как оценка. Отдельная вкладка, а не вторая половина экрана: три карточки рядом с деревом не помещаются на телефоне. Прогон: сервер 3043/417, витрина 1479/181. |
| 2026-09-20 (этап 19) | **ЭТАП 19 «Раскрытие по статье и переход в реестр» ЗАКРЫТ ПО КОДУ.** Щелчок по сумме в отчёте открывает те операции, из которых она сложилась, — на языке человека, а не выпиской по бухгалтерскому счёту. **ВТОРОЕ ИЗМЕРЕНИЕ, А НЕ ЗАМЕНА ПЕРВОГО:** вызов с `accountId` отвечает ПОБАЙТНО как прежде (новые поля `articleId`/`articleName` просто отсутствуют, а не равны `null`) — им пользуются Баланс, ОПиУ и старый ДДС. **ИТОГ СХОДИТСЯ С ОТЧЁТОМ,** потому что повторены оба правила расчёта: статья берётся ВМЕСТЕ с подстатьями (в отчёте родительская строка показывает поддерево) и учитываются только документы, РАССЧИТАННЫЕ ДЕНЬГАМИ — без закупок в долг и переводов между своими счетами. Правило берётся общим помощником `cashSettledReferenceKeys`, а не переписывается: две реализации одного правила однажды разойдутся, и разойдутся тихо. **У СТАТЬИ НЕТ ОСТАТКА** — ноль в этих полях означает «такого понятия у статьи не существует», а не «не посчитали». **ПЕРЕХОД В РЕЕСТР:** отбор `articleId` уходит В АДРЕС, ссылку можно переслать (конкурент передаёт такой переход состоянием и ссылку переслать не даёт). У денежной проводки статьи нет — статья висит на встречном счёте документа, поэтому отбираются документы, задевшие счета статьи, и остаются их денежные ноги. Неизвестная статья в адресе НЕ означает «показать всё»: список честно выходит пустым. Реестр рисует строку контекста «Из отчёта: …» с крестиком, снимающим только пришедшее из отчёта. Круговая проверка: адрес, собранный панелью, читается реестром теми же отборами — иначе ссылка открывала бы список БЕЗ отбора, и человек смотрел бы на чужие операции, считая переход рабочим. Прогон: сервер 3031/416, витрина 1479/181. |
| 2026-09-20 (этап 18) | **ЭТАП 18 «Отчёт „Деньги (ДДС по статьям)“» ЗАКРЫТ ПО КОДУ.** Главный денежный отчёт продукта: три раздела (операционный, инвестиционный, финансовый) → поступления и выплаты → дерево статей; блок переводов между своими счетами; пять форматов выгрузки. **ГЛАВНОЕ:** расчёт НЕ ДУБЛИРУЕТСЯ — отчёт зовёт `ArticlesCashflowRollupService`, тот самый, что питает график на главной. До этого этапа график считался прямым методом, а таблица в разделе «Отчёты» — косвенным: график и таблица показывали РАЗНОЕ, и это был дефект. **РАВЕНСТВО «начало + поток = конец» ДЕРЖИТСЯ КОНСТРУКЦИЕЙ:** чистый поток считается по ДЕНЕЖНОЙ стороне (`конец − начало`), а не сложением статей. Счёт без статьи в разбивку не попадает, и сумма статей может быть меньше движения — считай мы поток статьями, равенство разошлось бы по вине отчёта. Разница показана строкой «Не разнесено по статьям». **НАПРАВЛЕНИЕ ВЫВОДИТСЯ ИЗ ВИДА СТАТЬИ,** а не угадывается по знаку: доход, обязательство и капитал — приток; расход и актив — отток (вид обязан совпадать с корневым типом счёта, а тот задаёт нормальную сторону). Меню: «Деньги (ДДС)» ведёт на прямой отчёт, косвенный остался на месте и помечен `accountantOnly` — он нужен бухгалтеру. Новое право `READ_CASHFLOW_ARTICLES`. **ДВА СТОРОЖА ПОЙМАЛИ НОВЫЙ ЭКРАН:** системное поле даты браузера (у английского браузера период выглядел бы иначе, чем везде) и таблица без прокрутки (на телефоне правый край обрезается). Оба правила верны, починен мой код. **ГРАБЛЯ ПРОЦЕССА:** рабочая копия создаётся от `origin/develop` НА МОМЕНТ ПОСЛЕДНЕГО ОБНОВЛЕНИЯ ССЫЛОК — копия для этапа 18 отвязалась от develop БЕЗ этапа 17, и это было видно только по падению числа тестов (3012 → 2993). Перед созданием копии обновлять ссылки на сервер. Мутация ядра (поток статьями вместо денежной стороны; итог группы по всем строкам вместо корней) роняет 5 тестов из 14. Прогон: сервер 3026/415, витрина 1474/180. |
| 2026-09-20 (этап 17) | **ЭТАП 17 «Статьи пяти видов» ЗАКРЫТ ПО КОДУ, PR #523, статус `на ревью`.** Пять шагов: (1) домен пяти видов, раздел ДДС обязателен у балансовых, свёртка ОПиУ берёт только доходы и расходы; (2) колонка `seed_key` и догоняющая миграция на 11 балансовых статей; (3) строгая проверка `?kind=` и `seedKey` наружу; (4–5) пять вкладок со счётчиками, вкладка в адресе, пустое состояние с объяснением, замок на системных, ВИД ЗАБЛОКИРОВАН ПРИ ПРАВКЕ. **ПОЧЕМУ СУММЫ НЕ ИЗМЕНИЛИСЬ:** догоняющий сид заводит статьи БЕЗ привязки счетов — пока счёт не привязан, статья не собирает ничего. **ТРИ ПОПРАВКИ К ТЗ** (места без метки `[CODE-CONFIRMED]` оказались догадками): проверка совместимости счёта и вида уже работала обобщённо; отбор `?kind=` уже существовал вместе с обработкой дерева; правило «постоянный/переменный только у расходов» расширилось само. **ГЛАВНЫЙ РИСК ЭТАПА И КАК ЗАКРЫТ:** свёртку по статьям зовут ПЯТЬ расчётов — отбор поставлен в одном месте сборки, мутация роняет 3 теста из 4. Прогон: сервер 3012/414, витрина 1464/179. План и приёмка: `docs/superpowers/plans/2026-09-20-tz2-17-статьи.md`. ОСТАТКИ ВЛАДЕЛЬЦУ: живой проход глазами не делался; догоняющий сид НЕ прогонялся на живой базе стенда (этого требует DoD этапа). Дальше по очереди Б4 — **этап 18 «Отчёт „Деньги (ДДС по статьям)“»**. |
| 2026-09-20 (работа) | **ЭТАП 16 «Техническая подготовка» ЗАКРЫТ ПО КОДУ, PR #522, статус `на ревью`.** Все 4 шага: (1) `ArticlesCashflowRollupService` переехал в `ManagementArticles` и экспортируется оттуда; (2) `computeRunningBalance` отдаёт СПИСОК интервалов разрыва (`from`, `to`, `deepestAmount`, `deepestDate`), прежнее `gap` выводится из `gaps[0]`; (3) примитив `components/ui/sparkline.tsx` + 16 проверок + 6 историй + сторож §10.4 на зашитые цвета; (4) SDK перегенерирован ЛОКАЛЬНО. **ТРИ НАХОДКИ, КОТОРЫХ ТЗ НЕ ПРЕДВИДЕЛО.** (а) `ArticlesPlRollupService` переезжать было НЕ НУЖНО — он уже лежал в `ManagementArticles`; место в ТЗ не имело метки `[CODE-CONFIRMED]`. (б) SDK ОТСТАЛ НА 174 АДРЕСА: 273 → 447 путей. Витрина не видела больше трети возможностей сервера — а этапы 17–28 строят экраны поверх этих ручек. (в) SDK ЗВАЛ ДВЕ НЕСУЩЕСТВУЮЩИЕ РУЧКИ (`sale-estimates/{id}/notify-sms` и `/sms-details`, отправка КП по SMS) — на сервере их нет ни строчкой, обёртка тихо расходилась с сервером всё время, пока генерация была выключена. Убраны; в витрине их не звал никто. **ГЛАВНЫЙ УРОК ЭТАПА:** сторож вердикта главной в первой редакции мутацию НЕ ЛОВИЛ — в деньгах неразрывный пробел (U+00A0), а в тексте проверки обычный, и `not.toContain('300 000')` не совпадал никогда. Зелёный сторож ≠ работающий сторож. **ГРАБЛИ ОКРУЖЕНИЯ:** системный `pnpm` — девятка, проекту нужна `10.34.4` (лечится `corepack pnpm@10.34.4 …`); в свежей рабочей копии сначала надо собрать общие пакеты, иначе типы падают на «не найден `@bigfin/utils`». Дальше по очереди Б4 — **этап 17 «Статьи пяти видов»**. |
| 2026-09-20 | **ПРИШЛО ВТОРОЕ ТЗ — [MASTER-2.md](MASTER-2.md), блок Б4 «Вход в продукт», этапы 16–28.** Оно написано по живому проходу по ПланФакту и говорит одно: Bigfin не отстаёт по функциям (121 серверный модуль, 117 экранов, 19 отчётов против ~25 экранов и 3 отчётов у конкурента) — Bigfin отстаёт по ВХОДУ в функции. Семь мест, где конкурент лучше, и все семь про то, как человек попадает в возможность и понимает её: статьи как карта трёх отчётов, раскрытие любой суммы до операций, разрыв по каждому счёту в шапке на всех экранах, настройка отчёта в шапке вместо шторки, сводная строка реестра, встроенное обучение. Прежнее ТЗ не отменяется: блоки Б1–Б3 остаются как есть, Б4 к ним добавляется. Очередь блока — раздел 4, начало — этап 16 (техническая подготовка: пользователю не видна, но от неё зависят пять следующих). |
| 2026-09-19 (ночь) | **ЖИВОЙ ПРОХОД ПО ПРОДУКТУ — ВПЕРВЫЕ.** Открыл продукт глазами на ноутбуке и на телефоне. НАШЛОСЬ ПЯТЬ ДЕФЕКТОВ, которых не видели 4400 тестов, и все одного рода: продукт разговаривал с российским предпринимателем НА ЧУЖОМ ЯЗЫКЕ. (1) На главной рядом «1 749 839,09 ₽» и «0.00 RUB» — служба собирала формат вручную одной строкой, хотя §5.2 запрещает это прямо. Сторож на правило нашёл ТРЕТЬЕ такое место — мой собственный код того же вечера. (2) «3 операций ждут разноски» вместо «3 операции»: строка написана по-английски, где форм две, а в русском их три. Исправлено 8 строк. (3) Самый частый экран — «Все операции» — показывал «500,000.00»: валюту учёта не передавали, и общий помощник считал её неизвестной. (4) Там же `"formatted_status": "Manual"` — подписи состояния зашиты английскими словами мимо словарей. (5) Строки документов считались по-английски, хотя итог того же счёта — верно: валюту клали в «настройки», а читали из «окружения». ГЛАВНЫЙ УРОК: тест проверяет «поле заполнено», а «поле заполнено ПОНЯТНО» проверяет только человек. PR #514, #515, #516 |
| 2026-09-19 (ночь) | **ЗАДЕЛ ЗАКРЫТ ПОЛНОСТЬЮ.** Д1: замер остатка перемерен честно — «в файле есть @blueprintjs» показывал 49 файлов, а рисующих старым оказалось 20; из них переведены настройки, остальное — ФОРМЫ, и их перевод это подмена поведения прокси-признаком (поля форм сами и есть старая библиотека). Д3: карточки вместо таблиц-простынь на 14 списках; живая проверка на 390 точках нашла «1140000» без пробелов — блок сам разбирает число и готовую строку. Д4: сбой на 14 экранах-списках виден человеку, а не выглядит как «данных нет». Долг по адресам ОБНУЛЁН: за каждой из трёх строк стоял мёртвый код. Слепая зона типов: витрина 87 → 71, сервер 9 → 6; корень — `R.pipe` в примесях отчётов, из-за которого проверка не видела ни одного метода. Формы: решение «не переводить массово» закреплено границей из 67 форм. PR #507…#513 |
| 2026-09-19 (вечер) | **К9 ЗАКРЫТ — БАЛАНС ОДНОГО ЮРЛИЦА СХОДИТСЯ.** В балансе появилась строка «Расчёты внутри группы». Главное решение: строка СЧИТАЕТСЯ, а не пишется в базу. Записать пару проводок значило бы завести два счёта, накатить миграцию на все организации и ПЕРЕПРОВЕСТИ всю историю — иначе у старых переводов встречного требования не появится. Арифметика: величина требования равна перекосу отбора с обратным знаком (двойная запись гарантирует, что по всем проводкам дебет равен кредиту, значит перекос — это ровно ноги, ушедшие к другим юрлицам). Цена — ноль лишних запросов: перекос складывается из строк, которые отчёт уже получил. ЖИВАЯ ПРОВЕРКА ПОСЛЕ ВЛИВАНИЯ НАШЛА ИЗЪЯН ПОКАЗА: у ИП вышло «Активы 0» — деньги на счету есть, а имущественная строка со знаком минус их гасила. Сторона строки теперь выбирается по знаку итога: должны тебе — имущество, должен ты — обязательство (#505). Замер на стенде: все юрлица 2 863 838,59 = 2 863 838,59 без строки; ООО сходится, требование +500 000; ИП активы 500 000 = обязательства 500 000. PR #504, #505 |
| 2026-09-19 (вечер) | **К4 ЗАКРЫТ — И ОКАЗАЛСЯ НЕ ВОПРОСОМ ВЛАДЕЛЬЦУ.** В заделе он стоял как «нужен ответ, по какому курсу сводить». Ответ уже есть В САМОМ ТЗ (§7.4): по курсу НА ДАТУ ОПЕРАЦИИ. И продукт так считает с самого начала — дебет и кредит хранятся в валюте группы, пересчитанные на дату операции, а исходная сумма и курс лежат рядом. Это хорошо: пересчёт на лету означал бы, что вчерашний отчёт сегодня показывает другие числа. ЧЕГО НЕ ХВАТАЛО — человек не видел, В ЧЁМ цифры. Добавлена подпись в шапке всех трёх отчётов, и ТОЛЬКО когда валют в группе больше одной: постоянная подпись «суммы в рублях» — шум, который перестают читать вместе со всей шапкой. Пустая валюта юрлица = «как у группы», а не «другая»: колонка появилась позже юрлиц, и иначе многовалютной стала бы каждая вторая группа. Курсовые разницы отдельной строкой в ОПиУ уже были (счёт «Курсовые разницы», прочие расходы). Сторож проверяет все три отчёта, мутация красит. PR #506 |
| 2026-09-19 (вечер) | Долг `@ts-nocheck`, слой ХУКОВ ЗАПРОСОВ: витрина 107 → 102 файла. Второй общий корень за вечер: все хуки запросов устроены ОДИНАКОВО — принимают необязательные настройки и передают их в react-query, а помощники сброса кеша получают клиента запросов; ни то, ни другое нигде не было объявлено. Заведён `hookTypes.ts`, и пять файлов вычищены полностью, включая САМЫЙ ТЯЖЁЛЫЙ (кредит-ноты: 42 замечания → 0). НАЙДЕН НАСТОЯЩИЙ ДЕФЕКТ: сброс кеша одной записи вызывался как `invalidateQueries(t.ЗАПИСЬ, номер)` — номер уходил во ВТОРОЙ параметр, где ждут условия отбора, и сбрасывался кеш ВСЕХ записей вместо одной (соседние файлы пишут правильно — одним списком). ГЛАВНАЯ ПОЛЬЗА ТИПИЗАЦИИ: она вскрыла 14 несоответствий У ПОТРЕБИТЕЛЕЙ хуков — все одной природы, «запись ещё не выбрана». Правда оказалась на стороне потребителей: номер записи МОЖЕТ БЫТЬ НЕ ИЗВЕСТЕН (шторка не открыта, документ не выбран), и требовать его всегда значило заставлять каждого вызывающего врать — подставлять ноль. Типы хуков расширены по правде, попутно там же: «письмо ещё не загружено» это `undefined`, а не `null` — `null` читался бы как «загрузили и не нашли». Прогон: витрина 1327/168, типы чисты. |
| 2026-09-19 (вечер) | Долг `@ts-nocheck`, ВИТРИНА: 116 → 107 файлов. Замер слепой зоны витрины впервые сделан целиком: 116 файлов, 1564 замечания, МИНИМУМ 8 НА ФАЙЛ (лёгкого слоя «пять и ниже» там нет вовсе), и 71% замечаний — «тип не указан» у свойств компонентов. НАЙДЕН ОБЩИЙ КОРЕНЬ ДЛЯ ЦЕЛОГО СЕМЕЙСТВА: движок таблицы передаёт КАЖДОЙ ячейке одну и ту же тройку (колонка, строка, посылка), а каждый компонент-ячейка расписывал её заново или не расписывал вовсе. Заведён общий тип `DataTableCellProps` — и ВСЕ ДЕВЯТЬ ячеек таблицы вычищены разом, включая четыре, которые в слой по счёту замечаний даже не попадали. Попутно единообразно исправлено: «нет подсветки ошибки» — это ОТСУТСТВИЕ значения, а не пустое значение (было `null`, стало `undefined`); ссылка на поле ввода объявлена как живой элемент страницы; параметрам обработчиков даны настоящие типы, а не `any`. ДОКАЗАТЕЛЬСТВО НЕИЗМЕННОСТИ: сняты 11 снимков отчётов на живом стенде ДО и ПОСЛЕ переписывания движка (баланс, ОПиУ, ДДС × разрезы, сравнения, проценты) — СОВПАЛИ ДО БАЙТА. Прогон: витрина 1327/168, типы чисты. |
| 2026-09-19 (вечер) | Долг `@ts-nocheck`, слой «девять замечаний и ниже»: слепая зона СЕРВЕРА 14 → 10 файлов, 40 замечаний разобрано, 16 осталось в двух файлах. ЧИСТО ТЕПЕРЬ: периоды ДДС, периоды ОПиУ, прошлый период ОПиУ, колонки периодов ОПиУ. ЧЕТЫРЕ ОБЩИХ КОРНЯ вместо сорока правок: (1) ПЕРЕЧЕНЬ ВРАЛ ПРО НОМЕР УЗЛА — утверждал, что у ЛЮБОГО узла отчёта номер это название раздела, хотя у строки-счёта там номер счёта; (2) ДАТЫ СРАВНЕНИЯ объявлены обязательными, хотя появляются только при включённом сравнении — и нашлась ВТОРАЯ КОПИЯ того же перечня в ОПиУ, где их уже сделали необязательными в общем, а здесь нет (две копии одного правила разошлись, и видно это было только по странным замечаниям); (3) КАРРИРОВАНИЕ: восемь функций были обёрнуты `R.curry`, и проверка типов считает результат такой функции «чем-то» — передать его как обработчик нельзя; частичное применение переписано явными замыканиями, смысл тот же; (4) ПЕРЕЧНИ ДДС путали САМ СЧЁТ и РАЗДЕЛ СО СЧЕТАМИ. ПОПУТНО НАЙДЕН ДЕФЕКТ: подпись колонки периода при неопознанной единице времени возвращала ВООБЩЕ НИЧЕГО — человек видел столбец чисел без заголовка; теперь берётся самая подробная подпись. В Балансе композиция примесей развёрнута из `R.pipe` во вложенные вызовы (9 → 7 замечаний и другая их суть: раньше не виден был НИ ОДИН метод примесей). Два файла с композицией примесей оставлены следующему слою — директива возвращена с описанием, что именно осталось. Прогон: сервер 2930/405, типы чисты. |
| 2026-09-19 (вечер) | ПОЧИНКА ГЛАВНОЙ ПРОВЕРЕНА ВЖИВУЮ + НАЙДЕНА СЛАБОСТЬ САМОГО СТЕНДА. Замер после починки: Баланс 0,060 с, ОПиУ 0,051 с, ДДС 0,092 с, Баланс по юрлицу 0,046 с, ГЛАВНАЯ 0,141 с (была бесконечность), внутригрупповые обороты 0,021 с. ДОЛГИЙ РАЗБОР, ИЗ КОТОРОГО ГЛАВНЫЙ УРОК: ЗАСТРЯВШИЙ СЕРВЕР ПОРТИТ ВСЕ ПОСЛЕДУЮЩИЕ ЗАМЕРЫ. Node однопоточный: один запрос в вечном цикле занимает процесс НАВСЕГДА, и любой следующий запрос выглядит «зависшим». Я успел заподозрить по очереди отчёт дебиторки, сводку по деньгам, платёжный календарь и ОПиУ по месяцам — ВСЕ ОНИ БЫЛИ ЧИСТЫ, каждый отвечает за 0,2 с на свежем процессе. Настоящая причина была одна и уже починена. ВТОРАЯ ГРАБЛЯ: мягкий сигнал остановки застрявший процесс ИГНОРИРУЕТ — до обработчика он не доходит, цикл событий занят. Из-за этого скрипт стенда не мог его остановить, проверка здоровья честно не дожидалась ответа, и стенд ОТКАТЫВАЛСЯ на прежнюю версию раз за разом, хотя код был исправен: я несколько замеров подряд проверял СТАРЫЙ код и считал, что починка не работает. Скрипт стенда исправлен: через 10 секунд после вежливой остановки — принудительная. Зонды в собранный файл на стенде (временные, сняты) оказались единственным способом увидеть, где именно застревает запрос. |
| 2026-09-19 (вечер) | К8 (замер скорости) ЗАКРЫТ ЗАМЕРОМ И НАШЁЛ ГЛАВНУЮ ПОЛОМКУ СЕССИИ: ГЛАВНАЯ СТРАНИЦА УБИВАЛА ВЕСЬ СЕРВЕР. Замер на стенде: Баланс 0,054 с, ОПиУ 0,050 с, ДДС 0,057 с, Баланс по одному юрлицу 0,045 с — быстро. А `dashboard/overview` не отвечал 120 с, после чего процесс падал с «JavaScript heap out of memory». ПРИЧИНА В ОДНОЙ СТРОКЕ: в запросе отчёта стояло `displayColumnsBy: 'date_periods'` — это НАЗВАНИЕ РЕЖИМА КОЛОНОК, а не единица времени. `moment` НЕ РУГАЕТСЯ на неизвестную единицу: `add` возвращает ту же дату, `startOf`/`endOf` её не двигают — цикл по периодам становится БЕСКОНЕЧНЫМ и складывает записи, пока не кончится память. Падает ВЕСЬ СЕРВЕР, а не один запрос. ПОЧИНЕНО ДВОЙНО: (1) на главной разрез по месяцам; (2) сам помощник шага по датам теперь сразу говорит об ошибке, если единица НЕ ДВИГАЕТ дату — молчаливое зацикливание нельзя ни заметить, ни отладить. ГРАБЛЯ-ЗЕРКАЛО: подставной отчёт в спеке главной различал вызовы по тому же неверному полю `displayColumnsBy` — заглушка повторяла ошибку кода и потому не могла её заметить; теперь различает по `displayColumnsType`. ЛОЖНАЯ ТРЕВОГА, снятая повторным замером: «внутригрупповые обороты 43 с» были следствием падения сервера, отдельно отчёт отвечает за 0,11 с. УРОК: замер времени ответа — это ОТДЕЛЬНЫЙ вид проверки; ни один из 2930 тестов сюда не заглядывал, запрос просто «долго думал». +7 проверок. Прогон: сервер 2930/405. |
| 2026-09-19 (вечер) | Остатки О-v90-1 и О-v91-1 ЗАКРЫТЫ. О-v90-1: описание ответа по счёту отставало от кода на 14 ПОЛЕЙ — сервер их отдавал, не объявляя. Это не «документация для красоты»: из описания собирается библиотека типов для витрины и внешних интеграций, и поля, которого там нет, для них НЕ СУЩЕСТВУЕТ. Тот, кто его читает, делает это в обход договора, и однажды поле исчезает молча — ровно так уже терялся номер чека. Поля дописаны; НОВЫЙ СТОРОЖ `responseDtoMatchesTransformer.spec.ts` сверяет то, что отдаёт преобразователь, с тем, что обещает описание (мутация роняет). О-v91-1: поле ответа звалось `formatttedTotal` — с лишней буквой. Читать его было НЕЧЕМ: витрина считает итог сама, а имя с опечаткой не совпадало ни с чем; поиск по правильному имени его не находил, и поле лежало в ответе мёртвым грузом. Переименовано; проверено, что опечатку не читает никто во всём репозитории, включая библиотеку типов. Прогон: сервер 2923/404. |
| 2026-09-19 (вечер) | Остаток О6 ЗАКРЫТ, ЭТАП 4 ЗАКРЫТ ПОЛНОСТЬЮ (О1–О6): отбор отчётов по НАПРАВЛЕНИЮ. Опирается на справочник, построенный тем же вечером. ПРАВИЛО ПРОЩЕ, ЧЕМ У ЮРЛИЦ, и это главное, что можно было перепутать: юрлицо — часть УСТРОЙСТВА группы (пусто = вся группа, плюс надо исключать внутренние переводы, чтобы не посчитать деньги дважды), а направление — просто ЯРЛЫК (пусто = все операции, исключать нечего: перевод между направлениями это перекладывание внутри одного кармана). Лишнее исключение молча урезало бы выручку. ОПЕРАЦИИ БЕЗ НАПРАВЛЕНИЯ ПРИ ОТБОРЕ ВЫПАДАЮТ — это правильно («сколько заработала розница», а не «розница плюс всё непомеченное»), но сказано подписью под полем: иначе итог окажется меньше общего без видимой причины. Отбор наложен в том же ОБЩЕМ месте всех трёх отчётов, что и юрлицо; мутация роняет. Поля нет, пока направление одно — правило показа взято общее, второй копии нет. Сервер +12 проверок, витрина +9. Прогон: сервер 2920/403, витрина 1327/168, словари 4700/4700. |
| 2026-09-19 (вечер) | РАЗДЕЛ «НАПРАВЛЕНИЯ» (ПРОЕКТЫ) ПОСТРОЕН — пятая «мёртвая» возможность закрыта. Колонка `project_id` была у проводок с самого начала, поля «Проект» стояли в формах операций — а завести направление было НЕГДЕ: серверных ручек не существовало ни одной, и поля всегда оставались пустыми. РЕШЕНИЕ ПО ОБЪЁМУ: построен СПРАВОЧНИК ЯРЛЫКОВ, а не старый модуль управления проектами с задачами, часами и billable-записями. Причина: задачами и сроками в Bigfin заняты «Сделки» (это решение записано в коде маршрутов ещё картой v16), а предпринимателю нужен разрез «розница / опт / объект на Ленина», по которому он раскладывает деньги. Поэтому полей в форме РОВНО ДВА — название и состояние: лишние поля это вопросы, на которые человек не знает ответа и которые останутся пустыми. Сервер: модель на существующей таблице `projects` (миграции не нужно), CRUD, права на изменение настроек ВМЕСТЕ со стражем. НАПРАВЛЕНИЕ С ОПЕРАЦИЯМИ УДАЛИТЬ НЕЛЬЗЯ — прошлые операции остались бы со ссылкой в никуда; кнопка удаления прячется там, где сервер откажет. Витрина: экран `/directions`, пункт в «Справочниках», хуки, состояния загрузки/ошибки/пустоты. ГРАБЛЯ: `tenantKnex` это ФУНКЦИЯ, ВОЗВРАЩАЮЩАЯ knex, а knex сам зовётся с именем таблицы — заглушка, перепутавшая один уровень, падает «knex is not a function». Сервер +10 проверок, витрина +13. Прогон: сервер 2908/402, витрина 1318/167, словари 4698/4698. ОСТАЁТСЯ О6: отбор отчётов по направлению — теперь ему есть на что опереться. |
| 2026-09-19 (вечер) | Остаток Ю5 ЗАКРЫТ: в плане счетов появилась колонка «Юрлицо» — и ТОЛЬКО когда юрлиц больше одного. Правило «не навязывать» (§8.5): организация с одним юрлицом не должна видеть следов группы, а колонка с одним и тем же словом в каждой строке — именно такой след. Выбран план счетов, потому что юрлицо задаётся у СЧЁТА и именно оно решает, чьей считается операция: видеть связь надо сразу, а не открывая каждый счёт. Показывается название, а не номер; счёт без юрлица даёт прочерк, а не пустоту (пустая ячейка читается как «ещё не загрузилось»). Правило показа взято общее — второй копии не заведено. Витрина +8 проверок, 1305/160. |
| 2026-09-19 (вечер) | К3 ЗАКРЫТ ЖИВОЙ ПРОВЕРКОЙ — ПЕРВОЙ НАСТОЯЩЕЙ ПРОВЕРКОЙ КОНСОЛИДАЦИИ. На стенде заведено второе юрлицо «ИП Демидов», ему выдан счёт, между юрлицами сделан перевод 500 000. ПОДТВЕРДИЛОСЬ: перевод АВТОМАТИЧЕСКИ распознан внутригрупповым и виден в отчёте «Внутригрупповые обороты»; СВОДНЫЙ баланс сходится (2 863 838,59 = 2 863 838,59); ОПиУ группы равен сумме ОПиУ юрлиц (разница 0,00). НАЙДЕНО: баланс ОДНОГО юрлица НЕ СХОДИТСЯ ровно на сумму перевода (ООО: активы 2 363 838,59 против пассивов 2 863 838,59; ИП: 500 000 против 0). Причина не в расчёте: внутренний перевод кладёт вторую ногу на счёт ДРУГОГО юрлица, а встречного требования к своему же в балансе нет — расчётов внутри группы продукт не ведёт. ТЗ требует сходимости только от СВОДНОГО баланса, и она есть, но молчать нельзя. Сделано: разрез сообщает `balanceMayNotConverge`, и ПОЯСНЕНИЕ О РАЗРЕЗЕ ВЫВЕДЕНО НА ЭКРАН во всех трёх отчётах — сервер считал его с самого начала, а показывал НИКТО (четвёртая возможность, существовавшая только в ответе). В задел добавлен К9 «расчёты внутри группы» — решение уровня владельца. НА СТЕНДЕ ОСТАВЛЕНЫ: второе юрлицо, его счёт 1043 и журнал CHK-INTERCO-1 — они и есть доказательство, и на них можно проверять консолидацию дальше. Прогон: сервер 2898/401, витрина 1297/159, словари 4679/4679. |
| 2026-09-19 (вечер) | Остаток К5 ЗАКРЫТ + БАЛАНС НЕ СХОДИЛСЯ У ТЕХ, КТО ПЛАТИТ ЛИЧНОЙ КАРТОЙ. Тип счёта «Личные средства» помечен как балансовый, но НИ В ОДНОМ разделе схемы баланса его не было — такие счета не попадали в отчёт вообще. Ничего при этом не падало: обе стороны считались, они просто были РАЗНЫМИ. Добавлен раздел «Заём от учредителя» — в ОБЯЗАТЕЛЬСТВАХ, как требует §8.2: собственник платит за фирму своими деньгами, значит фирма должна ему. Счёт остаётся дебетовым (с него ПЛАТЯТ, как с кассы), поэтому остаток на балансе РАЗВОРАЧИВАЕТСЯ — иначе обязательство показывалось бы со знаком минус. Приём не новый: так же разворачивается накопленная амортизация, и обе причины остались раздельно описаны. Сверка: собственник платит 1000 с личной карты → расход 1000 уменьшает капитал, заём +1000 в обязательствах, обе стороны сходятся в ноль. +7 проверок. Прогон: сервер 2898/401. |
| 2026-09-19 (вечер) | Остаток К6 ЗАКРЫТ + ТРЕТЬЯ «МЁРТВАЯ» ВОЗМОЖНОСТЬ ЗА СЕССИЮ. Правило §8.4 («бухгалтеру ИП видно только ИП, владельцу — всё») было написано и проверено, но ЕГО НИКТО НЕ ЗВАЛ: хранить список было негде, спросить роль — неоткуда. Сделано: колонка `allowed_legal_entity_ids` у роли (ПУСТО = ВСЕ: владелец не должен ничего настраивать, чтобы видеть свою организацию); сервис `GetLegalEntityAccessService` спрашивает роль НА КАЖДЫЙ ОТЧЁТ (устаревший кеш ПРАВ хуже отсутствующего); запрос чужого юрлица — ОТКАЗ, а не пустой отчёт (пустота читается как «нет операций», человек поверит и будет неправ); смешанный запрос тоже отказ — тихое усечение не заметят; правило подключено во ВСЕХ трёх отчётах (забытый отчёт не падает, он показывает чужие деньги); выбор юрлиц выведен в форму роли, поля нет пока юрлицо одно. СТОРОЖ «СЕРВЕР СОБЕРЁТСЯ» РАСШИРЕН и на этом же поймал себя: теперь проверяет КОНТРОЛЛЕРЫ, а не только сервисы, и ДВА условия вместо одного (хозяин экспортирует И потребитель импортирует); первая версия искала модуль-хозяин только в собственном каталоге класса и не находила его для класса из `queries/` — молча пропускала ровно тот случай, ради которого писалась (поймано мутацией). Сервер +15 проверок. Прогон: сервер 2891/400, витрина 1288/158, словари 4677/4677. ТЕНАНТНУЮ МИГРАЦИЮ НАДО ПРИМЕНИТЬ НА СТЕНДЕ. |
| 2026-09-19 (вечер) | Остаток Д2 ЗАКРЫТ: документ палитры переписан. ОН СУЩЕСТВОВАЛ, НО ВРАЛ — описывал тёмную тему как основную и жёлтые кнопки, то есть палитру, от которой продукт ушёл ещё при переработке оформления. Устаревшее описание ХУЖЕ отсутствующего: следующий построил бы по нему тёмный экран с жёлтой кнопкой, и никакая проверка бы не возразила. Новый документ описывает не «какие цвета», а ПОЧЕМУ они такие: холст почти белый (структура держится на волосяных линиях, а не на серой подложке), РАСХОД НЕ КРАСНЫЙ (расход — норма работы, красный оставлен проблемам; отсюда же «рост не всегда зелёный»), жёлтый метит один смысл и не заливает площадь, скруглений два (оболочки 10px, органы управления 6px). Значения не дублируются: единственный источник — `tokens.css`. НОВЫЙ СТОРОЖ `colorsDocMatchesTokens.spec.ts` сверяет каждое имя токена и каждый `#HEX` из документа с файлом токенов; мутация (подмена одного цвета в документе) роняет его. Витрина 1288/158. |
| 2026-09-19 (вечер) | Остаток К7 ЗАКРЫТ + НАЙДЕНА ВТОРАЯ «МЁРТВАЯ» ВОЗМОЖНОСТЬ. §8.3 «печатные формы по реквизитам юрлица» НЕ РАБОТАЛА НИ РАЗУ: код спрашивал `invoice.legalEntity`, а такой связи у счёта НЕ СУЩЕСТВОВАЛО — ответ всегда `undefined`, и счёт на оплату молча печатал общие настройки аккаунта. Остальные четыре формы (счёт-фактура, УПД, акт, ТОРГ-12) не спрашивали и этого. Цена ошибки высокая: контрагент платит по тем реквизитам, что видит, — документ от ООО с чужим расчётным счётом выглядит совершенно нормально. Починено: связь объявлена у модели и ЗАГРУЖАЕТСЯ вместе со счётом (объявленная, но не загруженная связь — то же самое, что её отсутствие); все пять форм переведены на общий подбор через новый `sellerMetadataFor` — он накладывает реквизиты юрлица поверх настроек организации ОДИН раз, а формы продолжают читать то же, что читали (переписывать десяток мест значило бы десять раз повторить правило и однажды забыть его в одиннадцатом). Правило «всё или ничего» сохранено: отката по каждому полю нет. Не заменяются должность подписанта и главбух — это не реквизиты компании, а настройка того, КТО подписывает. Сторож `formsUseLegalEntityRequisites.spec.ts` (14 проверок) следит за всеми пятью формами и за загрузкой связи; мутация роняет. УРОК: проверка на слова из КОММЕНТАРИЯ всегда красная — `activeCode` их выбрасывает, и это правильно: комментарий можно оставить, а правило убрать. Прогон: сервер 2878/399. |
| 2026-09-19 (вечер) | Остаток К2 ЗАКРЫТ: появился выключатель «операция внутри своей группы» — в приходе, расходе и записи в журнал. До этого признак ставился ТОЛЬКО автоматически, и консолидация работала наполовину: автоматика видит юрлицо СЧЁТА, а «неизвестное юрлицо не считается другим», поэтому перевод собственной компании, оформленный документом на контрагента, она не распознаёт НИКОГДА — и в сводном отчёте одна выручка считается дважды. ГЛАВНОЕ ПРАВИЛО: ручная отметка только ДОБАВЛЯЕТ признак, снять автоматический ею нельзя (иначе стало бы можно дважды посчитать одну выручку). Признак живёт у ДОКУМЕНТА, а не у проводки: проводки при правке пересобираются заново, а выбор человека обязан её пережить; миграция заводит колонку у ОБЕИХ ручных операций — выключатель, работающий через раз, хуже отсутствующего. Поля нет вовсе, пока юрлицо одно (§8.5); правило показа взято общее (`shouldShowLegalEntityBreakdown`), второй копии не заведено. ГРАБЛЯ: у форм денег БЕЛЫЙ СПИСОК полей — первая версия правки выглядела рабочей (галочка нажимается, форма сохраняется) и не делала НИЧЕГО; сторож теперь следит именно за этим. Для прежних форм на Formik сделан отдельный вариант поля — не из-за оформления, а потому что у RHF и Formik нет общего способа прочитать значение. Сервер +13 проверок, витрина +15. Прогон: сервер 2864/398, витрина 1283/157, словари 4675/4675. ТЕНАНТНУЮ МИГРАЦИЮ НАДО ПРИМЕНИТЬ НА СТЕНДЕ РУКАМИ: автообновление накатывает только системные. |
| 2026-09-19 (вечер) | Остатки Ю3 и Ю4 ЗАКРЫТЫ + НАЙДЕНА ПОТЕРЯ ДАННЫХ. Справочник юрлиц отдавал 9 полей из 17, а форма правки шлёт на сервер ВСЕ свои поля разом. Поле, которого в списке нет, форме нечем заполнить — она отправляла пустое, и сервер честно затирал КПП, ОГРН, директора и адрес. Человек открывал юрлицо, правил название, сохранял — и терял половину реквизитов; запрос успешен, ответ 200, ничего не падает. Список расширен до всех изменяемых полей; НОВЫЙ СТОРОЖ `listReturnsEditableFields.spec.ts` сверяет поля DTO с полями строки списка (мутация — снятие одного поля — роняет его). Это не частный случай, а целый вид поломок: «форма возвращает меньше, чем получила». Ю3: на экран выведены полное наименование, НДС, валюта учёта, руководитель, оба адреса, банк (название, БИК, р/с, к/с), «головное» и «действует» — раньше сервер их принимал, а изменить было нельзя. Банковские поля собираются в ОДИН объект теми же ключами, что читают печатные формы; пустые не уходят вовсе (пустая строка в реквизите выглядит как «заполнено» и ломает счёт). НДС НЕ ВЫВОДИТСЯ из налогового режима: на упрощёнке НДС бывает, на общей — освобождение; угадать значит однажды выставить счёт с неверным НДС и промолчать. Ю4: колонка «Состояние» («Действует» / «Не действует») — приглушённую строку легко не заметить, и человек ищет, почему юрлица нет в отборах. ГРАБЛЯ: банковские реквизиты в MySQL лежат колонкой JSON и приходят то объектом, то строкой — разбор добавлен. Сервер +4 проверки, витрина +6. Прогон: сервер 2851/397, витрина 1267/157, словари 4673/4673. |
| 2026-09-19 (вечер) | СТЕНД ЛЁГ И БЫЛ ПОДНЯТ. Картинка структуры баланса (#482) берёт класс `BalanceSheetApplication`, а модуль Баланса его наружу НЕ ОТДАВАЛ — Nest отказался собрать зависимость и упал на старте. САМАЯ ТИХАЯ ПОЛОМКА ИЗ ВОЗМОЖНЫХ: сборка проходит, типы сходятся, 2847 проверок зелёные, а сервер не запускается вовсе; узнать можно только по журналу службы. Починено три вещи: (1) класс вынесен в `exports`; (2) НОВЫЙ СТОРОЖ `injectedClassesAreExported.spec.ts` — читает сервисы модуля отчётов, достаёт из конструкторов классы соседних модулей и проверяет, что модуль-хозяин их экспортирует (мутация роняет 2 теста; первая версия сторожа сама искала пути от неверного корня — `modules/modules/...`); (3) СКРИПТ СТЕНДА ТЕПЕРЬ ОТКАТЫВАЕТСЯ, когда новый код не запускается: раньше он честно писал «стенд не ответил» и оставлял его лежать, потому что снимок рабочей версии стирался ДО перезапуска и откатываться было нечем. УРОК ОБЩИЙ: зелёный прогон не доказывает, что сервер поднимется — это проверяет только живой запуск. |
| 2026-09-19 (вечер) | Остаток О4 ЗАКРЫТ, ЭТАП 4 ЗАКРЫТ ЦЕЛИКОМ (О1–О5 все закрыты, О6 «фильтр проекта» ждёт серверной части раздела). Суть дефекта: у ОПиУ есть переключатель «кассовый / по начислению», отчёт его слушался, а КОЛОНКА «ОТКЛОНЕНИЕ» — НЕТ: факт она всегда считала по начислению. На кассовом отчёте план сравнивался с числом, которого на экране нет, и ничего при этом не падало. ЛЕЧЕНИЕ ПРИНЦИПИАЛЬНОЕ: факт больше не считается вторым запросом — он БЕРЁТСЯ ИЗ САМОГО ОТЧЁТА, которому передан тот же метод учёта. Повторить кассовый метод вторым способом невозможно так, чтобы он не разошёлся: отчёт по оплате не просто отбирает оплаченные документы, он ещё и ПРИЗНАЁТ по ним выручку (проводки платежа ходят по балансовым счетам и дохода не приносят). Теперь разойтись нельзя по устройству. Итоги и расчётные строки отчёта в факт не попадают: номер у них словом, своих проводок нет. Мутация (снятие передачи метода) роняет 3 теста. Сервер +5 проверок, витрина +4. Прогон: сервер 2841/395, витрина 1261/157, словари 4653/4653, типы чисты. |
| 2026-09-19 (вечер) | Остаток О3 ЗАКРЫТ: в Движении денег появилось сравнение с прошлым периодом — это был единственный из трёх главных отчётов без него. ГЛАВНОЕ РЕШЕНИЕ: отчёт за прошлый период считается ТЕМ ЖЕ методом, просто с другими датами. Баланс и ОПиУ держат для прошлого периода отдельные «леджеры» и десяток классов-композеров, но у ДДС другое устройство (секции вместо узлов, своё поле периодов, собственный класс таблицы), и повторять там ту машинерию значило бы завести ВТОРОЙ способ считать те же числа. Второй способ однажды расходится с первым, и оба выглядят правильными. Цена — второй проход по проводкам, но только когда сравнение включено; у повторного вызова флаги гасятся, иначе он потребовал бы третий. ПРАВИЛО СДВИГА ДАТ ВЫНЕСЕНО В ОДНО МЕСТО `previousPeriodRange.ts`, и общая примесь `FinancialDateRanges` теперь зовёт его же: своя копия означала бы, что Баланс и ДДС могут сравнивать с РАЗНЫМИ отрезками, оба выглядя правильными. Длина периода считается ВКЛЮЧАЯ оба конца (март это 31 день), иначе прошлый период наезжал бы на текущий одним днём — мутация роняет 3 теста. ПРОЦЕНТ ПРИ НУЛЕ — `null`, а не ноль: рост с нуля до миллиона это не «плюс 100%», а ноль означал бы «ничего не изменилось» ровно там, где изменилось всё. Процент делится на МОДУЛЬ прошлого: было −100, стало −50 — это улучшение, а не ухудшение. ГРАБЛЯ: итоговая строка собирается в классе таблицы уже ПОСЛЕ развешивания значений и теряла колонки сравнения — пустые ячейки читались бы как «сравнивать нечего». Витрина строит колонки из серверных сама, поэтому там понадобились только три выключателя с тем же правилом связи, что в Балансе. Сервер +20 проверок, витрина +7. Прогон: сервер 2836/395, витрина 1257/157, словари 4653/4653, типы чисты. |
| 2026-09-19 (вечер) | Остаток О2 ЗАКРЫТ: над таблицей Баланса появилась картинка «во что вложены деньги / откуда деньги взялись». ДВЕ ПОЛОСЫ, А НЕ КРУГ: баланс тем и устроен, что обе стороны равны, а круговая диаграмма показывает одну сторону и это равенство прячет. БЕРЁТСЯ ВТОРОЙ УРОВЕНЬ, а не счета: у организации легко сорок счетов, картинка из сорока долей не сообщает ничего; второй уровень («Оборотные активы», «Капитал», «Обязательства») — те слова, которыми предприниматель и думает о деньгах. ГЛАВНОЕ РЕШЕНИЕ — ОТРИЦАТЕЛЬНЫЕ ГРУППЫ: группа бывает минусовой по-настоящему (накопленная амортизация больше стоимости основных средств). Ширины у минуса не существует, поэтому доля равна нулю, но сама группа НЕ ПРЯЧЕТСЯ — выводится подписью под полосой. Молча выбросить её было бы хуже всего: сумма полос разошлась бы с итогом таблицы, и человек перестал бы верить обеим. Доли считаются от суммы ПОЛОЖИТЕЛЬНЫХ групп: делить на итог половины нельзя — полоса вышла бы длиннее самой себя (600 000 из 570 000 это 105%); мутация роняет проверку. Цвета не светофорные: обязательства не «плохо», капитал не «хорошо». Новая ручка `GET financial-reports/chart/structure`. Сервер +8 проверок, витрина +10. Прогон: сервер 2816/394, витрина 1250/157, словари 4650/4650, типы чисты. |
| 2026-09-19 (вечер) | Остаток О1 ЗАКРЫТ: раскрытие суммы теперь есть в БАЛАНСЕ и ДВИЖЕНИИ ДЕНЕГ, не только в ОПиУ. ГЛАВНАЯ ТРУДНОСТЬ, из-за которой пункт откладывали: их строка — не ОБОРОТ за период, а ОСТАТОК на дату, и сумма операций периода с ней не сойдётся НИКОГДА (в остатке сидит всё, что накопилось раньше). Решение: панель показывает не одно число, а цепочку — остаток на начало + оборот за период = остаток на конец. Остаток на начало считается по операциям ДО первого дня; взять сам первый день нельзя — операции этого дня попали бы и в остаток, и в оборот (мутация роняет тест). ПОПУТНО ПОЧИНЕНО: итог панели считался по ПОКАЗАННЫМ строкам, а список обрезан до 200 — на длинном счёте панель показывала бы итог меньше отчётного, ровно то недоверие, ради устранения которого раскрытие и делалось; теперь итог берётся отдельным запросом, а обрезка называется вслух «показано N из M». ГРАБЛЯ: в ДДС номер строки-счёта идёт с приставкой `account-1025` — без её снятия раскрытие не заработало бы вовсе, а выглядело бы как «строка просто не кликается». Подставная база в спеке научена ОТБИРАТЬ и СКЛАДЫВАТЬ по-настоящему: заглушка, отдающая один список на любой запрос, повторяет за кодом вместо того, чтобы спорить с ним. Сервер +5 проверок, витрина +15. Прогон: сервер 2808/393, витрина 1240/157, словари 4647/4647, типы чисты. |
| 2026-09-19 (вечер) | НАЙДЕНА И ПОЧИНЕНА ДЫРА: раскрытие суммы в ОПиУ — пункт, который ТЗ зовёт КЛЮЧЕВЫМ («без него человек не верит отчёту») — НЕ РАБОТАЛО НИ РАЗУ. Витрина звала `financial-reports/drill-down`, сервер отдаёт `financial-reports/chart/drill-down`. Доказано вживую: старый путь отвечает «Cannot GET», новый — данными счёта. Ни один из 4000 прогонов этого не видел: витрина проверяет свою половину, сервер — свою, обе зелёные. НОВЫЙ СТОРОЖ `apiPathsExistOnServer.spec.ts` читает ОБЕ половины монорепо и сверяет адреса. Найдено 9 расхождений, 4 пути починено (раскрытие суммы, ожидающие банковские операции → `banking/pending`, регистрация → `auth/signup`, настройки → метод PUT), 4 записаны в долг с причиной, 1 оказалась ошибкой самого сторожа. ДВА СЛЕПЫХ ПЯТНА В СТОРОЖЕ, найденные проверкой: (1) комментарий между `apiRequest` и `.get(` прятал вызов — лечится чтением исходника без комментариев; (2) контроллеры собирались по ИМЕНИ ФАЙЛА `*.controller.ts`, и файл `AuthApiKeys.controllers.ts` (множественное число) выпал — живая ручка попала в долг как несуществующая; теперь ищем по содержимому `@Controller(`. Сторож, который врёт в свою пользу, хуже отсутствующего. Прогон: витрина 1225/157, типы чисты. |
| 2026-09-19 (вечер) | Остаток О-v90-2 ЗАКРЫТ: сторож `noHardcodedStrings` теперь видит и АНГЛИЙСКИЙ текст в разметке, не только русский. Причина: у Bigfin родная локаль русская, и опаснее не «русское слово в английской локали», а английская подпись из старой кодовой базы — её русский предприниматель видит как есть. Правило: ДВА И БОЛЕЕ английских слова подряд между тегами; одно слово не в счёт (`PRO`, `ID`, `Email` намеренно латиницей). Две оговорки с обоснованием: ключевые слова TypeScript (уголки дженериков выглядят как теги) и полоски-заглушки `<Skeleton>XXXX XXXX</Skeleton>` (букв там не видно вовсе). НАЙДЕНО И ПОЧИНЕНО 20 МЕСТ в 14 файлах: подписка (5 кнопок целиком по-английски), «Send Mail» в двух письмах, «Copy Link», «Set Up Stripe», диалоги излишка оплаты, диалог смены курса, «Preparing the document.», пагинация. Фраза с компонентом внутри (`You should unlock <Join/> modules first`) переписана на строку с подстановкой: разорванная фраза переводится только на тот язык, на котором её писали. +16 ключей парно. Мутация: возврат одной подписи роняет сторожа с указанием файла и строки. Прогон: витрина 1218/157, словари 4643/4643, типы чисты. |
| 2026-09-19 (вечер) | Остаток О5/К1 ЗАКРЫТ: разрез по юрлицу в трёх главных отчётах — Баланс, ОПиУ, Движение денег. Сервер принимает `legalEntityIds` и накладывает общий отбор `applyLegalEntityScope` в ОДНОМ месте на отчёт — в `commonFilterBranchesQuery`, который зовут все запросы отчёта. Это главное решение шага: Баланс собирается несколькими запросами (обороты, остатки на начало, периоды), ДДС — четырьмя; отбор, поставленный в один запрос, не падает, а РАССОГЛАСОВЫВАЕТ отчёт сам с собой. Шапка каждого отчёта теперь говорит, что показано (`describeLegalEntityScope`): сводный отчёт без внутренних переводов и отчёт по одному юрлицу дают РАЗНЫЕ числа, и оба правильные. ПУСТОЙ ВЫБОР = ВСЯ ГРУППА, а не «ничего не показывать»: иначе отчёт опустел бы у всех, кто про новое поле не знает. ПОЛЯ НЕТ ВОВСЕ, пока юрлицо одно (требование приёмки §8.5) — выбор из одного не выбор. Сторож `reportsLegalEntityScope.spec.ts` (8 проверок) следит за всеми тремя отчётами и за общим местом; снятие отбора в ДДС роняет 2 из 8. ГРАБЛЯ: подставной построитель запросов в спеке кассового метода не знал слова `where` — 5 тестов упали не от ошибки в коде, а от того, что заглушка говорит на более бедном языке, чем настоящий код. Прогон: сервер 2803/393, витрина 1214/157, словари 4627/4627. |
| **Режим** | `ТЗ` |
| **Текущий этап** | Блоки Б1–Б3 (этапы 1–15) и Б4 (этапы 16–28) закрыты по коду, ждут приёмки владельца. **Работа идёт по блоку Б5** ([MASTER-3.md](MASTER-3.md), этапы 29–41). Этапы 29–35 ВЛИТЫ (#546–#555). Следующий — **36 «Сверка и корзина»** |
| **Следующее действие** | Взять этап 36 по [MASTER-3.md](MASTER-3.md), раздел 25: СНАЧАЛА FT-042 (мягкое удаление операций D8/D9 + экран корзины: восстановленная операция возвращает отчёт к прежним значениям) и FT-043 (пакеты импорта D10/D11 и откат импорта), ПОТОМ FT-040/FT-041 (сверка D12/D13: два списка расхождений и массовое решение, по интеграции и по выписке без интеграции). Корзина делается ДО сверки: сверке нужно показывать удалённые вручную операции. DoD: после разрешения расхождений остаток равен банковскому; удалённая операция восстанавливается и отчёт возвращается к прежним значениям; откат импорта возвращает остаток |
| **Рабочая ветка** | `develop`. Следующая — `feat/tz3-36-сверка-корзина`. Этапы блока Б5 — ветки `feat/tz3-NN-краткое-имя`, один этап = один PR |
| **PR** | Блоки Б1–Б4 влиты целиком (#533–#543). #544 — третье ТЗ. #545 — стенд впервые накатывает миграции организаций. #546–#549 — этапы 29–32, #550 — починка ОПиУ по деньгам, #551 — этап 33, #552 — настройки зарплаты, #553 — этап 34, #554 — починка «применить к прошлым», #555 — этап 35 |
| **Блокеры** | Ю1 ЗАКРЫТ 19.09: миграции и заполнение прошли на живой базе стенда (20 организаций, 776 строк). Оказалось не «руки не дошли», а ТРИ поломки подряд — см. журнал. Ю2 ЗАКРЫТ РЕШЕНИЕМ: миграция NOT NULL НЕ пишется — на базе с пустой колонкой она либо падает, либо молча ставит нули; тот же запрет живёт в коде (счёт без юрлица не создаётся, операции наследуют, смена юрлица у счёта с операциями запрещена). Разбор — в плане этапа 8 |
| **Ждём от владельца** | подтверждение удалений из карты v88 и двух мёртвых мест из v89. Вопросы В1–В4 ЗАКРЫТЫ самостоятельно по поручению владельца 19.09 (решения — в журнале) |

---

## 1а. Остаток на будущее — ОДИН список

**Задела больше нет.** Всё, что стояло здесь 19.09, закрыто в тот же день.
Раздел оставлен, чтобы следующая сессия видела: пусто — это проверенный
факт, а не «никто не смотрел».

| Что было | Чем закрыт |
|---|---|
| ~~К4, К9 — решения владельца~~ | оба оказались не вопросами: курс назван в самом ТЗ, расчёты внутри группы считаются из перекоса отбора (#504, #505, #506) |
| ~~Д1 — экраны 4–8~~ | очередь §5.1 пройдена; остались ФОРМЫ, и это другой долг (#509) |
| ~~Д3 — мобильная версия~~ | карточки вместо таблиц на 14 списках (#508) |
| ~~Д4 — состояние «ошибка»~~ | сбой виден на всех экранах-списках (#507) |
| ~~277 форм на Formik~~ | замер был неверный: настоящих форм 67. Решение — НЕ переводить массово, граница закреплена сторожем (#513) |
| ~~3 адреса-долга~~ | за каждым стоял мёртвый код, зовущий несуществующую ручку (#510) |

**Что осталось и почему это не задел:**

| Что | Состояние |
|---|---|
| Слепая зона типов | витрина 71 файл, сервер 6. Это не «недоделка», а очередь слоёв: каждый слой закрывается отдельной работой и каждый раз находит настоящие дефекты |
| Цвет графика «Топ контрагентов» на главной | `Homepage/TopContractorsSection.tsx`, строки 101 и 107: `var(--c-action)` и `var(--c-text-secondary)` — голые числа без `rgb()`, браузер цвет не понимает. Та же ошибка в «Деньгах» исправлена этапом 30 (#547). Починка — две строки: `var(--color-action)`, `var(--color-text-secondary)` |
| Раскрытие ячейки управленческого ОПиУ по деньгам | отключено (PR #550): выручка счёта, оплаченного позже, «достраивается» по факту оплаты и в журнале её нет — панель не сошлась бы с ячейкой. Решение: научить `drill-down` показывать достроенные строки (платёж + доля счёта) |
| Автоправила: «разные поля применяются последовательно» | FT-035 говорит: правила, меняющие РАЗНЫЕ поля, применяются друг за другом. У Bigfin каждое правило разносит строку и ставит статью — значит, любые два подходящих правила спорят за одно поле, и срабатывает верхнее. Последовательное применение появится, когда правила начнут дописывать поля уже разнесённым операциям (метка, сделка поверх статьи) |
| Автоправила: этап сделки у операции | у денежной операции нет колонки этапа сделки — правило «сделка» ставит операции сделку, а выбранный этап виден только в следе применения. Привязка денег к этапу — отдельная задача модуля «Сделки» |
| Проверка конфликта правил | смотрит последние 2000 строк выписки и совпадение условий. Правило, которое спорит только на строках старше, предупреждения не вызовет — это подсказка, а не доказательство |
| Автоправила: метка | действие «метка» (FT-030) хранится у правила (`assign_tag`), но ставить её некуда: колонка метки у проводок — D4, этап 37 (FT-025). Поле метки на экране правила появится вместе с ней |
| Автоправила: условия «направление» и «сделка» | FT-030 перечисляет их среди условий, но у строки выписки ни направления, ни сделки нет — правило работает со строками выписки. Условия появятся, когда правила начнут применяться к уже разнесённым операциям |
| Ручная панель разбиения операции | пишет части под видом операции («OtherExpense»), а не под `CashflowTransaction` — её части проводок не меняют, как и до этапа 34. Части от правила «Разбить» проводятся. Починка — перевести панель на `CashflowTransaction` и пересобирать проводки при сохранении |
| Старый `_utils.ts` сопоставления правил | `BankingTranasctionsRegonize/_utils.ts` (`bankRulesMatchTransaction`) больше никем не вызывается — работает общий движок `matchRule.ts`. Файлы удаляются только с разрешения владельца — оставлен |
| ФОТ по сотрудникам не видит направлений и юрлиц | расчёт зарплаты не знает ни направления, ни юрлица сотрудника. Поэтому раскрытие работает только на вкладке «По статьям» и без отбора по юрлицу, а в остальных случаях экран говорит словами, почему не раскрыто. Сумма сотрудника — «на руки»: НДФЛ и взносы уходят на статью налогов. Расхождение с операциями по статье видно отдельной строкой-остатком, а не прячется |
| Распределение косвенных по неделям и дням | база считается по колонке отчёта. В колонке без выручки база нулевая, и косвенные расходы остаются «Без направления» — экран об этом предупреждает. На месяцах и крупнее это редкость, на днях — обычное дело |
| Три подписи «Денег» больше не используются | `cash_flow_articles.from_date`, `.to_date`, `.column.amount` (en+ru) — экран с этапа 30 берёт период из общей полосы, а колонки подписывает по периодам. Ключи переводов удаляются только с разрешения владельца — оставлены |
| 67 форм на старой библиотеке | осознанное решение, а не долг. Граница держится сторожем: список может только уменьшаться |

**Приёмка владельца.** Живой проход по продукту сделан — и нашёл пять
дефектов, которых не видели 4400 тестов. Но это проход ГЛАЗАМИ ПРОГРАММЫ:
владелец по продукту руками не ходил, и его проход найдёт другое.

---

## 2. Словарь статусов

| Статус | Что означает |
|---|---|
| `не начат` | Кода нет, ветки нет |
| `в работе` | Есть ветка, есть коммиты, PR ещё не открыт |
| `на ревью` | PR открыт, ждёт владельца или CI |
| `влит` | PR в `develop` |
| `принят` | Владелец проверил вживую и подтвердил приёмку из ТЗ |
| `заблокирован` | Нужно решение владельца или внешнее условие |

Этап считается **закрытым** только в статусе `принят`. `влит` ≠ `принят`.

---

## 3. Этапы

Порядок в таблице — номера ТЗ. Порядок исполнения — в разделе 4.

| № | Этап | Блок | Статус | Ветка / PR | Комментарий |
|---|---|---|---|---|---|
| 1 | Навигация | Б1 | на ревью | меню 14 → 8 пунктов, флаг `documents`, режим «Бизнес» прячет 13 адресов | шаг 1.1 сделан: 14 пунктов → 8, сверка адресов. Ждёт этапа 3 (пункт «Операции» вести некуда). Решения владельца: «Проекты» из меню НЕ убирать — оживлять; «Документы» выключить всем |
| 2 | Дашборд | Б1 | на ревью | #432, #433 влиты | все шаги сделаны; в заделе строка о превышении бюджета и замер скорости с кешем — таблица приёмки в плане |
| 3 | Экран операций | Б1 | на ревью | все шаги влиты; 3 пункта ТЗ зависят от чужих этапов — таблица приёмки в плане | ВЗЯТ ВПЕРЁД ЭТАПА 1 (решение владельца). 3.1–3.4 сделаны: сервер (счёт стал отбором и у проведённых, и у ждущих разноски), экран `/cashflow-accounts/transactions` с отборами в адресе, полоса «N ждут разноски» с режимом показа и подсказкой от правила. 3.5 разнос одним движением и 3.6 массовые действия влиты |
| 4 | Отчёты: каркас и три главных | Б1 | на ревью | #435–#440 влиты | все 7 шагов сделаны. Таблица приёмки — в плане этапа. ОСТАТКИ: ~~О1 раскрытие суммы в ДДС и Балансе~~ — ЗАКРЫТ 19.09 (цепочка «остаток на начало + оборот = остаток на конец»), ~~О2 график структуры активов в Балансе~~ — ЗАКРЫТ 19.09, ~~О3 сравнение с прошлым периодом в ДДС~~ — ЗАКРЫТ 19.09, ~~О4 кассовый метод в колонке «Отклонение»~~ — ЗАКРЫТ 19.09, ~~О5 фильтр юрлица~~ — ЗАКРЫТ 19.09, ~~О6 фильтр проекта~~ — ЗАКРЫТ 19.09 вместе со справочником направлений |
| 5 | Дизайн-токены и состояния | Б1 | на ревью | этот PR | сквозной. Сделано: правило четырёх состояний `pickScreenState` (загрузка важнее ошибки, ошибка важнее пустоты) и состояние «ошибка» с «Повторить» на новых экранах. Тест-запрет прямого `Intl.NumberFormat` УЖЕ БЫЛ. Остатки Д1–Д4: миграция экранов 4–8, документ токенов, мобильная версия, состояние ошибки на прежних экранах |
| 6 | Модель данных юрлица | Б2 | на ревью | #446–#451 влиты | закрыт по коду КРОМЕ ломающего шага 6.5. Сделано: миграции (таблица + колонка nullable в 13 таблицах), юрлицо по умолчанию, заполнение пакетами, ручки и экран справочника. Таблица приёмки — в плане этапа. ОСТАТКИ: Ю1 миграции и заполнение НЕ ЗАПУСКАЛИСЬ на живой базе, Ю2 шаг NOT NULL ждёт согласия владельца, Ю3 часть полей юрлица не выведена в форму, Ю4 нет колонки «активно», Ю5 правило «не навязывать» ждёт колонок из этапа 7 |
| 7 | Консолидированная отчётность | Б2 | на ревью | #452, #453 влиты | 7.1–7.6 сделаны: колонка `is_intercompany` у проводок + правила внутригрупповых оборотов. ГЛАВНОЕ ПРАВИЛО: НЕИЗВЕСТНОЕ юрлицо не считается «другим» — иначе внутригрупповым стал бы каждый перевод и отчёт молча потерял бы выручку. пометка в единственной точке записи проводок, общий отбор `applyLegalEntityScope` (строки без юрлица ОСТАЮТСЯ), отчёт «Внутригрупповые обороты». Приёмка — в плане этапа 8; остатки К2–К4 (К1 закрыт 19.09) |
| 8 | Юрлицо в операциях, формах, правах | Б2 | на ревью | #454 + этот PR | 8.1–8.5 сделаны: юрлицо у счёта (обязательно только когда юрлиц несколько), наследование в точке записи проводок, тип счёта «Личные средства», печатные формы по реквизитам юрлица (правило «всё или ничего»), правило видимости юрлиц. Приёмка блока Б2 — в плане этапа; остатки К2–К8 (К1 закрыт 19.09) |
| 9 | Анализ расходов | Б3 | на ревью | #442–#445 влиты | все 4 шага сделаны. Таблица приёмки — в плане этапа. ОСТАТКИ: Р1 на экране нельзя выбрать период (ручка его принимает), Р2 нет разрезов по направлению и проекту на экране (сервер умеет), Р3 разрез по юрлицу ждёт этапа 6, Р4 РАСХОДЫ ПО СЧЕТАМ ВНЕ СТАТЕЙ НЕ ВИДНЫ ВООБЩЕ — свёртка их молча пропускает, итог меньше, чем в ОПиУ, и ничто на это не указывает |
| 10 | Разделение операции | Б3 | на ревью | этот PR | таблица `transaction_splits` (родитель не трогается: в отчёты части, в сверку с банком родитель) + правила сходимости (13 проверок). Мутация вскрыла МЁРТВУЮ страховку — допуск поверх округления ничем не проверялся, убран. Остатки Р1–Р3: панель на витрине, подключение частей к отчётам, деление по процентам в правилах разноски |
| 11 | Капитализация | Б3 | на ревью | этот PR | расчёты готовы: чистые активы, оценка по мультипликатору (убыточный бизнес так НЕ оценивается — мутация роняет 2 теста), доля владельца, разложение (направление по СМЫСЛУ статьи, а не по знаку). Остатки С1–С2: экран `/capitalization` и настройка множителя |
| 12 | ИИ-разнос по истории | Б3 | на ревью | этот PR | БЕЗ внешнего ИИ, как и решает ТЗ. Совпадение по контрагенту (объяснимо: «12 из 14 платежей»), классификатор по словам со сглаживанием, порог 70% — мутацией подтверждён. Почти все проверки про МОЛЧАНИЕ: неверная подсказка хуже её отсутствия, её принимают одним щелчком. Остатки И1–И4: переобучение, подсказка в строке, кнопка массового применения, счётчик в настройках |
| 13 | ИИ-аналитик | Б3 | закрыт по коду | PR этапа 13 | Разрешительный список полей (тест §13.4), сверка каждого числа с отчётом, флаг выключен по умолчанию, запрет внешних сервисов закрывает раздел с объяснением, 4 провайдера за интерфейсом, суточный кеш. Остатки И1–И4 — витрина блока и настроек, агрегаты только по ОПиУ, живой вызов провайдера |
| 14 | ИИ-чат | Б3 | закрыт по коду | PR этапа 14 | Модель без доступа к базе: закрытый перечень инструментов, каждый зовёт существующий отчёт. Неизвестный инструмент отвергается, а не угадывается; каждое число ответа сверяется с данными; без ссылки ответ не показывается; не больше 4 кругов. Остатки Ч1–Ч4 — экран, ДДС/Баланс/разрывы, память разговора, живой вызов |
| П | Оживление раздела «Проекты» | вне ТЗ | не начат | — | Решение владельца 18.09: раздел оживлять (сервера нет вовсе — модель, ручки, права, рентабельность). Объём отдельной работой, в мастер-ТЗ не описан |
| 15 | Публичный API и вебхуки | Б3 | закрыт по коду | PR этапа 15 | Токены (только отпечаток в базе), вебхуки с подписью и повторами, Swagger UI на `/api/docs` за флагом, `docs/public-api.md`. Остатки П1–П4 — экран настроек, рассылка событий, страж входа, публичная страница |

### Блок Б4 — «Вход в продукт» (ТЗ [MASTER-2.md](MASTER-2.md), раздел 12)

Зависимости взяты из ТЗ и нарушению не подлежат. Порядок исполнения — в разделе 4.

| № | Этап | Блок | Статус | Ветка / PR | Зависит от | Комментарий |
|---|---|---|---|---|---|---|
| 16 | Техническая подготовка | Б4 | влит | #522 | — | ЗАКРЫТ ПО КОДУ: переезд расчёта ДДС в статьи (`ArticlesPlRollup` там уже был), список интервалов разрыва с глубиной ямы, примитив `sparkline` + сторож зашитых цветов, SDK перегенерирован локально (273 → 447 путей; две несуществующие ручки убраны). План и приёмка — `docs/superpowers/plans/2026-09-20-tz2-16-подготовка.md` |
| 17 | Статьи пяти видов | Б4 | влит | #523 | 16 | ЗАКРЫТ ПО КОДУ: пять видов, раздел ДДС обязателен у балансовых, свёртка ОПиУ отбирает доходы и расходы, догоняющий сид на 11 статей БЕЗ привязки счетов (поэтому суммы не изменились), строгая проверка `?kind=`, пять вкладок со счётчиками и адресом, замок на системных, вид заблокирован при правке. План: `docs/superpowers/plans/2026-09-20-tz2-17-статьи.md` |
| 18 | Отчёт «Деньги (ДДС по статьям)» | Б4 | влит | #524 | 16, 17 | ЗАКРЫТ ПО КОДУ: новый подмодуль `FinancialStatements/modules/CashFlowArticles` на СУЩЕСТВУЮЩЕМ расчёте (график и таблица наконец согласованы), пять форматов, три раздела → поступления/выплаты → дерево статей, блок переводов, строка «не разнесено». Чистый поток считается по ДЕНЕЖНОЙ стороне, поэтому равенство «начало + поток = конец» держится конструкцией. Меню: «Деньги (ДДС)» ведёт на прямой отчёт, косвенный переехал в `accountantOnly` |
| 19 | Раскрытие по статье и переход в реестр | Б4 | влит | #525 | 17, 18 | ЗАКРЫТ ПО КОДУ: раскрытие получило ВТОРОЕ измерение — по статье, не сломав первое (по счёту ответ побайтно прежний, новые поля просто отсутствуют). Статья берётся с подстатьями и только по документам, рассчитанным деньгами, — тем же правилом, что в отчёте, иначе список не сойдётся с числом. Реестр операций умеет отбор `articleId` в адресе, панель даёт кнопку «Открыть в Операциях», реестр рисует строку контекста с крестиком |
| 20 | Схема «Куда попадает статья» | Б4 | влит | #526 | 17, 18, 19 | ЗАКРЫТ ПО КОДУ: три карточки (Деньги, Прибыль, Баланс), подсветка строк ВЫБРАННОЙ статьи и её оборот рядом; карта ВЫВОДИТСЯ из вида статьи, раздела ДДС и наличия счетов (решение D2 — таблицы не заводим); оборот считает тот же код, что отчёт; статья без счетов честно говорит «ни на что не влияет»; отдельная вкладка, чтобы схема не ломала телефон |
| 21 | Виджет денег и разрывы по счетам | Б4 | влит | #527 | 16 | ЗАКРЫТ ПО КОДУ: виджет в шапке (остаток, ближайший разрыв, линия за месяц) на ВСЕХ экранах; панель со счетами, у каждого своя дата разрыва и глубина; одна ручка `dashboard/money-widget` с кэшем 60 с (в памяти процесса — обоснование в коде); выключенный календарь показывает остаток и объясняет, почему прогноза нет |
| 22 | Группы счетов | Б4 | влит | #528 | 21 | ЗАКРЫТ ПО КОДУ: таблица `account_groups` + nullable ссылка у счёта (`SET NULL`), CRUD и перенос счёта, панель денег группирует счета. ОТСТУПЛЕНИЕ ОТ ТЗ: управление группами не в списке счетов, а отдельным экраном `/preferences/account-groups` — список счетов помечен `@ts-nocheck`, а ЧАСТЬ A2 ТЗ запрещает трогать такие файлы |
| 23 | Реестр: состояние, итоги, группировка | Б4 | влит | #529 | 19 | ЗАКРЫТ ПО КОДУ: состояния операции ВЫВОДЯТСЯ из остатка документа и срока (второй даты нет — она разрушила бы двойную запись), подписи СЛОВАМИ, а не буквами «Д»/«К»; сводная строка итогов под ТЕМ ЖЕ отбором, что и список (отборы вынесены в общий помощник — иначе «87 операций» над списком из 84); отбор по состоянию в адресе; слияние планов с фактом без дублей |
| 24 | Единая шапка отчёта, разрезы, ярусы, тренд | Б4 | влит | #530 | 18, 19 | ЗАКРЫТ ПО КОДУ ЧАСТИЧНО: полоса отчёта доросла до единого каркаса (масштаб, способ построения, метод учёта) и пишет всё в АДРЕС; ярусы прибыли посчитаны и покрыты спекой, домен параметра строгий. ОСТАТОК: подключение полосы к каждому из 19 отчётов и колонка «Тренд» в общем каркасе таблицы — это правка чужих экранов, её надо делать с живым прогоном глазами |
| 25 | Платёжный календарь | Б4 | влит | #531 | 19, 21 | ЗАКРЫТ ПО КОДУ: пять масштабов (день/неделя/месяц/квартал/год), два источника, кнопка «На сегодня». ГЛАВНОЕ ПРАВИЛО: потоки складываются, ОСТАТОК — НЕТ (это состояние на конец, а не сумма); внутри крупного столбца прошедшая часть считается фактом, остальная планом. Неделя начинается с ПОНЕДЕЛЬНИКА. На узком экране по умолчанию недели: дневная таблица не помещается |
| 26 | Бюджеты и личные настройки | Б4 | влит | `feat/tz2-26-бюджеты` | 23, 24 | ЗАКРЫТ ПО КОДУ: «прошло времени» и вывод СЛОВАМИ («расходы идут быстрее плана»), доля при нулевом плане «н/о»; таблица `user_display_preferences` (ключ-значение, чтобы не мигрировать на каждую галочку), ручки и экран `/preferences/display`. Права организации на личную настройку НЕ спрашиваются — обоснование в стороже ручек записи |
| 27 | Главная, контрагенты, сделки, справка | Б4 | влит | `feat/tz2-27-главная` | 17, 24 | ЗАКРЫТ ПО КОДУ: Парето по контрагентам с выводом СЛОВАМИ и прибыльность направлений — оба блока в ТОМ ЖЕ ответе `overview`; долг разделён на денежный и неденежный ПО САЛЬДО (зачёт аванса до классификации), авансы названы в сводке денег; отбор списков по природе долга подзапросом на сервере; «оплачено/отгружено» в сделках с пометкой при расхождении > 20 п.п.; контекстная справка у заголовка ВСЕХ 50 корневых экранов, ключ выводится из адреса |
| 28 | Отраслевые демо и приёмка | Б4 | влит | `feat/tz2-28-демо` | все | ЗАКРЫТ ПО КОДУ: три отраслевых набора (услуги, торговля, строительство) с параметром `industry` и миграцией системной схемы; в каждом наборе есть будущий кассовый разрыв — это проверяется чистым расчётом, а не на глаз; прогон Playwright расширен с одного файла до пяти, `items.spec.ts` наполнен, новый сторож набора сценариев. Вопрос об отрасли в мастере настройки и `onboarding.spec.ts` НЕ сделаны осознанно — причины в «Указателе». ОСТАЛОСЬ ВЛАДЕЛЬЦУ: живой проход по чек-листу раздела 15 |

### Блок Б5 — «Управленческая глубина» (ТЗ [MASTER-3.md](MASTER-3.md), раздел 25)

Зависимости взяты из ТЗ и нарушению не подлежат. Порядок исполнения — в разделе 4.

| № | Этап | Блок | Статус | Ветка / PR | Зависит от | Комментарий |
|---|---|---|---|---|---|---|
| 29 | Фундамент: юрлицо в свёртках + управленческий тип статьи | Б5 | влит | `feat/tz3-29-фундамент`, #546 | — | ЗАКРЫТ ПО КОДУ. FT-008: `applyManagementReportScope` в трёх управленческих сводах, остатки «Денег по статьям» тоже через него; сторож считает запросы и отборы, проверен мутацией. FT-009: `pl_type` (миграция D1 + сид D2, проверены на живой базе: накат → откат → накат), `utils/plTypes.ts` (домен, совместимость с видом, наследование), 422 с кодами, действующий ярус в списке и в карте статьи, поле/бейдж/строка схемы на экране. Сид узнаёт системные статьи по ключу ИЛИ по подписи исходного сида — у старых организаций ключей у доходов и расходов нет. Ярусы по умолчанию не сдвигают ОП и ЧП. ОСТАЛОСЬ ВЛАДЕЛЬЦУ: фильтр юрлиц на экране «Деньги по статьям» придёт с этапом 30 (экран переписывается целиком); SDK не перегенерирован — новых ручек нет, только поля, а витрина SDK не использует (§7.3 п. 6 ТЗ-3) |
| 30 | ОДДС-матрица | Б5 | влит | `feat/tz3-30-оддс-матрица`, #547 | 29 | ЗАКРЫТ ПО КОДУ. FT-001: `dateGroup` (day/week/month/quarter/year/total) и `showTotalColumn`; периоды по календарю (`periodizeRows.ts`), > 400 колонок — 400 `PERIOD_TOO_WIDE_FOR_GRANULARITY`; кассовая свёртка по периодам одним проходом (`getRollupByPeriods`); остатки сцепляются и сверяются с базой; «Итого» = Σ колонок; общий построитель `common/ManagerialReportColumns.ts`; экран на `ReportSheet + ReportTable`, период/масштаб/юрлицо в адресе, график по периодам, четыре состояния. FT-005b: `stickyHeader`, `stickyFirstColumn` у `ReportTable`. Неделя пока с понедельника — настройка начала недели это FT-006b, этап 31. Кэша нет — это FT-093, этап 40 |
| 31 | ОДДС: группировки, проценты, раскрытие | Б5 | влит | `feat/tz3-31-оддс-группировки`, #548 | 30 | ЗАКРЫТ ПО КОДУ. FT-002 `group` (6 режимов, `groupings/*`, инвариант `groupingsAreConsistent.spec.ts`), FT-003 доли (`formatShare`, «н/о»), FT-004 раскрытие ячейки (`onCellClick` у `ReportTable`, отбор отчёта и границы всего отчёта в `drill-down`, карточка операции из панели), FT-005 `showEmpty`, FT-006 `showTransfers`, FT-006b `organization.week_start_day / highlight_weekends / show_weekdays` (без миграции, хранилище настроек). Тумблеры — `user_display_preferences`: `showPercent`, `showEmptyRows`, `showTransfers`. Раскрываются только строки-статьи; «без направления» не раскрывается — на сервере нечем отобрать «без» |
| 32 | Управленческий ОПиУ: ярусы | Б5 | влит | `feat/tz3-32-управленческий-опиу`, #549 | 29 | ЗАКРЫТ ПО КОДУ. FT-010: `ManagerialProfitLoss/*` (`computeManagerialTiers`, `buildManagerialPnlReport`, `ManagerialPnlSource.service` — начисление/деньги, `ManagerialPnlTable`, ручка с пятью форматами), право `read-managerial-profit-loss`, раскрытие `drill-down?plType=&basis=`; экран `ManagerialPnl/*` + `ProfitLossPage` (переключатель вида). FT-016: `reportFormulas.ts`, ключи `reports.formula.*`, сторож `formulaTooltipsExist.spec.ts`. Проверки: пример ТЗ (МД 314 039,07 и 79,03 %), 1 000 операций (ВП2 и ЧП сходятся с группами), золотой ответ, нулевая выручка — «н/о», мутация ловится. FT-011 (распределение косвенных), FT-015 (водопад) — этап 33 |
| 33 | ОПиУ: методология | Б5 | влит | `feat/tz3-33-опиу-методология`, #551 | 32 | ЗАКРЫТ ПО КОДУ. FT-015 водопад (`pnlWaterfall.ts`, `PnlWaterfallChart.tsx`). FT-013 месяц начисления: колонка `accrual_period` в `cashflow_transactions` и `accounts_transactions` (миграция `20260924100000`), поле в форме операции, массовая простановка `PATCH banking/transactions/accrual-period`, отбор `applyAccrualDateRange` в ОПиУ, проверка «Качества данных» `accrual-shifts`. FT-012 источники: `pnlSources.ts`, настройки `pnl_sources`, панель `PnlSourcesPanel` (зарплата и налоги в журнал не пишут — тумблер честно недоступен). FT-011 распределение косвенных: пять баз `allocationBases.ts` (выручка, ФОТ, ВП1, поровну, вручную), цель правила «сделки/направления» (миграция `20260924100100`, `target_type`/`target_ids`, старое поле оставлено), `spreadIndirect.ts` + галочка в ОПиУ, пометка о нулевой базе. FT-014 ФОТ по сотрудникам: настройка `managerial_pnl.payroll_grouping`, `payrollByEmployees.ts` + `PayrollByEmployees.service.ts` — сотрудники из утверждённых расчётов плюс строка-остаток, только при модуле «Зарплата» |
| 34 | Автоправила: типы и применение | Б5 | влит | `feat/tz3-34-автоправила-типы`, #553 | — | ЗАКРЫТ ПО КОДУ. ГЛАВНОЕ: правило больше не только ПОДСКАЗЫВАЕТ — новые строки любой выписки (1С, таблица, банк по API, Дзен-мани) оно РАЗНОСИТ само тем же путём, что человек (`ApplyBankRuleService` → `CategorizeBankTransaction`); старые — только через предпросмотр с галочками. Миграции `20260925100000` (D5 + `assign_contact_id`), `20260925100100` (D6), `20260925100200` (`project_id` у денежной операции — СВЕРХ списка ТЗ: без него действие «направление» не работает). Движок `BankRules/utils/matchRule.ts` (операторы «не равно», «начинается с», «любой из списка», до 50 условий, «оба направления», правила «для любого счёта»). FT-031: `splitShares.ts` (копейки, остаток первой строке), части в `transaction_splits` под видом `CashflowTransaction`, проводки по частям (`BankTransactionGL` со splits). FT-032: 422 `TRANSFER_RULE_CURRENCY_MISMATCH`, удаление счёта-получателя удаляет правило. FT-034: `GET banking/rules/:id/preview`, `POST …/apply` → задача в очереди распознавания → уведомление `bank_rule_applied`. Экран: тип правила, строки разбиения, окно «Применить к прошлым операциям?» |
| 35 | Автоправила: сделки, порядок, след | Б5 | влит | `feat/tz3-35-автоправила-след`, #555 | 34 | ЗАКРЫТ ПО КОДУ. FT-036: миграция `20260926100000` (D7 `transaction_rule_applications`, мягкие ссылки), `ApplyBankRuleService.recordApplication` пишет след с изменёнными полями; реестр отдаёт `autoRule` одним запросом на страницу (бейдж «А», нажатие открывает правило); карточка операции — раздел «Автоправила» (`GET banking/rules/applications/transaction/:id`, словами). FT-033: тип `deal` — статья + сделка или этап (сделка берётся у этапа; этап — только в следе, у операции колонки этапа нет); отдельно поступления/списания; условия — описание и контрагент. FT-035: `PUT banking/rules/order` (перетаскивание мышью + стрелки для телефона), `POST :id/pause`, `POST :id/clone` (« (копия)», на паузе, в конце), `POST conflicts` — пересечение по последним 2000 строкам выписки и «те же условия», окно «Сохранить всё равно» ДО сохранения |
| 36 | Сверка и корзина | Б5 | не начат | — | — | FT-040 (автосверка по интеграции: два списка расхождений и массовое решение), FT-041 (сверка по выписке без интеграции), FT-042 (soft-delete операций + экран корзины), FT-043 (пакеты импорта и откат). Корзина делается ДО сверки: сверке нужно показывать удалённые вручную операции |
| 37 | Реестр: доводка | Б5 | не начат | — | 34, 35 | FT-020 (быстрый фильтр «Без статьи (N)»), FT-021 (сохранённые фильтры), FT-022 (контекстное меню из 9 действий), FT-023 (разбиение суммы в форме), FT-024 (пакетный ввод), FT-025 (метки), FT-026 (история изменений операции) |
| 38 | Планирование | Б5 | не начат | — | 30 | FT-050 (календарь как матрица план/факт — с НАКОПИТЕЛЬНЫМ плановым остатком, чего у конкурента нет), FT-051 (сценарий «что можно перенести»), FT-052 (автоподтверждение плана фактом с подсказкой «что заполнить»), FT-053 (заявки: черновик и частичные оплаты), FT-054 (автозаполнение бюджета), FT-055, FT-056 |
| 39 | Права, публичный API, MCP | Б5 | не начат | — | — | FT-080 (row-level по статьям/направлениям/счетам), FT-081 («посмотреть, что видит пользователь»), FT-082 (право на экспорт), FT-084 (закрыть правами читающие ручки новых модулей), FT-091 (включить проверку токена `bgf_`), FT-092 (отправка вебхуков), FT-090 (MCP-сервер, только чтение). MCP — строго после FT-091 |
| 40 | Кэш, главная, справочники | Б5 | не начат | — | 30, 32 | FT-093 (кэш отчётов с картой инвалидации + «Пересобрать» с прогрессом), FT-060…FT-065 (пропорциональный план, сравнение периодов, кумулятивная динамика, направления, глобальный период, персонализация, цели), FT-070 (налоговый режим счёта), FT-071 (фиксация остатка на дату), FT-095 (онбординг с прогрессом в шапке) |
| 41 | AI CFO и приёмка | Б5 | не начат | — | все | FT-100 (аналитическая записка с разделами), FT-101 (контекст бизнеса выводится из данных), FT-102 (AI CFO: интенты → детерминированный расчёт → evidence → объяснение → ссылки на операции → действие с подтверждением). Идёт последним: модель не должна объяснять числа, которые ещё меняются. Затем живой проход по чек-листу раздела 31 ТЗ-3 |

---

## 4. Очередь исполнения

Из ЧАСТИ F ТЗ. Идти строго по ней, если владелец не сказал иначе:

```
1 → 2 → 3 → 4 → 9 → 6 → 7 → 8 → 5 → 10 → 12 → 11 → 15 → 13 → 14
    ^                 ^
    |                 |
    |                 этап 9 вставлен рано: дешёвый, заметный,
    |                 ни от чего не зависит
    этап 5 идёт поздно: экраны, переработанные в Б2,
    всё равно придётся причёсывать
```

**Очередь блоков Б1–Б3 пуста:** все 15 этапов закрыты по коду (5 — частично), остаток — приёмка владельца.

### Очередь блока Б4 (ТЗ [MASTER-2.md](MASTER-2.md), раздел «Рекомендуемый порядок реализации»)

Порядок подчинён зависимостям: **данные → расчёт → ручка → экран**. Экран раньше ручки
не делается — в проекте стоит сторож на это.

```
16  фундамент, ни от чего не зависит
 ├─ 17 → 18 → 19 → 20   статьи → отчёт «Деньги» → раскрытие → схема статьи
 │             ├→ 23    реестр: состояние, итоги, планы
 │             └→ 24    единая шапка отчёта, разрезы, ярусы, тренд
 └─ 21 → 22             виджет денег → группы счетов

25  платёжный календарь   — после 19 и 21
26  бюджеты и настройки   — после 23 и 24
27  главная и справка     — после 17 и 24
28  демо и приёмка        — последним, после всех
```

Чего нельзя делать в другом порядке (раздел ТЗ-2 с тем же названием): отчёт раньше
статей соберётся неправильно; раскрытие раньше отчёта непроверяемо; группы счетов
раньше виджета показывать негде; колонки и ярусы раньше личных настроек придётся
делать дважды.

Этапы 21 и 22 зависят только от 16 — при двух исполнителях идут параллельно ветке 17 → 20.

**Текущая позиция в очереди Б4:** этапы 16–28 ВЛИТЫ. **ОЧЕРЕДЬ БЛОКА Б4 ПУСТА, И СПИСОК ЗАДАЧ РАЗДЕЛА 13 (T-01…T-58) СВЕРЕН ПОИМЁННО** — закрыто всё, кроме T-54 (вопрос об отрасли в мастере настройки), пропущенной осознанно: вести ему некуда.

### Очередь блока Б5 (ТЗ [MASTER-3.md](MASTER-3.md), разделы 25 и 33)

Схема ниже переписана 23.09 по самому ТЗ-3. Прежняя версия (21.09) описывала другие
этапы 30–41 и относила к этапу 29 починку токена `bgf_` — по ТЗ-3 это FT-091, этап 39.
**Источник правды о составе этапа — раздел 25 `MASTER-3.md`, не эта схема.**

Закон тот же, что в Б4: **модель → миграция → служба → ручка → экран → проверки**.
Экран раньше ручки не делается.

```
29  фундамент: юрлицо в свёртках (FT-008) + pl_type у статьи (FT-009)
 │   ни от чего не зависит, от него зависит почти всё
 │
 ├─ 30 → 31             ОДДС: матрица периодов + «Итого» → группировки, проценты, раскрытие
 ├─ 32 → 33             ОПиУ: ярусы МД → ВП1 → ВП2 → ОП → ЧП → распределение, источники,
 │                      месяц начисления
 ├─ 34 → 35             автоправила: четыре типа и применение к прошлому → сделки,
 │                      порядок, след «А»
 ├─ 36                  сверка с банком и корзина (корзина ДО сверки)
 └─ 39                  права построчно, публичный API (токен bgf_, вебхуки), MCP

37  реестр: доводка                    — после 34 и 35
38  планирование                       — после 30
40  кэш, главная, справочники          — после 30 и 32
41  AI CFO и приёмка                   — последним, после всех
```

Что нельзя делать в другом порядке (раздел 33 ТЗ-3):
1. колонки-периоды ОДДС (30) — только после починки отбора по юрлицу (29);
2. ярусы ОПиУ (32) — только после появления `pl_type` (29);
3. кэш отчётов (40) — только когда отчёты стабилизировались;
4. корзина — до сверки, а не после (оба в 36, корзина первой);
5. AI CFO (41) — только после принятых ОДДС-матрицы и управленческого ОПиУ;
6. MCP (39) — только после починки проверки токена `bgf_` (тоже 39, раньше MCP).

Этапы 32→33, 34→35, 36 и 39 зависят только от 29 — при нескольких исполнителях
идут параллельно ветке 30→31.

**Текущая позиция в очереди Б5:** этапы 29 (#546), 30 (#547), 31 (#548), 32 (#549, починка #550), 33 (#551, починка #552), 34 (#553, починка #554) и 35 (#555) влиты. Следующий — этап 36 «Сверка и корзина» (FT-040…FT-043; БД D8–D13; корзина ДО сверки).

**ЧТО ДЕЛАТЬ СЛЕДУЮЩЕЙ СЕССИИ.** Открыть [MASTER-3.md](MASTER-3.md), раздел 25, взять
первый этап этой очереди, у которого в таблице раздела 3 статус не «влит». Режим
стабилизации НЕ включается, пока очередь Б5 не пуста.

**Про стабилизацию (устарело с приходом ТЗ-3).** Запись ниже писалась, когда очередей было две и обе опустели. Теперь очередей три, и очередь Б5 полна — режим стабилизации по разделу 5 не включается, пока она не опустеет. Наблюдение из неё остаётся верным и переносится в работу по Б5: за 20.09 живой проход и сверка «кто это ЧИТАЕТ» нашли ШЕСТЬ дефектов класса «написано, но ни с чем не соединено» и ДВЕ настоящие ошибки расчёта, которых не видели ни типы, ни 3343 серверные проверки. Аудит Финтабло 21.09 тем же способом нашёл ещё два. Значит: каждый этап Б5 заканчивается не зелёным прогоном, а вопросом «кто это читает и видно ли это человеку».

**ЧТО ОСТАЛОСЬ ВЛАДЕЛЬЦУ (не блокирует стабилизацию):** (1) чек-лист раздела 15; (2) включить `ONE_CLICK_DEMO_ENABLE` на стенде, чтобы проверить три отраслевых демо-набора; (3) экспорт в XLSX не знает личную настройку «копейки» — её форматирует сервер; (4) сквозные сценарии Playwright написаны, но не запускались: локального бэкенда нет, Actions выключены, стенд закрыт паролем nginx.

---

## 5. Режим «Стабилизация»

Включается, когда по ТЗ работы нет: пусты **обе** очереди раздела 4 — и блоков
Б1–Б3, и блока Б4, — либо обе упёрлись в ожидание владельца (`заблокирован` /
«ждём от владельца»), либо владелец явно попросил стабилизацию.

С 20.09.2026 очередь блока Б4 не пуста, поэтому сам по себе режим стабилизации
**не включается**: работа по ТЗ есть.

Источники работы — **строго в этом порядке**, брать первый непустой:

| # | Источник | Как проверить |
|---|---|---|
| 1 | Красный CI на открытых PR | `gh pr list --state open` → `gh pr checks <N>` |
| 2 | Падающие тесты на `develop` | `pnpm --filter @bigfin/server test`, `pnpm --filter @bigfin/webapp test` |
| 3 | Ошибки типов | `pnpm typecheck` |
| 4 | Непарные переводы | `node packages/webapp/scripts/lang-check.js` |
| 5 | Долг по `@ts-nocheck` | линия карт v84–v87, ~148 файлов. **Внутри работ по ТЗ эти файлы не трогать** — но в режиме стабилизации это законная работа |
| 6 | Непокрытые сторожевые спеки | `navigationReachability`, `routePathsAreReal`, `sidebarHonoursMenuGuards`, `emptyStateTexts`, `moduleDisabledSpeaks`, `tableScrollOnPhone` — расширять, не отключать |
| 7 | Дыры в e2e | `e2e/`, Playwright: основной суточный сценарий из E3 ТЗ |
| 8 | Известные грабли | раздел «Known gotchas» в `CLAUDE.md` |

Результат стабилизации оформляется так же: одна тема = один PR, зелёный прогон,
запись в «Журнал» ниже.

---

## 6. Журнал

Только факты: дата, что закрыто, ссылка. Новые записи — сверху.

| Дата | Что |
|---|---|
| 2026-09-23 | **ЭТАП 35 ЗАКРЫТ ПО КОДУ, PR #555.** Автоправила: сделка (FT-033), порядок/пауза/копия/конфликт (FT-035), след (FT-036). DoD: бейдж «А» в реестре ведёт к правилу (нажатие открывает окно правила); конфликт предупреждается ДО сохранения — окно «Сохранить всё равно / Вернуться» со списком правил, числом общих операций и тем, какое сработает; пауза отключает правило без удаления (движок пропускает правила с `paused_at`). РЕШЕНИЯ: порядок — перетаскиванием мышью И стрелками (перетаскивание браузера на телефоне не работает, а основатель работает с телефона); копия создаётся на паузе — две одинаковые активные копии тут же заспорили бы; след — мягкие ссылки: удалили правило, история остаётся с именем из следа; реестр читает след напрямую через подключение к базе — модуль правил в модуль реестра не подключается, иначе кольцо модулей. СТОРОЖ ПОЙМАЛ: ручка проверки конфликта шла без права — теперь то же право, что на сохранение. Прогон: сервер 3597 проверок, витрина 206 файлов / 1670 проверок, typecheck по трём пакетам и lang-check — зелёные; миграция D7 прошла круг «накат → откат → накат» на живой базе стенда. |
| 2026-09-23 | **«ПРИМЕНИТЬ К ПРОШЛЫМ» ПАДАЛО НА СТРОКЕ, КОТОРУЮ УЗНАЛО ДРУГОЕ ПРАВИЛО. ПОЧИНЕНО, PR #554.** Найдено живой проверкой этапа 34 на стенде, а не тестами. Вживую сошлись разбиение 1 500 → 1 050 + 450 с направлениями и проводками по частям, отказ долей 70 + 20 и перевода «в тот же счёт», предпросмотр ровно двух строк, снятая галочка, уведомление и — главное — САМОСТОЯТЕЛЬНАЯ разноска новой строки из импорта выписки 1С. А перевод строки, которую заранее узнало правило «разбить», не случился. Причина: служба удаляла старую отметку «распознано» РАНЬШЕ, чем строка переставала на неё ссылаться, — внешний ключ отвергал удаление, и падала вся задача (строки после первой ошибки не разносились, уведомление не приходило). Подделка базы в тестах внешних ключей не знала. Починка: сначала отвязать, потом удалить; сбой одной строки больше не роняет пакет — она уходит в «пропущено» с причиной. Подделка теперь ведёт себя как внешний ключ; мутация (обратный порядок) роняет тест. Попутно: при удалении денежной операции убираются её части в `transaction_splits` — раньше оставались сиротами. Порядок правил вживую: новую строку разнесло правило «разбить», а не «озон» — оба подходят, и побеждает созданное раньше; перетаскивание порядка — этап 35. |
| 2026-09-23 | **ЭТАП 34 ЗАКРЫТ ПО КОДУ, PR #553.** Автоправила: четыре типа (FT-030 «заполнить поля», FT-031 «разбить», FT-032 «перевод»; «сделка» — этап 35) и применение к прошлым (FT-034). ГЛАВНАЯ ПЕРЕМЕНА: правило раньше только ПОДСКАЗЫВАЛО статью — строку всё равно разносил человек; теперь новые строки любой выписки правило разносит само, после фиксации импорта, одной склеенной задачей на счёт. Старые строки — только явно, через предпросмотр с галочками: молча разносить то, что лежит, правило не вправе. DoD: 100 000 × 70/30 = 70 000 + 30 000, сумма частей равна родителю на 1 000 случайных наборов (копейки, остаток первой строке); перевод между рублями и долларами — 422 `TRANSFER_RULE_CURRENCY_MISMATCH`; применение идёт фоновой задачей с уведомлением. ПОЧИНЕНЫ СТАРЫЕ ДЫРЫ: правила «для любого счёта» и «оба направления» не срабатывали НИКОГДА; «не содержит» отклонялось сервером (экран слал `not_contains`, сервер ждал `not_contain`) и сравнивало с учётом регистра; импорт выписки распознавание не запускал вовсе; ошибка задачи распознавания тонула в `console.log`. СТОРОЖА ПОЙМАЛИ ДО СЛИЯНИЯ: список направлений запрашивался без флага модуля «Сделки» (403 при выключенном модуле). ОТСТУПЛЕНИЕ ОТ СПИСКА БД: `cashflow_transactions.project_id` и `bank_rules.assign_contact_id` — без них действия «направление» и «контрагент» не работают. Прогон: сервер 3583 проверки, витрина 205 файлов / 1661 проверка, typecheck по трём пакетам и lang-check — зелёные; миграции прошли круг «накат → откат → накат» на живой базе стенда. |
| 2026-09-23 | **НАСТРОЙКИ ЗАРПЛАТЫ НИКОГДА НЕ СОХРАНЯЛИСЬ. ПОЧИНЕНО, PR #552.** Найдено живой проверкой этапа 33 на стенде, а не тестами: вживую сошлись месяц начисления (расход 05.01.2026 с начислением 2025-12 — в прибыли декабря, в деньгах января; после стирания месяца — в январе), распределение косвенных (пул и чистая прибыль те же при базах «выручка» и «поровну») и источники данных, а ФОТ по сотрудникам включить не удалось. Причина: группы настроек `payroll` не было в справочнике `metable-options`, а необъявленную настройку `SaveSettings` не сохраняет — отвечает ошибкой. Окно «Настройки зарплаты» получало отказ на КАЖДОЕ сохранение ставок, а статью зарплаты и статью налогов в нём нельзя было выбрать вовсе: утверждённый расчёт ставил платежи в календарь без статьи, ФОТ по сотрудникам (FT-014) и база распределения «по ФОТ» (FT-011) работали вхолостую. Починка: группа `payroll` (7 ключей) и `capitalization.profit_multiple` объявлены; в окне — выбор статьи зарплаты и статьи налогов. СТОРОЖ: `metableOptions.spec.ts` теперь проверяет, что каждая пара «группа + ключ», написанная в коде сервера и витрины, объявлена; мутация (группа убрана) роняет его с перечнем всех 7 ключей. УРОК: «написано, но не соединено» — чтение настройки было покрыто тестами, запись не проверял никто. |
| 2026-09-23 | **ЭТАП 33 ЗАКРЫТ ПО КОДУ, PR #551.** ОПиУ: методология — FT-011 (распределение косвенных по направлениям, пять баз, цель правила «сделки/направления»), FT-012 (источники данных), FT-013 (месяц начисления у операции), FT-014 (ФОТ по сотрудникам), FT-015 (водопад прибыли). DoD: сумма распределённого = пулу (спека `spreadIndirect`, остаток копеек — первому по алфавиту); все источники включены — отчёт тот же (спека балансов); платёж 05.01 с начислением 2025-12 — в прибыли декабря и в деньгах января. ГРАБЛЯ: миграция с сырым `UPDATE cost_allocation_rules` упала на стенде — на MySQL под Linux имена таблиц в верхнем регистре, а сырой SQL перевода имён не получает; упавшая миграция оставила замок `knex_migrations_lock`. Переписана построителем запросов, круг «накат → откат → накат» пройден на живой базе, перенос старого поля проверен временным правилом. РЕШЕНИЕ по FT-014: расчёт зарплаты в журнал не пишет, поэтому строка «Зарплата» — это операции по статье, а сотрудники — из расчётов; под ними строка «Остаток статьи вне расчётов зарплаты», и сумма раскрытия всегда равна строке. Прогон: сервер 464 набора / 3546 проверок, витрина 204 файла / 1653 проверки, typecheck по трём пакетам, lang-check зелёный. Сторожа поймали два промаха нового кода (таблица «Месяц начисления» без прокрутки на телефоне, локаль процента в водопаде вписана руками) — починены до слияния. |
| 2026-09-23 | **ПОЧИНЕН УПРАВЛЕНЧЕСКИЙ ОПиУ ПО ДЕНЬГАМ, PR #550.** Выручка счетов, оплаченных отдельной оплатой, признаётся по факту платежа — общим с бухгалтерским ОПиУ правилом (`settlementRecognition.ts`). Живая проверка этапа 32: по начислению отчёты сходятся до рубля. |
| 2026-09-23 | **ЭТАП 32 ЗАКРЫТ ПО КОДУ, PR #549.** Управленческий ОПиУ по ярусам статей (FT-010) и подсказки-формулы (FT-016): лестница МД → ВП1 → ВП2 → ОП → ЧП с рентабельностью, два метода учёта, раскрытие до статей/направлений и до операций, переключатель вида на странице «Прибыль». Живая проверка этапа 31 пройдена. |
| 2026-09-23 | **ЭТАП 31 ЗАКРЫТ ПО КОДУ, PR #548.** Шесть группировок «Денег», доли под суммой, раскрытие ячейки до операций, скрытие пустых строк и переводов, календарь организации (день начала недели, выходные). Попутно исправлены: отбор по направлению обнулял выручку направления (признак «оплачено» теперь по всем ногам), раскрытие не знало юрлица и границ отчёта, ссылка из «Карты статей» терялась, личная галочка сбрасывала соседние. Живая проверка этапа 30 на стенде пройдена (14 колонок, «Итого» не зависит от масштаба, ответ 0,1–0,5 с). |
| 2026-09-23 | **ЭТАП 30 ЗАКРЫТ ПО КОДУ, PR #547.** «Деньги по статьям» — матрица «статьи × периоды» + «Итого» (FT-001), закреплённые шапка и первая колонка общей таблицы отчётов (FT-005b). Равенства держатся устройством: одна выборка ног на весь отрезок, признак «оплачено деньгами» один на все колонки, остатки сцепляются и сверяются с базой. Попутно: дата ноги без moment (втрое быстрее), цвета графика через `--color-*`. |
| 2026-09-23 | **ЭТАП 29 ЗАКРЫТ ПО КОДУ, PR #546; СТЕНД ВПЕРВЫЕ НАКАТЫВАЕТ МИГРАЦИИ ОРГАНИЗАЦИЙ, PR #545.** FT-008 (юрлицо в управленческих сводах) и FT-009 (ярус прибыли у статьи). Главные уроки: (1) ТЗ писалось по коду, а не по данным — «сид по seed_key» на живых данных старых организаций не нашёл бы ни одной доходно-расходной статьи; проверка базы стенда ДО написания сида это показала; (2) стенд месяц жил без миграций организаций — живые проверки блока Б4 на стенде частично шли по коду без таблиц; (3) подделка базы в спеке обязана отвечать так же, как настоящая (camelCase), иначе проверка отката зелёная на коде, который удаляет размеченные статьи. Резервная копия баз стенда перед накатом: `~/stands/bigfin-db-backup-20260923-094358.sql.gz`. Прогон: сервер 447/3399, витрина 195/1584. |
| 2026-09-21 | **ПРИШЛО ТРЕТЬЕ ТЗ — [MASTER-3.md](MASTER-3.md), блок Б5 «Управленческая глубина», этапы 29–41.** Написано по живому интерактивному аудиту Финтабло (личный аккаунт + демо-аккаунт с данными за 2021–2022), а не по описаниям со стороны. Прежние два ТЗ не отменяются: Б1–Б3 и Б4 остаются как есть, Б5 к ним добавляется третьей очередью. Итог аудита короткий: после Б4 Bigfin не отстаёт ни по функциям, ни по входу в них — Bigfin отстаёт по ГЛУБИНЕ управленческого смысла. Три содержательных разрыва: (1) ОДДС у конкурента — это план и факт в одной матрице с сальдо на начало и конец и сцепкой периодов, у нас факт без плановой колонки и без цепочки; (2) ОПиУ у конкурента разложен на ярусы маржинальный доход → ВП1 → ВП2 → ОП/EBITDA → ЧП, у нас одноуровневая прибыль; (3) статья у конкурента — ДВОЙНОЙ классификатор: вид деятельности для ОДДС и тип дохода/расхода для ОПиУ, у нас второго измерения (`pl_type`) нет вовсе, поэтому ярусы не на чем считать. Плюс два дефекта в уже написанном коде Bigfin, найденные тем же аудитом: `applyLegalEntityScope` не вызывается в управленческих сводах (`CashFlowArticlesService`, `ArticlesPlRollup`, `ArticlesCashflowRollup`) — разрез по юрлицу молча игнорируется; `resolveToken` для токенов `bgf_` не вызывается ниоткуда и вебхуки не отправляются. Оба подняты до P0/P1 и входят в этап 29. Отдельно в ТЗ зафиксирован закон для ИИ-финдиректора (раздел 27): числа считает детерминированный бэкенд, модель только объясняет посчитанное и не меняет финансовые данные без явного подтверждения человеком. Очередь блока — раздел 4, начало — этап 29 (фундамент: `pl_type`, раздел ДДС, поведение затрат). |
| 2026-09-20 | **ПОЧИНЕНА ПОМЕТКА «ПРОСРОЧЕНО».** PR #543. Отбор находил просроченные строки, а бейдж не появлялся: срок оплаты приходит объектом даты, а сравнивается со строкой — молчаливое «не просрочено». Найдено живым проходом по стенду через минуту после того, как состояния заработали. Спека закрепляет и верное поведение, и то, что с объектом расчёт врёт. |
| 2026-09-20 | **СПИСОК ЗАДАЧ ТЗ-2 ЗАКРЫТ ПОЛНОСТЬЮ: T-38 И T-34.** PR #542. Колонка «Тренд» в отчётах по периодам (числа из уже пришедших ячеек, без своего запроса; общая таблица отчётов научилась рисовать ячейку, а не только печатать строку) и настройка состава колонок реестра («Дата» и «Сумма» не снимаются). ГЛАВНАЯ ЛОВУШКА ЗАХОДА: разбор напечатанной суммы обратно в число — неразрывный пробел и типографский минус дают `NaN`, и линия пропала бы молча. Прогон: сервер 441 набор / 3343 проверки, витрина 193 файла / 1572 проверки. ДАЛЬШЕ — ТОЛЬКО ПРИЁМКА ВЛАДЕЛЬЦА по чек-листу раздела 15. |
| 2026-09-20 | **СОЕДИНЕНЫ СОСТОЯНИЯ ОПЕРАЦИЙ (T-29, T-32) И ЗАКРЫТ T-14.** PR #541. Расчёт, бейджи и модель отборов существовали порознь и не были связаны ничем — прогон зелёный, экран пустой. Теперь сервер отдаёт состояния строк, таблица показывает колонку, панель — отбор (серверный, по общей формуле долга). Добавлен график над отчётом «Деньги»: числа берутся из строк таблицы, разойтись им не на чем. ОСТАЛОСЬ: T-38 (колонка «Тренд») и T-34 (настройка колонок). Прогон: сервер 441 набор / 3343 проверки, витрина 191 файл / 1545 проверок. |
| 2026-09-20 | **ЗАКРЫТА ПОЗИЦИЯ T-43: ЧЕКБОКСЫ КОЛОНОК ПЛАН-ФАКТА.** PR #540. Их не было вовсе, хотя позиция числилась закрытой. Четыре чекбокса, правило «Факт управляет остальными тремя» отдельным расчётом со спекой, колонка «Вып. плана, %» с «н/о» при нулевом плане. ЭТО ЧЕТВЁРТЫЙ ЗА ДЕНЬ ДЕФЕКТ ОДНОГО КЛАССА — «числится сделанным, а на экране ничего». Прогон: витрина 190 файлов / 1538 проверок. ТЗ-2 закрыто по коду целиком; дальше — приёмка владельца по чек-листу раздела 15. |
| 2026-09-20 | **ЗАРАБОТАЛИ ЛИЧНЫЕ НАСТРОЙКИ ВИДА.** PR #539. Галочка «отображать копейки» сохранялась и не меняла ничего: её никто не читал. Общая печать денег теперь спрашивает настройку, настройки грузятся при входе и применяются сразу при сохранении. Сторож проверяет и влияние на печать, и сам факт загрузки при входе. ЯВНЫЙ ОСТАТОК: экспорт в XLSX формирует сервер, личную настройку он пока не знает. Прогон: витрина 189 файлов / 1530 проверок. |
| 2026-09-20 | **ПОЧИНЕН МАСШТАБ ПЛАТЁЖНОГО КАЛЕНДАРЯ, ЗАКРЫТА ПОЗИЦИЯ T-42.** PR #538. Переключатель масштаба менял запрос, сервер считал периоды — а экран рисовал дни и периоды не читал ни разу. Добавлены разбор `periods` в ответе, раскрываемая клетка `PeriodRow` (факт и план врозь, внутри — дни со строками и кнопкой «Записать в учёт») и сторож, проверяющий, что экран периоды ЧИТАЕТ. Список задач ТЗ-2 закрыт полностью, кроме осознанно пропущенной T-54 (вопрос об отрасли в мастере настройки — вести некуда). Прогон: витрина 188 файлов / 1523 проверки. |
| 2026-09-20 | **ЗАКРЫТЫ ПОСЛЕДНИЕ ЗАДАЧИ СПИСКА ТЗ-2: T-56, T-57, T-39.** PR #537. Сверка раздела 13 по позициям показала, что этапы раздела 12 покрывают не весь список: два P0 и один P2 остались без исполнителя. Заведён каталог `FinancialStatements/convergence/`: три набора данных, десять инвариантов раздела 11.10, реестр «инвариант → файл проверки», снимки «золотых» ответов. Добавлен индекс `(project_id, date)` с замером в комментарии. УРОК: прогон миграции на живой базе поймал падающий `down()` — база убирает одиночный индекс внешнего ключа, когда появляется составной с той же первой колонкой, и удалить составной становится нечем. Прогон: сервер 440 наборов / 3335 проверок. |
| 2026-09-20 | **ЗАВЕДЕНА ПРОВЕРКА «ОСТАТКИ РАЗОШЛИСЬ».** PR #536. Живой проход нашёл расхождение между шапкой и отчётом о движении денег — 1 000,50 ₽ по одной кассе. Остаток хранится дважды (колонка счёта и проводки), и когда они расходятся, экраны спорят друг с другом молча. Новая вкладка в «Качестве данных» показывает счёт, обе суммы, разницу и итог. Чинить сами не беремся: какая сумма верна, решает человек. Прогон: сервер 434 набора / 3267 проверок, витрина 187 файлов / 1518 проверок. |
| 2026-09-20 | **ПОЧИНЕН ОПиУ: ГЛАВНАЯ ПОКАЗЫВАЛА ЧИСЛА ЧУЖОГО ПЕРИОДА.** PR #535. Найдено живым проходом по стенду сразу после закрытия очереди Б4: главная за август показывала июльскую выручку, и «за период» совпадало с «к прошлому периоду» до копейки. Причина — общее хранилище отчёта и ДВА ОДНОВРЕМЕННЫХ вызова с главной. Починено очередью построений в самой службе — так защищены все вызывающие, включая тех, кто о проблеме не знает. Мутацией доказано, что без очереди оба вызова получают один период. УРОК: заглушка отчёта в спеке главной не могла поймать эту ошибку в принципе — гонка живёт НЕ в вызывающем коде, а в общем состоянии того, кого зовут. Прогон: сервер 433 набора / 3259 проверок. |
| 2026-09-20 | **ЭТАП 28 БЛОКА Б4 ЗАКРЫТ ПО КОДУ — ОЧЕРЕДЬ ТЗ-2 ПУСТА.** PR #534. Три отраслевых демо-набора, параметр `industry` через всю цепочку (миграция системной схемы → запись о демо → подписчик → наполнение), расширение прогона Playwright. **ЖИВОЙ ПРОХОД ПО СТЕНДУ НАШЁЛ ДЕФЕКТ ЭТАПА 27, КОТОРОГО НЕ ВИДЕЛИ ТЕСТЫ:** «Кто приносит прибыль» пуст при выручке 1 430 000, потому что на стороне дохода нет контрагента ни у одной проводки. Починено в этом же PR, покрыто спекой с мутацией. ВТОРАЯ НАХОДКА, УЖЕ СВОЯ: первая версия расчёта разрыва считала просроченный счёт будущим поступлением — то есть закрывала разрыв деньгами, которых не получить. ТРИ МЕСТА, ГДЕ ТЗ ОКАЗАЛОСЬ НЕТОЧНЫМ: путь к наборам (сиды базы — а демо наполняется службами), «`onboarding.spec.ts` уже написан» (написан под англоязычный экран), вопрос об отрасли в мастере настройки (вести некуда). Все три разобраны в «Указателе». Прогон: сервер 432 набора / 3255 проверок, витрина 187 файлов / 1518 проверок, `typecheck` по трём пакетам, `lang-check` зелёный. |
| 2026-09-20 | **ЭТАП 27 БЛОКА Б4 ЗАКРЫТ ПО КОДУ** — главная, контрагенты, сделки, справка. PR #533. ЧЕТЫРЕ ЧИСТЫХ РАСЧЁТА СО СПЕКАМИ: Парето (`computeParetoContractors`, 13 проверок), прибыльность направлений (`computeDirectionsProfit`, 13), разбор долга (`computeDebtBreakdown`, 13), прогресс сделки (`dealProgress`, 10). ЧТО ВСКРЫЛОСЬ ПО ХОДУ. (1) Первый вариант разбора долга делил документы по видам — и противоречил ТЗ, которое требует зачёта аванса ДО классификации; переписан на сальдо расчётов, знак сальдо и есть ответ «чем закроется». (2) Свёртка `ArticlesPlRollup` режет по ОДНОМУ направлению за вызов, поэтому блок направлений считается своим запросом с группировкой в SQL — иначе главная делала бы столько запросов, сколько у человека направлений. (3) `Deal` и `Project` — ОДНА таблица `projects` с разной семантикой; это не ошибка, но знать об этом надо. (4) Новая ручка `contacts/debt-breakdown` объявлена ВЫШЕ `:id`: ниже она угодила бы в `ParseIntPipe` и отвечала «400, ожидалось число». СТОРОЖА: два новых — `screenHelpPresence` (50 корневых экранов, парность en↔ru, абзацы, «вы» со строчной) и `noCalquesInHelp` (13 калек с заменами). Оба проверены мутацией; первый поймал собственного автора и был СУЖЕН до середины предложения. Прогон: сервер 429 наборов / 3188 проверок, витрина 186 файлов / 1511 проверок, `typecheck` по трём пакетам, `lang-check` зелёный. Словари выросли на 105 ключей справки + 17 ключей блоков. |
| 2026-09-20 | **ЭТАП 17 БЛОКА Б4 ЗАКРЫТ ПО КОДУ** — статья учёта стала пятивидовой. Пять шагов, пять коммитов, PR #523. ЗАЧЕМ: движение денег, которое не доход и не расход (взяли кредит, купили станок, собственник внёс деньги), разметить было НЕЧЕМ — человек ставил неверную статью, и тело кредита попадало в расходы, искажая прибыль, либо бросал разноску. **ГЛАВНЫЙ РИСК И КАК ОН ЗАКРЫТ:** свёртку по статьям зовут ПЯТЬ расчётов (безубыточность, рентабельность сделок, распределение накладных, капитализация, ИИ-аналитик). Попади туда «Получение кредита» — все пять посчитали бы тело кредита выручкой, и посчитали бы ТИХО: строка выглядит как обычная статья, отчёт остаётся связным, прибыль другая. Отбор поставлен В ОДНОМ МЕСТЕ СБОРКИ, а не у каждого потребителя: потребитель, который о правиле не знает, — это ошибка, ждущая своего дня. Мутация роняет 3 теста из 4. **ПОЧЕМУ НИ ОДНА СУММА НЕ ИЗМЕНИЛАСЬ:** догоняющий сид заводит 11 балансовых статей БЕЗ привязки счетов. Пока счёт не привязан, статья не собирает ничего. **УСТОЙЧИВЫЙ КЛЮЧ ВМЕСТО ИМЕНИ:** колонка `seed_key`. Имя системной статьи разрешено менять, и сверка по имени после переименования «Получения кредита» в «Кредиты банка» завела бы близнеца — половина операций размечена одной статьёй, половина другой, отчёт разъезжается на две строки, и обе выглядят правдоподобно. По этому же ключу витрина ставит замок. **ТРИ ПОПРАВКИ К ТЗ.** Места ТЗ-2 без метки `[CODE-CONFIRMED]` — догадки, и в этом этапе их нашлось три, все вида «сделать то, что уже сделано»: проверка совместимости счёта и вида (`accountRootType !== kind`) написана обобщённо ещё под два вида и на пяти заработала БЕЗ ПРАВОК (корневые типы плана счетов ровно те же пять); отбор `?kind=` в ручке существовал вместе с тонкостью «в режиме дерева отбор нельзя делать запросом, иначе ребёнок отфильтрованного родителя станет ложным корнем»; правило «поле постоянный/переменный только у расходов» написано как `kind === 'expense'`, а не «если не доход», и расширилось само. **ЧТО ДОДЕЛАНО ПРОТИВ ТИХОГО ВРАНЬЯ:** опечатка в виде (`?kind=liabilty`) раньше возвращала 200 и пустой список — экран показал бы пустую вкладку, и человек поверил бы ей. На сервере теперь отказ; на витрине опечатка в адресе приводит к вкладке по умолчанию, а не к пустоте (ошибку показывать некому — ссылку прислали читателю). **ВИД ЗАБЛОКИРОВАН ПРИ ПРАВКЕ:** смена вида у статьи с операциями разом переносит суммы между отчётами — прибыль прошлого квартала станет другой, и причину никто не вспомнит. Не «предупредить», а не дать, с объяснением что делать вместо. ОТКАТЫ ОБЕИХ МИГРАЦИЙ ЧЕСТНЫЕ: домен переводит балансовые статьи в расход и ГРОМКО пишет в журнал (удалить — осиротеет разметка, оставить — прежний код упрётся в неизвестный вид); сид не трогает статьи с привязанными счетами и их родителей и удаляет дерево ЦИКЛОМ (один проход оставил бы корни висеть пустыми). Мутации: отбор видов 3/4, идемпотентность и цикл отката 4/14, адрес и счётчик 2/14. Прогон: типы чисты, парность en↔ru, сервер 3012/414, витрина 1464/179 — зелёные. ОСТАТКИ ВЛАДЕЛЬЦУ: живой проход глазами не делался; догоняющий сид НЕ прогонялся на живой базе стенда, хотя DoD этапа этого требует. План и приёмка: `docs/superpowers/plans/2026-09-20-tz2-17-статьи.md` |
| 2026-09-20 | **ЭТАП 16 БЛОКА Б4 ЗАКРЫТ ПО КОДУ** — первый этап второго ТЗ. Четыре шага, четыре коммита, PR #522. ГЛАВНОЕ НЕ В КОДЕ, А В ТОМ, ЧТО ВСКРЫЛОСЬ. **SDK ОТСТАЛ НА 174 АДРЕСА** (273 → 447 путей): витрина не видела больше трети возможностей сервера, потому что workflow «Generate OpenAPI SDK Types» стоит `disabled_manually` (вместе с E2E). Ровно на этих ручках этапы 17–28 собираются строить экраны. Генерация выполнена ЛОКАЛЬНО по рецепту из самого workflow — одноразовые контейнеры MySQL и Redis на портах 3399/6399, а НЕ службы стенда: приложение при старте цепляется к очереди задач, и на Redis стенда процесс генерации мог бы съесть настоящие задачи. **SDK ЗВАЛ ДВЕ НЕСУЩЕСТВУЮЩИЕ РУЧКИ**: `/api/sale-estimates/{id}/notify-sms` и `/sms-details` (отправка КП по SMS). На сервере их нет ни строчкой — обёртка молча расходилась с сервером всё время, пока генерация была выключена: список есть, но он про прошлое, и машинная проверка «нельзя позвать несуществующий адрес» превратилась в вежливую ложь. Убраны вместе с вызовами, в витрине их не использовал никто. Без этого SDK не собирается вовсе. **ТЗ ОШИБЛОСЬ В ОДНОМ ИЗ ДВУХ ПЕРЕЕЗДОВ**: `ArticlesPlRollupService` уже лежал в `ManagementArticles/queries/` — место в ТЗ-2 не имело метки `[CODE-CONFIRMED]`, и проверка по Приложению Б окупилась первым же действием. **УРОК ПРО СТОРОЖЕЙ, ПОВТОРЁННЫЙ ТРЕТИЙ РАЗ**: регрессионная спека вердикта главной в первой редакции мутацию НЕ ЛОВИЛА. Подмена «не хватит 30 000 пятого июня» на глубину ямы «300 000» проходила зелёной, потому что в деньгах неразрывный пробел (U+00A0), а в тексте проверки обычный, — `not.toContain('300 000')` не совпадал НИКОГДА. Зелёный сторож и работающий сторож — разные вещи, и отличает их только мутация. Мутации этапа: расчёт разрывов 6 падений из 14, геометрия тренда 2 из 16, зашитый цвет ловится сторожем §10.4. **СОБСТВЕННЫЕ НАРУШЕНИЯ, ПОЙМАННЫЕ ЧУЖИМИ СТОРОЖАМИ**: `fractionPrintGuard` поймал `toFixed(2)` в координатах SVG, `moneyFormatGuard` — свой форматтер денег в историях Storybook. Оба правила верны (пример в истории копируют как образец), починен мой код, сторожа не ослаблялись. РЕШЕНИЯ: `to: null` у незакрытой ямы не подменяется датой горизонта («кончилась в последний день» и «выхода не видно» — разные ответы); дном при равной глубине считается ПЕРВЫЙ день; оповещение о разрыве по-прежнему читает `gap`, а не глубину, и причина записана в коде. ГРАБЛИ ОКРУЖЕНИЯ: системный `pnpm` девятка при требуемой `10.34.4` (`corepack pnpm@10.34.4 …`); в свежей рабочей копии общие пакеты надо собрать до `typecheck`. Прогон: типы чисты, парность en↔ru 4706 ключей, сервер 2979/410, витрина 1450/178 — зелёные. План и приёмка: `docs/superpowers/plans/2026-09-20-tz2-16-подготовка.md` |
| 2026-09-20 | В репозиторий принято ВТОРОЕ мастер-ТЗ — `docs/tz/MASTER-2.md`, блок Б4 «Вход в продукт», этапы 16–28. Написано по живому проходу по ПланФакту (`demo.planfact.io`) и чтению кода Bigfin; каждое утверждение помечено `[OBSERVED]` / `[CODE-CONFIRMED]` / `[INFERRED]` / `[PROPOSED]`. Вывод: отставание не в функциях, а во входе в них. `MASTER.md` не редактировался — в его шапку добавлена одна строка-ссылка, как требует Приложение В ТЗ-2. Развёрнуты: таблица этапов 16–28 (раздел 3), очередь блока Б4 (раздел 4), условие включения стабилизации (раздел 5). `CONTINUE.md` дополнен: при пустой очереди Б1–Б3 берётся очередь Б4, и только при пустой и этой — стабилизация |
| 2026-09-19 (вечер) | Заход «доделать проект» по поручению владельца: решения по открытым вопросам принимались самостоятельно. ПЯТЬ PR: #467, #468, #470–#473. **БЛОКЕР Ю1 СНЯТ** — миграции и заполнение прошли на живой базе стенда (20 организаций, 776 строк, юрлицо «Демо-организация, ООО, RUB» создано из реквизитов). Оказалось, что Ю1 — не «руки не дошли», а ТРИ ПОЛОМКИ ПОДРЯД. (1) МИГРАЦИИ ТЕНАНТОВ НЕ ПРОХОДИЛИ ВООБЩЕ: рядом с ними лежат две сторожевые спеки, а загрузчик читает КАЖДЫЙ файл каталога — спека при загрузке зовёт `describe(...)`, и все 20 организаций падали с «describe is not defined». Запустить миграции было нельзя в принципе с того дня, как эти спеки появились. (2) ЗАПОЛНЕНИЕ БЫЛО НАПИСАНО, НО ЕГО НЕЧЕМ БЫЛО ЗАПУСТИТЬ: службу звали только из очереди задач, а поставить задачу в очередь не умел никто. Появилась команда `tenants:legal-entity:backfill` с пробным прогоном; сторож `commandsAreReachable.spec.ts` (класс объявлен → перечислен В СПИСКЕ providers → есть ярлык запуска) сразу нашёл ещё одну команду без ярлыка. (3) МИГРАЦИЯ КОЛОНОК ЮРЛИЦА МОЛЧА НИЧЕГО НЕ ДЕЛАЛА: `hasTable('accounts_transactions')` отвечает «нет» про существующую `ACCOUNTS_TRANSACTIONS` — имя уезжает ЗНАЧЕНИЕМ, отображение регистра к нему не применяется, а MySQL под Linux к регистру чувствителен. Миграция прошла все 13 таблиц мимо и записалась выполненной. Тот же капкан ломал проверку «юрлицо чем-то занято» (удалить юрлицо дали бы вместе со всем, что на него ссылается). ВАЖНО: `hasColumn` и `hasTable` ведут себя ПО-РАЗНОМУ, поэтому соседняя миграция сработала — разница невидима. Капкан был описан ещё в августе в миграции про демо «в один щелчок» и через месяц повторён: теперь его держит сторож `noRawSchemaExistenceChecks.spec.ts`. ЧЕТВЁРТАЯ ПОЛОМКА того же рода: код читал `row.legal_entity_id`, а отображение knex возвращает `legalEntityId` — в справочнике у КАЖДОГО юрлица стояло «0 счетов», а отчёт «Внутригрупповые обороты» складывал ВСЕ ноги в одну группу (ключ `undefined:undefined`), то есть весь смысл этапа 7 не работал. Тесты были зелёными, потому что ПОДДЕЛКИ в них отдавали змеиные имена — повторяли ту же ошибку. ПЯТАЯ: сторож зашитого текста проверял разметку ПОСТРОЧНО, а текст после форматирования стоит на своей строке — сторож был зелёным, пока весь вход и регистрация говорили зашитыми словами (52 места в 11 файлах, переведено 84 подписи). ШЕСТАЯ: `<x.div width="180px">` — `@xstyled` знает `w`, но не `width`. РЕШЕНИЯ ПО ОТКРЫТЫМ ВОПРОСАМ: В1 — правовые страницы остаются только на русском, переводится лишь пояснение о языке документа; В2 — кнопка «Войти через Google (скоро)» УБРАНА (обещание, которого продукт не выполняет); В3 — тёмная тема на стенде; В4 — удалённый `TopbarUser.tsx` остаётся удалённым. Р4 ЗАКРЫТ: деньги мимо статей больше не исчезают молча — пробел считается отдельно и виден числом НАД цифрами. Строкой в свёртку не подмешивали: её зовут два десятка мест, и это лечило бы тишину новой тишиной. ПЯТЬ ЭКРАНОВ: капитализация `/capitalization` (С1–С2), публичный API `/preferences/public-api` (П1), панель разделения операции в ящике операции (Р1), блок «Что говорят цифры» на главной и настройки `/preferences/ai-analyst` (И1–И2), чат `/ai-chat` (Ч1). Прогон: сервер 2791/391, витрина 1204/155 — зелёные. |
| 2026-09-19 | Стабилизация v90, источник №5 (долг по `@ts-nocheck`): слой «семь замечаний и ниже» — 16 файлов, 85 замечаний (15 сервер, 1 витрина). Слепая зона 149 → 133 файла. ДВЕ ЖИВЫЕ ПОЛОМКИ. Первая: окно «Пригласите свою команду» ГОВОРИЛО ПО-АНГЛИЙСКИ — пять строк («Select role», «Duplicate email», «Role is required», две про почту) были вписаны в код мимо словаря; сторож `noHardcodedStrings` каталог `ee/` охватывает, но ищет КИРИЛЛИЦУ В РАЗМЕТКЕ — зеркальную беду, а здесь английский текст внутри выражений кода; сторожа на вписанный английский в проекте нет вовсе. Вторая: `<x.div width="180px">` — библиотека `@xstyled` знает `w`, но НЕ знает `width` (сверено по описанию типов 3.8.1), неизвестное свойство уходит атрибутом в `<div>`, где его в HTML нет, — ширина колонки выбора роли НЕ ПРИМЕНЯЛАСЬ НИКОГДА. Три ловушки: проверки валюты счёта объявляли, что принимают и правку тоже, хотя у правки поля `currencyCode` нет вовсе (позови кто из правки — молча пропустили бы всё); справочник «Прибыли и убытки» построен на ПУСТОМ классе, тогда как примесь рассчитывает на `getAmountMeta`/`getDateMeta`; письмо покупателю читало шесть полей, которых не было в описании ответа — ровно тот же род промаха, что убил номер чека в v89. Шесть перечней, отставших от кода: горизонтальные итоги Баланса описаны не тем видом (у `IBalanceSheetTotal` нет `total`, а код читает `horizontalTotals[i].total.amount`) — одна правка сняла замечания в пяти файлах; номер узла «чистая прибыль» — строка `'NET_INCOME'`, а не число; узел-свёртка собирается сразу с `nodeType` и `type`; помощники «прошлый период» описаны как узел одного отчёта, хотя общие для трёх; двойное объявление `i18n`; заготовка миграций обещала `number`, а отдаёт обещание. Четыре общих корня закрыли 47 замечаний из 85. Снятие `R.curry` только там, где ни одного частичного вызова нет (проверялось поиском). УДАЛЕНО ТОЛЬКО двойное объявление `i18n` (дубль того же поля в том же классе). Прогон: сервер 2709/385 зелёные, витрина зелёная, парность en↔ru 4441 ключ. Остатки владельцу: О-v90-1 описание ответа по счёту по-прежнему неполное; О-v90-2 завести сторожа на вписанный АНГЛИЙСКИЙ текст — зеркало к нынешнему `noHardcodedStrings`. Карта: `docs/superpowers/specs/2026-09-19-stabilization-ts-nocheck-v90.md` |
| 2026-09-19 | Стабилизация v89, источник №5 (долг по `@ts-nocheck`): слой «четыре замечания и ниже» — 40 файлов, 73 замечания, все серверные. Слепая зона 189 → 149 файлов (сервер 72 → 32). ШЕСТЬ НАСТОЯЩИХ ПОЛОМОК. ГЛАВНАЯ: НОМЕР ЧЕКА НЕ ДОХОДИЛ ДО ПЕЧАТНОЙ ФОРМЫ ВООБЩЕ — сервер отдавал `receiptNumber`, а форма читает `receiptNumebr` (перестановка букв), и покупатель на каждом чеке видел заглушку шаблона «346D3D40-0001». Переименовать нельзя: этим ключом лежат сохранённые настройки шаблонов в JSON-поле `pdf_templates.attributes`, переименование осиротило бы их молча — поэтому починена сторона, которая ошибалась. Сторож `documentNumberReachesTemplate.spec.ts` проверяет ОБЕ стороны сразу по трём видам документов (у кредит-ноты и счёта они согласованы). Вторая: БАЛАНС «ПО ПЕРИОДАМ» — `isDisplayColumnsBy` сравнивал поле у ОБЁРТКИ `BalanceSheetQuery`, которого у неё нет, то есть ВСЕГДА «нет»; шапка показывала периоды, а значения приходили из итоговой колонки. Ещё: единица периода уходила `undefined` в двух местах; обращение к модели без скобок вызова (`.query()` вместо `().query()`); `TenantDBManager.seed()` не работал никогда (обращался к несуществующим `tenantSeedConfig` и `tenant`) — ОСТАВЛЕН с внятным отказом, ПРЕДЛОЖЕН К УДАЛЕНИЮ. Плюс шесть перечней, отставших от кода (виды операций склада без кредит-ноты и возврата поставщику; скидка и корректировка в чеке; `children` у узлов отчёта; `relationFields`; `FIELD_TYPE` без `as const`). Сломанные импорты: `@/interfaces` НЕ СУЩЕСТВУЕТ, а `ICreditNote`/`ISaleInvoice`/`ISaleReceipt`/`IInventoryTransaction` — призраки, не объявлены нигде; `FsMigrations` объявлен дважды (класс и пустой перечень), настройки ссылались на пустой. Три общих корня закрыли 26 замечаний из 73: `toMutableList` (ramda отдаёт «только для чтения»), `as const` в семи вызывающих, `sameNodeShape`. НИЧЕГО НЕ УДАЛЕНО. Карта: `docs/superpowers/specs/2026-09-19-stabilization-ts-nocheck-v89.md`. Прогон: сервер 2706/385, типы чисты. |
| 2026-09-18 | Этап 14 ЗАКРЫТ ПО КОДУ «ИИ-чат по финансам» — ОЧЕРЕДЬ ТЗ ПУСТА, все 15 этапов закрыты по коду. Модель НЕ имеет доступа к базе: закрытый перечень из 6 инструментов, каждый зовёт существующий отчёт, своего расчёта нет ни одного (иначе чат отвечал бы не то, что человек видит на экране). Инструмента «произвольный запрос» нет намеренно. ГЛАВНОЕ: здесь опаснее, чем у аналитика, — человек СПРОСИЛ и ждёт число, примет его как факт и сверять не пойдёт. Поэтому неизвестный инструмент ОТВЕРГАЕТСЯ, А НЕ УГАДЫВАЕТСЯ (догадка подставит не тот отчёт, и ответ будет уверенным и неверным), причина отказа уходит обратно модели (молчаливый отказ заставил бы её выдумать ответ), лишний параметр отвергается (модель думает, что сузила запрос, а получила бы весь отчёт), дата 2026-02-31 не проходит (JS молча делает из неё 3 марта), перепутанные даты не проходят (пустой отчёт модель озвучила бы как «ничего не было»), число из ВОПРОСА не считается выдумкой, ответ без ссылки НЕ ПОКАЗЫВАЕТСЯ, неразобранный шаг = пустой ответ а не вызов, не больше 4 кругов (каждый круг платный). Результат инструмента идёт ОБРАТНО В МОДЕЛЬ, поэтому проходит ту же проверку приватности. Своего выключателя у чата НЕТ: два выключателя одного ИИ — два места, где можно забыть выключить отправку. POST /ai-chat/ask внесён в OPEN_BY_DESIGN сторожа с обоснованием (ничего не меняет; право на запись сделало бы вопрос о своих цифрах доступнее меньшему числу людей, чем сам отчёт) — сторож НЕ ослаблен. Мутация: угадывание инструмента роняет 2 теста, снятие сверки чисел — 2. ОСТАТКИ Ч1–Ч4: экран, инструменты ДДС/Баланса/разрывов (там строка = остаток, подставлять свёртку статей нельзя — сошлось бы по форме и разошлось по смыслу), память разговора, живой вызов провайдера. Прогон: сервер 2698/384, типы чисты. |
| 2026-09-18 | Этап 13 ЗАКРЫТ ПО КОДУ «ИИ-аналитик». ГЛАВНАЯ МЫСЛЬ: модель хорошо формулирует и плохо считает, а НЕВЕРНОЕ ЧИСЛО ВЫГЛЯДИТ РОВНО ТАК ЖЕ, КАК ВЕРНОЕ — человек примет решение по цифре, которой нет ни в одном отчёте. Два слоя: (1) сбор данных РАЗРЕШИТЕЛЬНЫЙ — в модель уходят только перечисленные поля, забыть добавить поле безопасно, забыть запретить нет; (2) каждое число из ответа сверяется с теми, что мы модели дали, не сошлось — наблюдение выбрасывается ЦЕЛИКОМ, и недобор НЕ добирается выброшенными (иначе вернули бы в отчёт ровно то, что признали недостоверным). Приватное поле ЛОМАЕТ сборку промпта, а не вычищается молча: тихая вычистка «починила» бы настоящую утечку незаметно для нас. Проверка стоит в ЕДИНСТВЕННОЙ точке сборки. Обязательный тест §13.4 написан (назначение платежа, ИНН, ФИО, контрагенты). Флаг `ai_analyst` выключен по умолчанию; запрет «не передавать данные во внешние сервисы» ЗАКРЫВАЕТ РАЗДЕЛ С ОБЪЯСНЕНИЕМ (§13.1 п.5 «не деградирует молча» — пустой блок неотличим от «нечего сказать»), но локальной моделью УДОВЛЕТВОРЯЕТСЯ; чужой адрес не выдаёт себя за локальную модель, кривой адрес локальным не считается. 4 провайдера за интерфейсом `AiProvider`, ключи в настройках организации; неизвестный провайдер = «выключено», чтобы опечатка не роняла главную. Суточный кеш (расписание 5:00, очередь, таблица `ai_insights`) со счётчиками предложено/отбраковано — иначе не увидеть, что модель начала врать. Мутация: разрешительный список → запретительный роняет 2 теста, снятие сверки чисел — 3. ОСТАТКИ И1–И4: витрина блока и настроек, агрегаты пока только по ОПиУ (в ДДС и Балансе строка = остаток), живого вызова провайдера не было. Прогон: сервер 2662/381, витрина 1099/142, словари 4386/4386. Дальше — этап 14. |
| 2026-09-18 | Этап 15 ЗАКРЫТ ПО КОДУ «Публичный API и вебхуки». Токены: в базе ТОЛЬКО отпечаток sha256, сам токен показывается человеку один раз; сравнение постоянного времени; ГЛАВНОЕ ПРАВИЛО — ПУСТОЙ СПИСОК ПРАВ ЭТО НЕ «МОЖНО ВСЁ» (токен, у которого забыли проставить права, не получает их по умолчанию); незнакомое право выбрасывается при сохранении, иначе опечатка `reports:raed` осела бы в базе и выглядела как выданное право; отзыв, а не удаление (удалённый унёс бы ответ на вопрос «кто им пользовался»); наружу всегда ОДИН отказ, чтобы перебирающий не узнал, что угадал. Вебхуки: подпись HMAC от `время.тело` со сроком свежести 300 с (без времени перехваченный вызов повторяется вечно), 4xx не повторяем кроме 408/429, удвоение задержки, журнал доставок. ЗАПРЕТ ВНУТРЕННИХ АДРЕСОВ: вебхук делает НАШ сервер по адресу пользователя, и без запрета его можно послать на `169.254.169.254` за ключами машины. СХЕМЫ: `api_tokens` в СИСТЕМНОЙ (по токену организацию ещё предстоит найти — в тенантной пришлось бы обходить все базы), `webhooks`/`webhook_deliveries` в тенантной. Swagger UI на `/api/docs`, МОЛЧАНИЕ ЗНАЧИТ ЗАКРЫТО (на бою только при `API_DOCS_ENABLED=true`). Руководство `docs/public-api.md` с curl и TS SDK. НАХОДКА: автопроверка безопасности указала на настоящую дыру — при нечисловом времени `Math.abs(NaN) > 300` даёт «нет», и проверка свежести пропускала бы что угодно; закрыто. Сторож `settingsReadOnLoadGuard` покраснел на `process.env` в значении параметра по умолчанию (по духу не нарушение) — сторожа НЕ ослаблял, перенёс чтение в тело. Мутация двух правил роняет по тесту. ОСТАТКИ П1–П4: экран настроек, рассылка событий подписчиками и очередью, страж проверки токена на входе, публичная веб-страница. МИГРАЦИИ НЕ ЗАПУСКАЛИСЬ НИ НА ОДНОЙ БАЗЕ. Прогон: сервер 2586/375, типы чисты. Дальше — этап 13. |
| 2026-09-18 | Этап 7, шаг 7.1: колонка `is_intercompany` у проводок (миграция, по умолчанию false) и правила внутригрупповых оборотов (`LegalEntities/utils/intercompany.ts`, 13 тестов). Признак ХРАНИТСЯ, а не вычисляется: ТЗ даёт человеку переключатель в ручной операции, и его выбор должен пережить перезагрузку. ГЛАВНОЕ ПРАВИЛО: НЕИЗВЕСТНОЕ юрлицо НЕ считается «другим» — колонка юрлица пока пуста на всех базах (заполнение этапа 6 не запускалось), и трактовка пустоты как «другое» объявила бы внутригрупповым КАЖДЫЙ перевод, а отчёт молча потерял бы выручку. Из двух молчаливых ошибок выбрана заметная: «не исключили нужное» видно в отчёте, «исключили лишнее» — нет. Снятие защиты роняет тест. Плюс правило, когда исключать: все юрлица — да, одно — НЕТ (для него это настоящий доход), набор из нескольких — да. Прогон: сервер 2441/361. |
| 2026-09-18 | Этап 6 ЗАКРЫТ ПО КОДУ КРОМЕ ШАГА 6.5, шаг 6.6 — приёмка. Проверял по коду: миграции, модель, сервисы, ручки, экран. Закрыто: все 17 полей таблицы `legal_entities` в ТЕНАНТНОЙ схеме, колонка в 13 таблицах с индексами, юрлицо по умолчанию, заполнение пакетами, ручки с правами, экран с живой валидацией реквизитов. ОСТАТКИ Ю1–Ю5: Ю1 МИГРАЦИИ И ЗАПОЛНЕНИЕ НИ РАЗУ НЕ ЗАПУСКАЛИСЬ НИ НА ОДНОЙ БАЗЕ (порядок для владельца: бэкап → миграции → заполнение → проверка → и только потом NOT NULL); Ю2 шаг 6.5 NOT NULL ждёт согласия; Ю3 в форме нет полного наименования, НДС, валюты, директора, адресов, банка и переключателей «головное»/«действующее» (сервер их принимает, юрлицо по умолчанию заполняет из реквизитов, но изменить на экране нельзя); Ю4 нет колонки «активно» — сейчас это приглушённая строка; Ю5 правило «не навязывать» готово, но колонок «Юрлицо» в других разделах пока нет вовсе (этап 7). Живой пробы не было. Прогон: витрина 1094/141, сервер 2428/360. Дальше — этап 7. |
| 2026-09-18 | Этап 6, шаг 6.4б: экран `/legal-entities` и пункт «Юрлица» в «Справочниках». Таблица название·форма·ИНН·налоги·доля·счетов, форма RHF+Zod. ТРЕТЬЕЙ ВЕРСИИ ПРАВИЛ НЕ ПОЯВИЛОСЬ: проверки ИНН/КПП/ОГРН и списки форм/режимов переиспользованы из тех же мест, что реквизиты организации, — включая порядок подсказок (сначала длина, потом контрольная цифра). «Раздел не навязывается» разобрано: пункт меню есть ВСЕГДА (второе юрлицо надо откуда-то завести), а навязывание — это колонки и отборы в ДРУГИХ разделах; правило готово (`shouldShowLegalEntityBreakdown`, считает ДЕЙСТВУЮЩИЕ), сами колонки — этап 7. Кнопка удаления прячется там, где сервер откажет. Доля владельца 0..100, запятая принимается (поле текстовое: браузер в числовом её выбрасывает). 22 проверки. Прогон: витрина 1094/141, словари 4380/4380. |
| 2026-09-18 | Этап 6, шаг 6.4а: ручки справочника юрлиц — список со счётчиком закреплённых счетов, создание, правка, удаление; валидаторы ИНН/КПП/ОГРН переиспользованы из `RussianLegalAttributes`. Правила: список никогда не пуст (лениво создаётся юрлицо по умолчанию); юрлицо С ОПЕРАЦИЯМИ удалить нельзя; ПОСЛЕДНЕЕ удалить нельзя; головное одно — признак снимается с остальных ТОЛЬКО когда его явно поставили (ослабление роняет тест). ДВА СТОРОЖА ПОЙМАЛИ НАСТОЯЩУЮ ДЫРУ В ПРАВАХ: (1) `writeEndpointsCoverage` — ручки записи не объявляли нужное право, удалить юрлицо мог бы любой вошедший; (2) `accessGates` — пометка права БЕЗ стража `PermissionGuard` это «мнимая защита». Оба исправлены. 10 проверок. Прогон: сервер 2428/360, типы чисты. |
| 2026-09-18 | Этап 6, шаг 6.3: фоновая задача заполняет `legal_entity_id` у существующих строк пакетами по 10 000 во всех 13 таблицах. ДВА ПРАВИЛА ВАЖНЕЕ СКОРОСТИ: (1) трогаем ТОЛЬКО пустые строки — строке могли назначить юрлицо руками, и заполнение не должно переписать выбор; условие стоит ДВАЖДЫ, при чтении пакета и при записи (между ними есть окно), снятие второго роняет 2 теста; (2) пакетами — один UPDATE на сотни тысяч проводок держит блокировку и подвешивает продукт, поэтому отдельная задача, а не часть миграции. Из первого правила бесплатно следует возобновляемость. НОВЫЙ СТОРОЖ `backfillTables.spec.ts`: список таблиц живёт в ДВУХ местах (миграция и заполнение) и обязан совпадать — расхождение не падает, таблица просто останется пустой навсегда. Отсутствующая таблица пропускается; есть потолок пакетов от вечного цикла. ЗАДАЧА НИ РАЗУ НЕ ЗАПУСКАЛАСЬ: базы для прогона нет. 11 проверок. Прогон: сервер 2418/359. |
| 2026-09-18 | Этап 6, шаг 6.2: юрлицо по умолчанию из реквизитов организации — головное, доля 100%. ДВЕ СХЕМЫ В ОДНОМ ШАГЕ: реквизиты лежат в СИСТЕМНОЙ `tenants_metadata`, справочник юрлиц — в ТЕНАНТНОЙ; чтение и запись разведены явно. Сидер СДЕЛАН ЛЕНИВЫМ (создаётся при первом обращении к справочнику), а не разовым прогоном: разовый скрипт надо не забыть повторить для тех, кто заведётся завтра. Цена — вызывается часто и из разных вкладок, поэтому идемпотентен; отдельно закреплено, что второе юрлицо не заводится, если головное сняли руками (ослабление роняет тест). Пустые реквизиты кладутся как NULL, а не пустой строкой: пустая строка в ИНН выглядит как «заполнено» и падает на печатной форме. Полное наименование не выдумывается из короткого. 19 проверок. Прогон: сервер 2407/357, типы чисты. |
| 2026-09-18 | Этап 6, шаг 6.1: две тенантные миграции — справочник `legal_entities` (§6.1) и колонка `legal_entity_id` NULLABLE в 13 таблицах (§6.2), плюс модель `LegalEntity` и её регистрация. ГЛАВНАЯ НАХОДКА: ПЯТЬ имён таблиц в ТЗ не совпадают со схемой (`sale_invoices` → `sales_invoices`, `sale_estimates` → `sales_estimates`, `sale_receipts` → `sales_receipts`, `expenses` → `expenses_transactions`, `dividends` → `dividend_payouts`, `payment_calendar_entries` → `planned_operations`). Опечатка тут НЕ ПАДАЕТ: миграция защищена `hasTable` и молча пропустит таблицу, разрез по юрлицу в ней не появится никогда. Сторож `legalEntityColumns.spec.ts` сверяет список с настоящими `tableName` моделей; подстановка имени из ТЗ роняет его. УРОК: первая версия сторожа искала модели только в `*.model.ts` и дала ЛОЖНУЮ тревогу на пяти живых таблицах — соблазн был выкинуть их из миграции; расширил сторожа, и он нашёл одну НАСТОЯЩУЮ ошибку. МИГРАЦИИ НЕ ЗАПУСКАЛИСЬ НИ НА ОДНОЙ БАЗЕ: ТЗ требует бэкап и прогон на стенде — это решение владельца. Прогон: сервер 2388/355, типы чисты. |
| 2026-09-18 | Этап 9 ЗАКРЫТ ПО КОДУ, шаг 9.4 — приёмка. Проверял по коду: экран, ручку и свёртку статей. Закрыто всё, что просило ТЗ, кроме разрезов. ОСТАТКИ Р1–Р4: период и разрезы на экране не выбираются (сервер их принимает, тест следит), юрлицо ждёт этапа 6. ГЛАВНЫЙ ОСТАТОК Р4: расходы по счетам, НЕ ПРИВЯЗАННЫМ НИ К ОДНОЙ СТАТЬЕ, не попадают в анализ вообще — `foldAccountsIntoArticles` пропускает их молча. Это не то же самое, что «Не размечено» (статья без пометки видна), и опаснее: итог просто меньше, чем в ОПиУ, и ничто на это не указывает. Лечение — строка «расходы вне статей» в свёртке, затронет и другие экраны, поэтому отдельным шагом. Живой пробы не было. Прогон: витрина 1072/140, сервер 2383/354. Дальше — этап 6. |
| 2026-09-18 | Этап 9, шаг 9.3: в форме статьи обязательный выбор «Постоянный или переменный» с пояснением дословно из ТЗ. Правило УСЛОВНОЕ: обязательно у расходов, запрещено у доходов (у выручки постоянных и переменных не бывает) — и стоит С ОБЕИХ СТОРОН: форма требует выбор и прячет поле у доходной статьи, сервер отвергает пометку у дохода с внятной ошибкой. Иначе через ручку API импортом заехала бы бессмыслица и молча подмешалась в постоянные затраты. НАМЕРЕННАЯ АСИММЕТРИЯ: сервер НЕ требует пометку у расхода — в справочнике уже лежат старые статьи без неё, запрет заблокировал бы правку названия у половины справочника. Проверок: 6 на витрине (ошибка у самого поля, обход «не отправлять поле» закрыт), 5 на сервере; отключение обязательности роняет 3 из 6. Попутно покраснел старый тест создания статьи — в его самодельной пустышке не было нового правила, дополнил. Прогон: витрина 1072/140, сервер 2383/354, словари 4357/4357. |
| 2026-09-18 | Этап 9, шаг 9.2: экран «Анализ расходов» (`/expenses-analysis`) и пункт меню в группе «Отчёты». Блоки: постоянные и переменные с долями (третья строка «Не размечено» — только когда в ней что-то есть), точка безубыточности и запас прочности, линия доли расходов в выручке по месяцам, топ статей с подсветкой резкого роста. ГЛАВНОЕ: предупреждение стоит НАД цифрами — расчёт по половине расходов выглядит так же уверенно, как правильный. «Постоянных нет вовсе» важнее, чем «часть не размечена», порядок закреплён тестом. Неприменимая метрика — прочерк, не ноль. Месяц без выручки рвёт линию, а не опускает её в ноль. Ничего не пересчитывается на витрине. 14 проверок. Прогон: витрина 1066/139, словари 4354/4354, сборка зелёная. |
| 2026-09-18 | Этап 9, шаг 9.1: серверная ручка анализа расходов `GET /financial-model/expenses-analysis`. Разбор подтвердил обещание ТЗ — заготовки есть: `costBehavior` у статьи, свёртка `ArticlesPlRollupService`, `computeDealMargin`, `GetBreakEvenService`, `enumerateMonths`. ГЛАВНОЕ РЕШЕНИЕ: корзины ТРИ, а не две — постоянные, переменные и НЕ РАЗМЕЧЕННЫЕ. Поле в базе допускает пустоту; молча отнести пустые к переменным значит занизить постоянные затраты и показать безубыточность ближе, чем она есть. Экран отдельно сообщает, сколько статей ждут разметки. Прошлый период — той же длины и встык (на конкретных датах: март 1–31 это 31 день, прошлый — 29.01…28.02). Новая статья подсвечивается без доли роста. Порог — БОЛЕЕ чем 20%. Ничего не считается заново: выручка из той же свёртки, безубыточность из того же сервиса. Проверок 19 + 6, мутация правила пустого поля роняет тест. Прогон: сервер 2378/353, типы чисты. |
| 2026-09-18 | Этап 4 ЗАКРЫТ ПО КОДУ, шаг 4.7 — приёмка. Проверял по коду, а не по памяти: шапки трёх отчётов, панели действий, тела таблиц. Закрыто: вход тремя карточками, период и разрез во всех трёх, графики ОПиУ и ДДС, раскрытие суммы в ОПиУ, экспорт Excel/PDF и печать во всех трёх, переключатель метода, план-факт. ОСТАТКИ (О1–О6 в строке этапа): главный — раскрытие суммы есть только в ОПиУ, и это не лень: там строка = ОБОРОТ за период, а в ДДС и Балансе = ОСТАТОК на дату, сумма проводок периода с ним не сойдётся; нужен расчёт «остаток на начало + движения». Фильтр юрлица ждёт этапа 6 (таблицы `legal_entities` нет), фильтр проекта — раздел без серверной части. ЖИВОЙ ПРОБЫ НЕ БЫЛО: стенд витрины недоступен, всё проверено прогонами и сборкой. Прогон: витрина 1052/138, сервер 2353/351, словари 4332/4332. Дальше — этап 9 «Анализ расходов». |
| 2026-09-18 | Этап 4, шаг 4.6: план-факт в ДДС — СВОДКОЙ над таблицей, а не колонками. Причина принципиальная: отчёт о движении денег построен косвенным методом, его строки — балансовые счета (дебиторка, запасы, кредиторка), а бюджет разложен по статьям доходов и расходов; пересечения нет, приписать план строке «Запасы» не к чему. Сводка показывает «Поступления» и «Выплаты» с планом, фактом и отклонением. Факт для ДДС — только ОПЛАЧЕННОЕ (правило `cashSettledReferenceKeys` взято из бюджета, не переписано), переводы между своими счетами не считаются; отключение отбора роняет 2 теста. Раскраска по смыслу стороны, а не по знаку: перерасход не бывает зелёным. ПОЧИНЕН ШАГ 4.5: тип бюджета был написан `bdr` вместо `bdir` — колонки в ОПиУ не появились бы никогда, заглушки в тестах это скрыли; добавлена сверка с общим списком `BUDGET_TYPES`. Проверок: +5 на сервере, +10 на витрине. Прогон: витрина 1052/138, сервер 2353/351, словари 4332/4332, сборка зелёная. |
| 2026-09-18 | Этап 4, шаг 4.5: в ОПиУ появились колонки «План» и «Отклонение» — если по периоду заведён бюджет; нет бюджета, нет колонок. Главная трудность не в арифметике: план заводится по СТАТЬЯМ, а строки отчёта — СЧЕТА, и одна статья может собирать несколько счетов. Правило: план встаёт в строку счёта только когда счёт у статьи единственный, иначе виден лишь в итоге по виду; строка без плана получает прочерк, а не ноль. План не считается дважды (план на группе отменяет планы внутри неё). Правило знака вынесено в общий `reportAccountNet.ts` — им теперь пользуются и раскрытие суммы (4.3), и отклонение; подмена роняет 3 теста. Итог сравнивает план и факт одних и тех же статей. Проверок: 11 на сервере, 9 на витрине. НЕ ЗАКРЫТО: ДДС (шаг 4.6), кассовый метод, бюджет берётся по году начала периода. Прогон: витрина 1042/137, сервер 2348/351, словари 4328/4328, сборка зелёная. |
| 2026-09-18 | Этап 4, шаг 4.4: в шапке ОПиУ переключатель «Кассовый / По начислению» и пояснение в одну строку, чем методы отличаются. Считать двумя способами сервер умел и раньше, но переключатель лежал в панели «Настроить отчёт» — человек не знал ни что методов два, ни каким посчитаны цифры перед ним. Правило выбора метода вынесено в `accountingBasis.ts` и повторяет серверное (`ProfitLossSheet/utils.ts`); 8 проверок сделаны зеркалом серверной спеки, чтобы подпись не разошлась с движком, подмена умолчания роняет 2 из 8. Сторож `moduleDisabledSpeaks` поймал правку (экран за флагом не должен гаснуть) — переключатель не раздел, а часть работающего отчёта, внесён в список осознанных исключений с обоснованием. Прогон: витрина 1033/136, типы чисты, словари 4326/4326, сборка зелёная. |
| 2026-09-18 | Этап 4, шаг 4.3: клик по статье в ОПиУ открывает справа панель с операциями, из которых сложилась сумма, и «Итого по строке». ТЗ зовёт этот пункт ключевым: без него человек не верит отчёту. Считает сервер, ручка `GET /financial-reports/chart/drill-down`; главное правило — знак суммы (кредитовый счёт: кредит минус дебет, иначе наоборот), подмена его на «дебет плюс кредит» роняет 2 теста из 6. Итог панели — сумма её же строк, не отдельный запрос. Не больше 200 операций за раз. Раскрываются только строки-счета: у итогов своих проводок нет. ДДС и Баланс — отдельно, у них другой источник строк. Прогон: витрина 1025/135, сервер 2337/350, словари 4324/4324, сборка зелёная. |
| 2026-09-18 | Этап 4, шаг 4.2: над таблицей ОПиУ и ДДС появился график по месяцам (выручка и прибыль / поступления и выплаты). Ряды считает сервер отдельной лёгкой ручкой `GET /financial-reports/chart` — из узлов того же отчёта и из движения по денежным счетам, чтобы картинка и таблица показывали одни числа. 5 тестов, включая сторону дебет/кредит и ограничение на 36 столбцов. Баланс без графика: там нужна структура активов, а не ряды. Прогон: витрина 1025/135, сервер 2331/349. |
| 2026-09-18 | Этап 4, шаг 4.1: раздел отчётов открывается тремя большими карточками (Деньги, Прибыль, Баланс) с подписями «зачем этот отчёт», остальные — свёрнутым списком. Главные отчёты не дублируются в списке ниже, опустевшая группа не показывается (5 тестов). Прогон: витрина 1025/135, словари 4317/4317. |
| 2026-09-18 | Этап 2, шаги 2.4–2.5: блок «Требует внимания» (ждут разноски, кассовый разрыв, просроченная дебиторка, заявки на оплату) — только непустые строки, каждая ведёт к действию, сбой источника главную не роняет. Сторож зависимостей расширен на новую ручку. Приёмка этапа: не закрыты строка о превышении бюджета (нагрузка на показ) и замер скорости с Redis-кешем. Прогон: витрина 1020/134, сервер 2326/348. |
| 2026-09-18 | Этап 2, шаг 2.3: на главной появились полоса показателей, график «Деньги по месяцам», остатки по счетам, топ статей расходов и переключатель периода (запоминается вид, а не даты). Пустая организация видит приглашение подключить счёт. Попутно: в `DashboardSummary` семь строк были вписаны по-русски в разметку — переведены на словарь. Прогон: витрина 1020/134, словари 4305/4305. |
| 2026-09-18 | Этап 2, шаг 2.2: заведена ручка `GET /dashboard/overview` — всё для главной одним ответом (плитки с изменением к прошлому периоду, 12 месяцев для графика, остатки по счетам, топ-5 статей расходов). Доходы, расходы и прибыль берутся из узлов отчёта ОПиУ — второго способа считать те же суммы нет. Тест на 7 проверок, проверен мутацией. Прогон: сервер 2321/348. |
| 2026-09-18 | Этап 1 «Навигация»: меню перестроено с 14 пунктов до 8 (подгруппы «Новые задачи» убраны — панель формы создания и так отбрасывает), глубина 2 уровня, ни один адрес не потерян (сторож `navigationReachability` прошёл без правок). Заведён флаг `documents`, выключенный по умолчанию: первичка уходит под него девятым пунктом. Режим «Бизнес» прячет 13 адресов вместо 6. Прогон: витрина 1013/133, сервер 2314/347, словари 4282/4282. |
| 2026-09-18 | Этап 3, шаг 3.7 и приёмка: сторож телефона расширен на примитивы дизайн-системы (раньше экраны на `DataTable` не проверялись вовсе), в шапку добавлены «Импорт выписки» и «Экспорт», заведён отбор по сумме. Починен мигающий тест двухфакторного входа (bcrypt не укладывался в 5 с). Таблица приёмки — в плане этапа: не закрыты отбор по контрагенту и статье, проект и юрлицо (зависят от чужих этапов). Прогон: витрина 1006/133, сервер 2313/346, словари 4271/4271. |
| 2026-09-18 | Этап 3, шаг 3.6: массовые действия — выделение строк в режиме разноски, «назначить статью выделенным» одним запросом и «исключить из учёта». Смешанное выделение (приход + расход) разом не разносится, вместо списка статей выводится объяснение. Прогон: витрина 1005/133, сервер 2313/346, словари 4268/4268. |
| 2026-09-18 | Этап 3, шаг 3.5: разнос одним движением — статья выбирается прямо в строке, сторона операции определяет список статей и тип, подсказка правила применяется одной кнопкой. Логика вынесена в `categorizeInline.ts` (7 тестов). Прогон: витрина 1001/133, сервер 2313/346, словари 4263/4263. |
| 2026-09-18 | PR #425 влит в `develop` (`1d2d0de8`): шаги 3.1–3.4 этапа 3. Вливание промежуточное — чтобы работа была видна и не копилась в ветке; оставшиеся шаги 3.5–3.7 пойдут следующим PR. |
| 2026-09-18 | Этап 3, шаг 3.4: полоса «N операций ждут разноски» на экране «Операции» + режим их показа с подсказкой от правила разноски. Сервер: ручка `GET /banking/uncategorized` — ждущие разноски по всем счетам (раньше только по одному), сторож на 3 проверки. Прогон: сервер 2313/346, витрина 991/132, словари 4259/4259. |
| 2026-09-18 | Этап 3, шаг 3.3: новый экран «Операции» (`/cashflow-accounts/transactions`) — список по всем счетам на новой дизайн-системе, период (по умолчанию текущий месяц), отборы по счёту, направлению денег и поиску, отборы в адресной строке, подгрузка по кнопке. Пункт меню добавлен в группу «Банк». Сервер дополнен счётом, контрагентом и назначением в строке. Прогон: витрина 988/132, сервер 2310/345, типы чисты, словари 4250/4250. |
| 2026-09-18 | Этап 3, шаг 3.2: серверный список операций по всем счетам — счёт стал необязательным, добавлены отборы (период, направление, контрагент, сумма, поиск), входящий остаток без счёта не считается. Сторож `allAccountsTransactions.spec.ts` (6 проверок, проверен мутацией). Попутно починена красная проверка типов сервера с `develop` (тест уборки демо передавал 7 доводов вместо 8). Прогон: сервер 2310/345, типы чисты, словари 4238/4238. |
| 2026-09-18 | Решения владельца: (1) сначала этап 3 «Экран операций», меню — после; (2) раздел «Проекты» оживлять, а не убирать из меню; (3) блок «Документы» выключить всем строго по ТЗ. Очередь исполнения изменена: 3 → 1 → 2 → 4 → 9 → … |
| 2026-09-18 | Этап 1, шаг 1.1: разобрано текущее меню (1001 строка, 117 записей, 14 пунктов), составлено сопоставление в 8 пунктов, сверены 27 адресов целевого меню с таблицей маршрутов — четырёх не существует. План: `docs/superpowers/plans/2026-09-18-tz-01-navigation.md`. Кода не трогали. |
| 2026-09-18 | Стабилизация, источник №5 (долг по `@ts-nocheck`): слой «семь замечаний» — слепая зона 134 → 102 файла, замечаний 1810 → 1571, хуже никому. 20 настоящих поломок, из них поле «Ссылка» в формах входящего платежа и кредит-ноты не сохранялось никогда. Два сторожа. Карта `docs/superpowers/specs/2026-09-17-roadmap-actualization-v88.md`. |
| 2026-09-18 | ТЗ заведено в репозиторий: `MASTER.md`, этот файл состояния, алгоритм `CONTINUE.md`, команда `/tz`, точка входа в `CLAUDE.md`/`AGENTS.md`. Работы по этапам не начинались. |
