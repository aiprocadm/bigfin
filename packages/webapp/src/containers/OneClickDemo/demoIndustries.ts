/**
 * Отрасли демо-организации (FIN-027 ТЗ-2).
 *
 * ПОЧЕМУ СПИСОК ЖИВЁТ ОТДЕЛЬНО ОТ ЭКРАНА. Тот же набор нужен мастеру
 * первичной настройки: вопрос «чем вы занимаетесь?» задаётся там теми же
 * словами и теми же вариантами. Две копии списка однажды разойдутся, и
 * человек получит в мастере один набор, а на странице демо — другой.
 *
 * ПОРЯДОК НЕ СЛУЧАЕН: услуги первыми, потому что это самый частый случай
 * российского малого дела, и первый вариант выбирают чаще остальных.
 */
export type DemoIndustry = 'services' | 'trade' | 'projects';

export const DEMO_INDUSTRIES: DemoIndustry[] = ['services', 'trade', 'projects'];

/** Отрасль по умолчанию. */
export const DEFAULT_DEMO_INDUSTRY: DemoIndustry = 'services';
