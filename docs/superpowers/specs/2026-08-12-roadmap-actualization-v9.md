# Bigfin — Дорожная карта v9 (после фазы «Кто что может») — 2026-08-12

Предыдущие карты: v6 «Достоверность учёта» (закрыта), v7 «Надёжность записи»
(закрыта, кроме автопроверок), v8 «Кто что может» (закрыта целиком).

---

## 1. Приёмка фазы v8 на свежей организации

Фаза проверялась не по коду, а живьём: заведена **новая организация** (как у
настоящего новичка — регистрация, подтверждение почты, создание организации),
в неё приглашён человек с ролью «Сотрудник», и за него прожит обычный рабочий
день.

**Часть первая — работа должна идти.**

| Действие сотрудника | Итог |
|---|---|
| открыть список покупателей | ✅ 200 |
| завести покупателя | ✅ 201 |
| получить подсказку контрагентов | ✅ 200 |
| завести позицию (услугу) | ✅ 201 |
| выписать счёт покупателю на 20 000 ₽ | ✅ 201 |
| принять оплату по счёту | ✅ 201 |
| увидеть свои счета | ✅ 200 |

**Часть вторая — чего роль не давала, должно быть закрыто.** Шестнадцать
действий: банковские операции, правила разбора выписки, склады и их включение,
валюты, статьи учёта, категории товаров, реквизиты организации, шаблоны печати,
импорт, бюджеты, плановые операции, роли, закрытие периода, подписка на
продукт, эквайринг.

**Все шестнадцать отклонены.** Отдельно проверено, что отказ идёт **по праву**,
а не потому, что модуль выключен: владелец включил бюджеты и платёжный
календарь, после чего сотрудник получил «нет права», а не «модуль выключен».
Владелец на тех же ручках доходит до дела.

**Вывод: фаза v8 подтверждена на чистых данных. Новых дефектов приёмка не
нашла.**

---

## 2. Что дала разведка этой итерации

Шаг П1 закрыл **запись**: из 305 записывающих ручек каждая либо спрашивает
право, либо объявлена открытой с причиной, либо имеет собственную проверку.

Следующий естественный вопрос — **чтение**. Роль «Сотрудник» задумана как
повседневная работа с документами, а не как доступ ко всей финансовой картине
организации. Проверка живьём за приглашённого сотрудника:

| Отчёт | Видит ли сотрудник |
|---|---|
| Баланс | 🔒 закрыт |
| Прибыли и убытки (ОПиУ) | 🔒 закрыт |
| Движение денег | 🔒 закрыт |
| Главная книга | 🔒 закрыт |
| Журнал операций | 🔒 закрыт |
| Дебиторка и кредиторка по срокам | 🔒 закрыты |
| **Оборотно-сальдовая ведомость** | 👀 **видит** |
| **Остатки по покупателям и поставщикам** | 👀 **видит** |
| **Обороты по покупателям и поставщикам** | 👀 **видит** |
| **Продажи по позициям** | 👀 **видит** |
| **Оценка склада** | 👀 **видит** |

**Семь отчётов из шестнадцати открыты.** Разметка отчётов сделана меньше чем
наполовину: из восемнадцати отчётных контроллеров право спрашивают семь.

Показательнее всего пара «Баланс закрыт — оборотно-сальдовая ведомость
открыта»: ОСВ показывает обороты и остатки **по всем счетам организации**, то
есть ту же картину, только подробнее. Закрытая парадная дверь при открытой
задней.

---

## 3. Долг, вскрытый этой разведкой (новое в v9)

Порядок — по риску.

| № | Что | Почему важно | Объём |
|---|---|---|---|
| **В1** ✅ | **Половина отчётов не спрашивает права** | Приглашённый человек видит оборотно-сальдовую ведомость, обороты по всем контрагентам и оценку склада — всю финансовую картину, хотя роль давала только свои документы | **Закрыто (#230):** размечены одиннадцать отчётов, роль «Сотрудник» получила четыре отчёта по контрагентам |
| **В2** ✅ | **Действие без проверки, что документ есть** | Письмо по счёту и напоминание о долге по **несуществующему** документу отвечают «принято» (200/201) и уходят в очередь. Пользователь считает, что письмо отправлено | **Закрыто (#231):** пройден весь класс — девять цепочек разобраны и перепроверены, пять починены; попутно найдено и починено «переотправка приглашения не шлёт письмо вовсе» |
| **В3** ✅ | **Подсказка контрагентов не отбирает по праву** | Роль, которой доверены только поставщики, видит в подсказке и покупателей с их долгами. Сейчас лучше прежнего (было — видно всем), но не точно | **Закрыто (#232):** выдача сужается до доверенной стороны по тем же правам, что и списки |
| **В4** ✅ | **Оплата счёта по ссылке лежит за входом** | Ссылка задумана для покупателя снаружи, но ручка требует входа в систему — значит поток нерабочий либо задуман иначе | **Решение владельца: поток нужен. Закрыто (#233):** ручки публичные, организация ищется по самой ссылке |
| **В5** | **Автопроверки на GitHub не работают** | Хвост v7: восемь сторожей фазы v8 и все прежние тесты гоняются только вручную. Упирается в оплату аккаунта | Владельцу: Billing & plans |
| **В6** ✅ | **Роль всего одна** | «Сотрудник» — единственная урезанная роль. Механизм своих ролей есть, но в интерфейсе это редкий сценарий | **Решено (поручено исполнителю): оставить редким сценарием.** Вход существует и работает: Настройки → Пользователи → вкладка «Роли» + кнопка создания. Отдельный пункт меню добавил бы сложности всем ради редкой задачи — против духа продукта. Сервер закрыт стражем «только владелец» (#219). Кода не требуется |

---

## 4. Предлагаемая фаза: «Кто что видит»

Смысл фазы — чтобы **приглашённый человек видел ровно то, что ему показали**.
Прямое продолжение: v6 — верить цифрам, v7 — верить факту сохранения, v8 —
верить границам записи, v9 — верить границам показа.

**Рекомендованный порядок:**

1. **В1 — отчёты.** ✅ **Закрыто (#230).** Размечены все одиннадцать отчётов,
   у которых пометки не было: оборотно-сальдовая ведомость, остатки и обороты
   по покупателям и поставщикам, операции по документу, продажи и закупки по
   позициям, оценка склада, движение позиций склада, налог с продаж.

   Два права спрашивались кодом, но **в схеме ролей их не было** — поставить
   галочку было негде, поэтому отчёты и стояли открытыми: «оборотно-сальдовая
   ведомость» и «налог с продаж». Оба добавлены в схему вместе с переводами.

   **Решение по вопросу 11.** Роли «Сотрудник» оставлены отчёты **по
   контрагентам** — остатки и обороты по покупателям и поставщикам: с кем
   сколько наработали и кто сколько должен, это продолжение работы со счетами
   и оплатами. Общая финансовая картина организации остаётся администратору.
   Перенос `20260812160000_staff_can_read_contact_reports` (прогон на живой
   базе up→down→up).

   | Проба на свежей организации | До правки | После правки |
   |---|---|---|
   | сотруднику видно отчётов | **7 из 16**, включая ОСВ, продажи по позициям и оценку склада | **4 из 16** — только по контрагентам |
   | оборотно-сальдовая ведомость у сотрудника | 👀 видел | **403** |
   | владельцу видно | всё | **всё, без изменений** |

   Сторож `Roles/reportsAccessGates.spec.ts` следит за тремя вещами сразу:
   у каждого отчёта стоит пометка и страж; каждое требуемое право объявлено
   в схеме ролей (иначе владелец не сможет его выдать); **ни один отчётный
   контроллер не остался без пометки** — обход всей папки, а не список.
2. **В2 — действие без проверки существования.** ✅ **Закрыто (#231).**
   Пройден весь класс: девять цепочек «ручка → служба → очередь» разобраны
   по отдельности, каждый вывод перепроверен встречным чтением кода.

   **Сломаны были пять** — письма по счёту, чеку, смете и оплате плюс
   напоминание о долге: команда сразу ставила задачу в очередь, а документ
   загружался только внутри неё, где ошибку клиент уже не видит. Лечение
   одно на всех: загрузка документа с «не найдено» **до** постановки в
   очередь. Напоминание о долге шлётся через ту же команду, что письмо по
   счёту, поэтому починка одной точки закрыла две ручки.

   **Целые две**: переотправка приглашения проверяет пользователя первой
   операцией, а восстановление пароля отвечает «успех» для любой почты
   **намеренно** — иначе перебором адресов можно выяснять, кто
   зарегистрирован. Это не дефект, и чинить его на 404 нельзя.

   **Попутная находка серьёзнее самой темы: переотправка приглашения не
   слала письмо вовсе.** Ветка переотправки заводила новый токен, но не
   эмитила событие, которое слушает почтовый подписчик, — владелец жал
   «переотправить», получал успех, письмо не уходило. Починено: событие
   эмитится после вставки токена; вживую очередь писем выросла на одну
   джобу после переотправки (до правки — не росла).

   | Проба на стенде | До правки | После правки |
   |---|---|---|
   | письма по счёту/чеку/смете/оплате-«призраку» | **200 «отправлено»** | **404** |
   | напоминание о долге по счёту-«призраку» | **201 {queued: true}** | **404** |
   | письмо и напоминание по настоящему счёту | 200 / 201 | **без изменений** |
   | переотправка приглашения | успех, письмо не в очереди | успех, **письмо в очереди (+1)** |

   Известное ограничение (унаследованное, не новая регрессия): если у
   переотправляющего нет тенантной записи (рассинхронизированная база),
   письмо соберётся без отправителя и джоба упадёт — ровно так же ведёт
   себя первичное приглашение.

   **Находка владельцу: SMS по смете — ручка-пустышка.** `POST
   /sale-estimates/:id/notify-sms` отвечает 201, но вся служба SMS
   закомментирована целиком — ручка не делает ничего. Решение продуктовое
   (вернуть SMS или убрать ручку), поэтому код не тронут.
3. **В3 — отбор в подсказке.** ✅ **Закрыто (#232).** Пометка «любое из прав»
   решает, пустить ли человека к подсказке вовсе, а выдача теперь сужается до
   доверенной стороны: право на покупателей — видны покупатели, на
   поставщиков — поставщики, оба — все. Роль без обоих прав получает пустой
   список, а не «всех подряд».

   | Проба на демо-организации | До правки | После правки |
   |---|---|---|
   | роль «только поставщики» | 6 контрагентов, **из них 5 покупателей с долгами** | **1 поставщик** |
   | сотрудник (обе стороны доверены) | 6 | **6** |
   | владелец | 6 | **6** |

   Попутно у стенда применены штатные тенантные переносы (`tenants:migrate:latest`)
   — влитые в develop переносы прав до этого никто не применял, и роль
   «Сотрудник» на демо-организации стояла без справочников.
4. **В4 — внешний поток оплаты по ссылке.** ✅ **Закрыто (#233), по решению
   владельца «поток нужен».** Контроллер платёжных ссылок помечен публичным —
   как вебхуки: все стражи входа и организации уважают эту пометку.
   Организация ищется не по заголовку, а по самой ссылке (случайный
   идентификатор, который выдал владелец счёта); служба создания сессии
   оплаты научилась настраивать тенантное подключение из ссылки — раньше это
   умели только две из трёх служб потока.

   | Проба без входа (ни токена, ни организации) | До | После |
   |---|---|---|
   | метаданные счёта по ссылке | **401** | **200** — сумма, позиции, покупатель |
   | сессия оплаты | **401 (наш вход)** | доходит до Stripe (на стенде нет ключей — ошибка самого Stripe; аутентификация пройдена) |
   | PDF счёта | **401** | 500 — **рендер PDF на стенде не настроен вовсе** (под владельцем тот же 500); к публичности не относится |
   | выдуманная ссылка | 401 | **404** |

5. **В6 — свои роли в интерфейсе.** ✅ Решено: оставить редким сценарием
   (подробности в таблице долга выше).

**Что НЕ делать сейчас**: тяжёлые банковские интеграции, нативный клиент,
консолидацию мультиюрлица (без изменений с v7 и v8).

---

## 5. Открытые вопросы владельцу

Из одиннадцати вопросов карты v8 закрыт один (№10 — справочники, взят мягкий
вариант). Остаются:

1. **Графический ребрендинг** — когда?
2. **ЭДО (Контур/Диадок)** — после первых клиентов с потребностью?
3. **Метод начисления** — нужен ли ручной перенос месяца отдельной операции?
4. **Telegram в РФ** — нужен ли резервный канал (VK Teams / MAX)?
5. **Анализ НДС / МойСклад** — развивать или «по запросу пользователей»?
6. **SMS/WhatsApp-напоминания дебиторам** — нужен провайдер.
7. **Перепроведение исторических документов с НДС** — массово одной кнопкой
   или по мере открытия документов? Инструмент готов (#194).
8. **Невозмещаемый НДС** — куда относить по умолчанию (сейчас в стоимость).
9. **Включать ли CI обратно** — упирается в оплату аккаунта GitHub.
10. ~~**Роли гибче двух**~~ **РЕШЕНО (В6, поручено исполнителю): оставить
    редким сценарием** — вход через Настройки → Пользователи → вкладка «Роли»
    существует и работает, отдельный пункт меню не добавляется.
11. ~~**НОВОЕ. Что должен видеть сотрудник из отчётов?**~~ **РЕШЕНО в шаге В1
    (#230): по предложенному варианту** — остатки и обороты по покупателям и
    поставщикам да; Баланс, ОПиУ, ДДС, главная книга, журнал, ОСВ, продажи и
    закупки по позициям, склад и налог — нет. Если владелец захочет иначе, это
    четыре строки в заготовке роли и перенос.
12. ~~**НОВОЕ. Нужен ли внешний поток оплаты счёта по ссылке?**~~ **ОТВЕЧЕНО
    владельцем: нужен. Закрыто в В4 (#233).** Для живой оплаты остаётся
    настроить ключи Stripe на проде; рендер PDF на стенде не настроен вовсе
    (падает и под владельцем) — отдельная стендовая задача.

---

## 6. Что дальше

Фаза v9 «Кто что видит» предлагается к исполнению в порядке В1 → В2 → В3.

Единственный хвост, который не двигается силами разработки, — **автопроверки
на GitHub**: со стороны репозитория всё готово, мешает ответ GitHub про оплату
аккаунта. **Владельцу: GitHub → Settings → Billing & plans.**
