/**
 * Виды построителей запросов, общие для отчётов.
 */

/**
 * Построитель, которому можно дописать именованное условие.
 *
 * ЗАЧЕМ ОТДЕЛЬНЫЙ ВИД. Помощники отбора в отчётах объявляли довод как
 * построитель Knex, а приходит к ним построитель Objection — тот, что даёт
 * `model.query().onBuild((query) => …)`. Это разные вещи: у Knex `modify`
 * принимает функцию, у Objection — ИМЯ готового условия строкой, и именно
 * так их и зовут («filterByBranches»).
 *
 * Описываем не конкретный построитель, а то единственное, что помощнику от
 * него нужно. Так подпись перестаёт врать и подходит обоим: помощник и правда
 * не делает с запросом ничего, кроме как дописывает условие по имени.
 */
export interface INamedModifiableQuery {
  modify(name: string, ...args: any[]): unknown;
}
