# ТЗ Bigfin — состояние работ

<!--
  ЭТОТ ФАЙЛ — ЕДИНСТВЕННЫЙ ИСТОЧНИК ПРАВДЫ О ТОМ, ГДЕ МЫ СЕЙЧАС ПО ТЗ.

  Правила ведения:
  1. Файл ПЕРЕЗАПИСЫВАЕТСЯ, а не накапливается. История — в разделе «Журнал»
     внизу и в картах `docs/superpowers/specs/*-roadmap-actualization-vNN.md`.
  2. Обновляется в конце КАЖДОГО шага работы, до коммита.
  3. Блок «Указатель» правится всегда — даже если этап не сменился.
  4. Порядок работы с ним описан в `docs/tz/CONTINUE.md`.
-->

**Само ТЗ:** [MASTER.md](MASTER.md)
**Обновлено:** 2026-09-18
**Обновил:** Claude Code (этап 7, шаг 7.1 — признак внутригрупповой операции)
**База develop на момент обновления:** `d0b56c2a`

---

## 1. Указатель — что делать прямо сейчас

| Поле | Значение |
|---|---|
| **Режим** | `ТЗ` |
| **Текущий этап** | 7 «Консолидированная отчётность» в работе. Этапы 1, 2, 3, 4, 9 и этап 6 (кроме шага 6.5) ждут приёмки владельца |
| **Следующее действие** | Шаг 7.2 — автопометка переводов между счетами разных юрлиц и переключатель «Внутригрупповая операция» в ручной операции (§7.2 п. 1–2) |
| **Рабочая ветка** | `feat/tz-07-intercompany` — шаг 7.1 |
| **PR** | #425, #428–#451 влиты; шаг 7.1 — отдельным PR |
| **Блокеры** | Ю1: миграции этапа 6 и заполнение НИ РАЗУ НЕ ЗАПУСКАЛИСЬ ни на одной базе (нет локальной, общий стенд — решение владельца). Ю2: шаг 6.5 `legal_entity_id` → NOT NULL не начинать без явного согласия владельца, и только ПОСЛЕ Ю1 |
| **Ждём от владельца** | подтверждение удалений из карты v88; доступ к витрине стенда для накопившейся живой пробы |

---

## 2. Словарь статусов

| Статус | Что означает |
|---|---|
| `не начат` | Кода нет, ветки нет |
| `в работе` | Есть ветка, есть коммиты, PR ещё не открыт |
| `на ревью` | PR открыт, ждёт владельца или CI |
| `влит` | PR в `develop` |
| `принят` | Владелец проверил вживую и подтвердил приёмку из ТЗ |
| `заблокирован` | Нужно решение владельца или внешнее условие |

Этап считается **закрытым** только в статусе `принят`. `влит` ≠ `принят`.

---

## 3. Этапы

Порядок в таблице — номера ТЗ. Порядок исполнения — в разделе 4.

| № | Этап | Блок | Статус | Ветка / PR | Комментарий |
|---|---|---|---|---|---|
| 1 | Навигация | Б1 | на ревью | меню 14 → 8 пунктов, флаг `documents`, режим «Бизнес» прячет 13 адресов | шаг 1.1 сделан: 14 пунктов → 8, сверка адресов. Ждёт этапа 3 (пункт «Операции» вести некуда). Решения владельца: «Проекты» из меню НЕ убирать — оживлять; «Документы» выключить всем |
| 2 | Дашборд | Б1 | на ревью | #432, #433 влиты | все шаги сделаны; в заделе строка о превышении бюджета и замер скорости с кешем — таблица приёмки в плане |
| 3 | Экран операций | Б1 | на ревью | все шаги влиты; 3 пункта ТЗ зависят от чужих этапов — таблица приёмки в плане | ВЗЯТ ВПЕРЁД ЭТАПА 1 (решение владельца). 3.1–3.4 сделаны: сервер (счёт стал отбором и у проведённых, и у ждущих разноски), экран `/cashflow-accounts/transactions` с отборами в адресе, полоса «N ждут разноски» с режимом показа и подсказкой от правила. 3.5 разнос одним движением и 3.6 массовые действия влиты |
| 4 | Отчёты: каркас и три главных | Б1 | на ревью | #435–#440 влиты | все 7 шагов сделаны. Таблица приёмки — в плане этапа. ОСТАТКИ: О1 раскрытие суммы в ДДС и Балансе (там остатки, а не обороты — нужен другой расчёт), О2 график структуры активов в Балансе, О3 сравнение с прошлым периодом в ДДС, О4 кассовый метод в колонке «Отклонение», О5 фильтр юрлица (ждёт этапа 6), О6 фильтр проекта (раздел мёртв) |
| 5 | Дизайн-токены и состояния | Б1 | не начат | — | сквозной, по PR на экран |
| 6 | Модель данных юрлица | Б2 | на ревью | #446–#451 влиты | закрыт по коду КРОМЕ ломающего шага 6.5. Сделано: миграции (таблица + колонка nullable в 13 таблицах), юрлицо по умолчанию, заполнение пакетами, ручки и экран справочника. Таблица приёмки — в плане этапа. ОСТАТКИ: Ю1 миграции и заполнение НЕ ЗАПУСКАЛИСЬ на живой базе, Ю2 шаг NOT NULL ждёт согласия владельца, Ю3 часть полей юрлица не выведена в форму, Ю4 нет колонки «активно», Ю5 правило «не навязывать» ждёт колонок из этапа 7 |
| 7 | Консолидированная отчётность | Б2 | в работе | `feat/tz-07-intercompany` | 7.1 сделан: колонка `is_intercompany` у проводок + правила внутригрупповых оборотов. ГЛАВНОЕ ПРАВИЛО: НЕИЗВЕСТНОЕ юрлицо не считается «другим» — иначе внутригрупповым стал бы каждый перевод и отчёт молча потерял бы выручку. Дальше 7.2 пометка переводов, 7.3 фильтр юрлица, 7.4 исключение в ОПиУ и ДДС, 7.5 отчёт «Внутригрупповые обороты», 7.6 приёмка |
| 8 | Юрлицо в операциях, формах, правах | Б2 | не начат | — | — |
| 9 | Анализ расходов | Б3 | на ревью | #442–#445 влиты | все 4 шага сделаны. Таблица приёмки — в плане этапа. ОСТАТКИ: Р1 на экране нельзя выбрать период (ручка его принимает), Р2 нет разрезов по направлению и проекту на экране (сервер умеет), Р3 разрез по юрлицу ждёт этапа 6, Р4 РАСХОДЫ ПО СЧЕТАМ ВНЕ СТАТЕЙ НЕ ВИДНЫ ВООБЩЕ — свёртка их молча пропускает, итог меньше, чем в ОПиУ, и ничто на это не указывает |
| 10 | Разделение операции | Б3 | не начат | — | — |
| 11 | Капитализация | Б3 | не начат | — | `/capitalization` не существует |
| 12 | ИИ-разнос по истории | Б3 | не начат | — | ИИ-интеграций в проекте нет вовсе; решение ТЗ — локальный классификатор, без внешней модели |
| 13 | ИИ-аналитик | Б3 | не начат | — | — |
| 14 | ИИ-чат | Б3 | не начат | — | делать только после приёмки этапа 13 |
| П | Оживление раздела «Проекты» | вне ТЗ | не начат | — | Решение владельца 18.09: раздел оживлять (сервера нет вовсе — модель, ручки, права, рентабельность). Объём отдельной работой, в мастер-ТЗ не описан |
| 15 | Публичный API и вебхуки | Б3 | не начат | — | OpenAPI генерируется, Swagger UI не смонтирован |

---

## 4. Очередь исполнения

Из ЧАСТИ F ТЗ. Идти строго по ней, если владелец не сказал иначе:

```
1 → 2 → 3 → 4 → 9 → 6 → 7 → 8 → 5 → 10 → 12 → 11 → 15 → 13 → 14
    ^                 ^
    |                 |
    |                 этап 9 вставлен рано: дешёвый, заметный,
    |                 ни от чего не зависит
    этап 5 идёт поздно: экраны, переработанные в Б2,
    всё равно придётся причёсывать
```

**Текущая позиция в очереди:** этапы 1, 2, 3, 4, 9 закрыты по коду, этап 6 — кроме ломающего шага 6.5. Этап 7 «Консолидированная отчётность» в работе.

---

## 5. Режим «Стабилизация»

Включается, когда по ТЗ работы нет: все этапы `принят`, либо очередь упёрлась
в ожидание владельца (`заблокирован` / «ждём от владельца»), либо владелец
явно попросил стабилизацию.

Источники работы — **строго в этом порядке**, брать первый непустой:

| # | Источник | Как проверить |
|---|---|---|
| 1 | Красный CI на открытых PR | `gh pr list --state open` → `gh pr checks <N>` |
| 2 | Падающие тесты на `develop` | `pnpm --filter @bigfin/server test`, `pnpm --filter @bigfin/webapp test` |
| 3 | Ошибки типов | `pnpm typecheck` |
| 4 | Непарные переводы | `node packages/webapp/scripts/lang-check.js` |
| 5 | Долг по `@ts-nocheck` | линия карт v84–v87, ~148 файлов. **Внутри работ по ТЗ эти файлы не трогать** — но в режиме стабилизации это законная работа |
| 6 | Непокрытые сторожевые спеки | `navigationReachability`, `routePathsAreReal`, `sidebarHonoursMenuGuards`, `emptyStateTexts`, `moduleDisabledSpeaks`, `tableScrollOnPhone` — расширять, не отключать |
| 7 | Дыры в e2e | `e2e/`, Playwright: основной суточный сценарий из E3 ТЗ |
| 8 | Известные грабли | раздел «Known gotchas» в `CLAUDE.md` |

Результат стабилизации оформляется так же: одна тема = один PR, зелёный прогон,
запись в «Журнал» ниже.

---

## 6. Журнал

Только факты: дата, что закрыто, ссылка. Новые записи — сверху.

| Дата | Что |
|---|---|
| 2026-09-18 | Этап 7, шаг 7.1: колонка `is_intercompany` у проводок (миграция, по умолчанию false) и правила внутригрупповых оборотов (`LegalEntities/utils/intercompany.ts`, 13 тестов). Признак ХРАНИТСЯ, а не вычисляется: ТЗ даёт человеку переключатель в ручной операции, и его выбор должен пережить перезагрузку. ГЛАВНОЕ ПРАВИЛО: НЕИЗВЕСТНОЕ юрлицо НЕ считается «другим» — колонка юрлица пока пуста на всех базах (заполнение этапа 6 не запускалось), и трактовка пустоты как «другое» объявила бы внутригрупповым КАЖДЫЙ перевод, а отчёт молча потерял бы выручку. Из двух молчаливых ошибок выбрана заметная: «не исключили нужное» видно в отчёте, «исключили лишнее» — нет. Снятие защиты роняет тест. Плюс правило, когда исключать: все юрлица — да, одно — НЕТ (для него это настоящий доход), набор из нескольких — да. Прогон: сервер 2441/361. |
| 2026-09-18 | Этап 6 ЗАКРЫТ ПО КОДУ КРОМЕ ШАГА 6.5, шаг 6.6 — приёмка. Проверял по коду: миграции, модель, сервисы, ручки, экран. Закрыто: все 17 полей таблицы `legal_entities` в ТЕНАНТНОЙ схеме, колонка в 13 таблицах с индексами, юрлицо по умолчанию, заполнение пакетами, ручки с правами, экран с живой валидацией реквизитов. ОСТАТКИ Ю1–Ю5: Ю1 МИГРАЦИИ И ЗАПОЛНЕНИЕ НИ РАЗУ НЕ ЗАПУСКАЛИСЬ НИ НА ОДНОЙ БАЗЕ (порядок для владельца: бэкап → миграции → заполнение → проверка → и только потом NOT NULL); Ю2 шаг 6.5 NOT NULL ждёт согласия; Ю3 в форме нет полного наименования, НДС, валюты, директора, адресов, банка и переключателей «головное»/«действующее» (сервер их принимает, юрлицо по умолчанию заполняет из реквизитов, но изменить на экране нельзя); Ю4 нет колонки «активно» — сейчас это приглушённая строка; Ю5 правило «не навязывать» готово, но колонок «Юрлицо» в других разделах пока нет вовсе (этап 7). Живой пробы не было. Прогон: витрина 1094/141, сервер 2428/360. Дальше — этап 7. |
| 2026-09-18 | Этап 6, шаг 6.4б: экран `/legal-entities` и пункт «Юрлица» в «Справочниках». Таблица название·форма·ИНН·налоги·доля·счетов, форма RHF+Zod. ТРЕТЬЕЙ ВЕРСИИ ПРАВИЛ НЕ ПОЯВИЛОСЬ: проверки ИНН/КПП/ОГРН и списки форм/режимов переиспользованы из тех же мест, что реквизиты организации, — включая порядок подсказок (сначала длина, потом контрольная цифра). «Раздел не навязывается» разобрано: пункт меню есть ВСЕГДА (второе юрлицо надо откуда-то завести), а навязывание — это колонки и отборы в ДРУГИХ разделах; правило готово (`shouldShowLegalEntityBreakdown`, считает ДЕЙСТВУЮЩИЕ), сами колонки — этап 7. Кнопка удаления прячется там, где сервер откажет. Доля владельца 0..100, запятая принимается (поле текстовое: браузер в числовом её выбрасывает). 22 проверки. Прогон: витрина 1094/141, словари 4380/4380. |
| 2026-09-18 | Этап 6, шаг 6.4а: ручки справочника юрлиц — список со счётчиком закреплённых счетов, создание, правка, удаление; валидаторы ИНН/КПП/ОГРН переиспользованы из `RussianLegalAttributes`. Правила: список никогда не пуст (лениво создаётся юрлицо по умолчанию); юрлицо С ОПЕРАЦИЯМИ удалить нельзя; ПОСЛЕДНЕЕ удалить нельзя; головное одно — признак снимается с остальных ТОЛЬКО когда его явно поставили (ослабление роняет тест). ДВА СТОРОЖА ПОЙМАЛИ НАСТОЯЩУЮ ДЫРУ В ПРАВАХ: (1) `writeEndpointsCoverage` — ручки записи не объявляли нужное право, удалить юрлицо мог бы любой вошедший; (2) `accessGates` — пометка права БЕЗ стража `PermissionGuard` это «мнимая защита». Оба исправлены. 10 проверок. Прогон: сервер 2428/360, типы чисты. |
| 2026-09-18 | Этап 6, шаг 6.3: фоновая задача заполняет `legal_entity_id` у существующих строк пакетами по 10 000 во всех 13 таблицах. ДВА ПРАВИЛА ВАЖНЕЕ СКОРОСТИ: (1) трогаем ТОЛЬКО пустые строки — строке могли назначить юрлицо руками, и заполнение не должно переписать выбор; условие стоит ДВАЖДЫ, при чтении пакета и при записи (между ними есть окно), снятие второго роняет 2 теста; (2) пакетами — один UPDATE на сотни тысяч проводок держит блокировку и подвешивает продукт, поэтому отдельная задача, а не часть миграции. Из первого правила бесплатно следует возобновляемость. НОВЫЙ СТОРОЖ `backfillTables.spec.ts`: список таблиц живёт в ДВУХ местах (миграция и заполнение) и обязан совпадать — расхождение не падает, таблица просто останется пустой навсегда. Отсутствующая таблица пропускается; есть потолок пакетов от вечного цикла. ЗАДАЧА НИ РАЗУ НЕ ЗАПУСКАЛАСЬ: базы для прогона нет. 11 проверок. Прогон: сервер 2418/359. |
| 2026-09-18 | Этап 6, шаг 6.2: юрлицо по умолчанию из реквизитов организации — головное, доля 100%. ДВЕ СХЕМЫ В ОДНОМ ШАГЕ: реквизиты лежат в СИСТЕМНОЙ `tenants_metadata`, справочник юрлиц — в ТЕНАНТНОЙ; чтение и запись разведены явно. Сидер СДЕЛАН ЛЕНИВЫМ (создаётся при первом обращении к справочнику), а не разовым прогоном: разовый скрипт надо не забыть повторить для тех, кто заведётся завтра. Цена — вызывается часто и из разных вкладок, поэтому идемпотентен; отдельно закреплено, что второе юрлицо не заводится, если головное сняли руками (ослабление роняет тест). Пустые реквизиты кладутся как NULL, а не пустой строкой: пустая строка в ИНН выглядит как «заполнено» и падает на печатной форме. Полное наименование не выдумывается из короткого. 19 проверок. Прогон: сервер 2407/357, типы чисты. |
| 2026-09-18 | Этап 6, шаг 6.1: две тенантные миграции — справочник `legal_entities` (§6.1) и колонка `legal_entity_id` NULLABLE в 13 таблицах (§6.2), плюс модель `LegalEntity` и её регистрация. ГЛАВНАЯ НАХОДКА: ПЯТЬ имён таблиц в ТЗ не совпадают со схемой (`sale_invoices` → `sales_invoices`, `sale_estimates` → `sales_estimates`, `sale_receipts` → `sales_receipts`, `expenses` → `expenses_transactions`, `dividends` → `dividend_payouts`, `payment_calendar_entries` → `planned_operations`). Опечатка тут НЕ ПАДАЕТ: миграция защищена `hasTable` и молча пропустит таблицу, разрез по юрлицу в ней не появится никогда. Сторож `legalEntityColumns.spec.ts` сверяет список с настоящими `tableName` моделей; подстановка имени из ТЗ роняет его. УРОК: первая версия сторожа искала модели только в `*.model.ts` и дала ЛОЖНУЮ тревогу на пяти живых таблицах — соблазн был выкинуть их из миграции; расширил сторожа, и он нашёл одну НАСТОЯЩУЮ ошибку. МИГРАЦИИ НЕ ЗАПУСКАЛИСЬ НИ НА ОДНОЙ БАЗЕ: ТЗ требует бэкап и прогон на стенде — это решение владельца. Прогон: сервер 2388/355, типы чисты. |
| 2026-09-18 | Этап 9 ЗАКРЫТ ПО КОДУ, шаг 9.4 — приёмка. Проверял по коду: экран, ручку и свёртку статей. Закрыто всё, что просило ТЗ, кроме разрезов. ОСТАТКИ Р1–Р4: период и разрезы на экране не выбираются (сервер их принимает, тест следит), юрлицо ждёт этапа 6. ГЛАВНЫЙ ОСТАТОК Р4: расходы по счетам, НЕ ПРИВЯЗАННЫМ НИ К ОДНОЙ СТАТЬЕ, не попадают в анализ вообще — `foldAccountsIntoArticles` пропускает их молча. Это не то же самое, что «Не размечено» (статья без пометки видна), и опаснее: итог просто меньше, чем в ОПиУ, и ничто на это не указывает. Лечение — строка «расходы вне статей» в свёртке, затронет и другие экраны, поэтому отдельным шагом. Живой пробы не было. Прогон: витрина 1072/140, сервер 2383/354. Дальше — этап 6. |
| 2026-09-18 | Этап 9, шаг 9.3: в форме статьи обязательный выбор «Постоянный или переменный» с пояснением дословно из ТЗ. Правило УСЛОВНОЕ: обязательно у расходов, запрещено у доходов (у выручки постоянных и переменных не бывает) — и стоит С ОБЕИХ СТОРОН: форма требует выбор и прячет поле у доходной статьи, сервер отвергает пометку у дохода с внятной ошибкой. Иначе через ручку API импортом заехала бы бессмыслица и молча подмешалась в постоянные затраты. НАМЕРЕННАЯ АСИММЕТРИЯ: сервер НЕ требует пометку у расхода — в справочнике уже лежат старые статьи без неё, запрет заблокировал бы правку названия у половины справочника. Проверок: 6 на витрине (ошибка у самого поля, обход «не отправлять поле» закрыт), 5 на сервере; отключение обязательности роняет 3 из 6. Попутно покраснел старый тест создания статьи — в его самодельной пустышке не было нового правила, дополнил. Прогон: витрина 1072/140, сервер 2383/354, словари 4357/4357. |
| 2026-09-18 | Этап 9, шаг 9.2: экран «Анализ расходов» (`/expenses-analysis`) и пункт меню в группе «Отчёты». Блоки: постоянные и переменные с долями (третья строка «Не размечено» — только когда в ней что-то есть), точка безубыточности и запас прочности, линия доли расходов в выручке по месяцам, топ статей с подсветкой резкого роста. ГЛАВНОЕ: предупреждение стоит НАД цифрами — расчёт по половине расходов выглядит так же уверенно, как правильный. «Постоянных нет вовсе» важнее, чем «часть не размечена», порядок закреплён тестом. Неприменимая метрика — прочерк, не ноль. Месяц без выручки рвёт линию, а не опускает её в ноль. Ничего не пересчитывается на витрине. 14 проверок. Прогон: витрина 1066/139, словари 4354/4354, сборка зелёная. |
| 2026-09-18 | Этап 9, шаг 9.1: серверная ручка анализа расходов `GET /financial-model/expenses-analysis`. Разбор подтвердил обещание ТЗ — заготовки есть: `costBehavior` у статьи, свёртка `ArticlesPlRollupService`, `computeDealMargin`, `GetBreakEvenService`, `enumerateMonths`. ГЛАВНОЕ РЕШЕНИЕ: корзины ТРИ, а не две — постоянные, переменные и НЕ РАЗМЕЧЕННЫЕ. Поле в базе допускает пустоту; молча отнести пустые к переменным значит занизить постоянные затраты и показать безубыточность ближе, чем она есть. Экран отдельно сообщает, сколько статей ждут разметки. Прошлый период — той же длины и встык (на конкретных датах: март 1–31 это 31 день, прошлый — 29.01…28.02). Новая статья подсвечивается без доли роста. Порог — БОЛЕЕ чем 20%. Ничего не считается заново: выручка из той же свёртки, безубыточность из того же сервиса. Проверок 19 + 6, мутация правила пустого поля роняет тест. Прогон: сервер 2378/353, типы чисты. |
| 2026-09-18 | Этап 4 ЗАКРЫТ ПО КОДУ, шаг 4.7 — приёмка. Проверял по коду, а не по памяти: шапки трёх отчётов, панели действий, тела таблиц. Закрыто: вход тремя карточками, период и разрез во всех трёх, графики ОПиУ и ДДС, раскрытие суммы в ОПиУ, экспорт Excel/PDF и печать во всех трёх, переключатель метода, план-факт. ОСТАТКИ (О1–О6 в строке этапа): главный — раскрытие суммы есть только в ОПиУ, и это не лень: там строка = ОБОРОТ за период, а в ДДС и Балансе = ОСТАТОК на дату, сумма проводок периода с ним не сойдётся; нужен расчёт «остаток на начало + движения». Фильтр юрлица ждёт этапа 6 (таблицы `legal_entities` нет), фильтр проекта — раздел без серверной части. ЖИВОЙ ПРОБЫ НЕ БЫЛО: стенд витрины недоступен, всё проверено прогонами и сборкой. Прогон: витрина 1052/138, сервер 2353/351, словари 4332/4332. Дальше — этап 9 «Анализ расходов». |
| 2026-09-18 | Этап 4, шаг 4.6: план-факт в ДДС — СВОДКОЙ над таблицей, а не колонками. Причина принципиальная: отчёт о движении денег построен косвенным методом, его строки — балансовые счета (дебиторка, запасы, кредиторка), а бюджет разложен по статьям доходов и расходов; пересечения нет, приписать план строке «Запасы» не к чему. Сводка показывает «Поступления» и «Выплаты» с планом, фактом и отклонением. Факт для ДДС — только ОПЛАЧЕННОЕ (правило `cashSettledReferenceKeys` взято из бюджета, не переписано), переводы между своими счетами не считаются; отключение отбора роняет 2 теста. Раскраска по смыслу стороны, а не по знаку: перерасход не бывает зелёным. ПОЧИНЕН ШАГ 4.5: тип бюджета был написан `bdr` вместо `bdir` — колонки в ОПиУ не появились бы никогда, заглушки в тестах это скрыли; добавлена сверка с общим списком `BUDGET_TYPES`. Проверок: +5 на сервере, +10 на витрине. Прогон: витрина 1052/138, сервер 2353/351, словари 4332/4332, сборка зелёная. |
| 2026-09-18 | Этап 4, шаг 4.5: в ОПиУ появились колонки «План» и «Отклонение» — если по периоду заведён бюджет; нет бюджета, нет колонок. Главная трудность не в арифметике: план заводится по СТАТЬЯМ, а строки отчёта — СЧЕТА, и одна статья может собирать несколько счетов. Правило: план встаёт в строку счёта только когда счёт у статьи единственный, иначе виден лишь в итоге по виду; строка без плана получает прочерк, а не ноль. План не считается дважды (план на группе отменяет планы внутри неё). Правило знака вынесено в общий `reportAccountNet.ts` — им теперь пользуются и раскрытие суммы (4.3), и отклонение; подмена роняет 3 теста. Итог сравнивает план и факт одних и тех же статей. Проверок: 11 на сервере, 9 на витрине. НЕ ЗАКРЫТО: ДДС (шаг 4.6), кассовый метод, бюджет берётся по году начала периода. Прогон: витрина 1042/137, сервер 2348/351, словари 4328/4328, сборка зелёная. |
| 2026-09-18 | Этап 4, шаг 4.4: в шапке ОПиУ переключатель «Кассовый / По начислению» и пояснение в одну строку, чем методы отличаются. Считать двумя способами сервер умел и раньше, но переключатель лежал в панели «Настроить отчёт» — человек не знал ни что методов два, ни каким посчитаны цифры перед ним. Правило выбора метода вынесено в `accountingBasis.ts` и повторяет серверное (`ProfitLossSheet/utils.ts`); 8 проверок сделаны зеркалом серверной спеки, чтобы подпись не разошлась с движком, подмена умолчания роняет 2 из 8. Сторож `moduleDisabledSpeaks` поймал правку (экран за флагом не должен гаснуть) — переключатель не раздел, а часть работающего отчёта, внесён в список осознанных исключений с обоснованием. Прогон: витрина 1033/136, типы чисты, словари 4326/4326, сборка зелёная. |
| 2026-09-18 | Этап 4, шаг 4.3: клик по статье в ОПиУ открывает справа панель с операциями, из которых сложилась сумма, и «Итого по строке». ТЗ зовёт этот пункт ключевым: без него человек не верит отчёту. Считает сервер, ручка `GET /financial-reports/chart/drill-down`; главное правило — знак суммы (кредитовый счёт: кредит минус дебет, иначе наоборот), подмена его на «дебет плюс кредит» роняет 2 теста из 6. Итог панели — сумма её же строк, не отдельный запрос. Не больше 200 операций за раз. Раскрываются только строки-счета: у итогов своих проводок нет. ДДС и Баланс — отдельно, у них другой источник строк. Прогон: витрина 1025/135, сервер 2337/350, словари 4324/4324, сборка зелёная. |
| 2026-09-18 | Этап 4, шаг 4.2: над таблицей ОПиУ и ДДС появился график по месяцам (выручка и прибыль / поступления и выплаты). Ряды считает сервер отдельной лёгкой ручкой `GET /financial-reports/chart` — из узлов того же отчёта и из движения по денежным счетам, чтобы картинка и таблица показывали одни числа. 5 тестов, включая сторону дебет/кредит и ограничение на 36 столбцов. Баланс без графика: там нужна структура активов, а не ряды. Прогон: витрина 1025/135, сервер 2331/349. |
| 2026-09-18 | Этап 4, шаг 4.1: раздел отчётов открывается тремя большими карточками (Деньги, Прибыль, Баланс) с подписями «зачем этот отчёт», остальные — свёрнутым списком. Главные отчёты не дублируются в списке ниже, опустевшая группа не показывается (5 тестов). Прогон: витрина 1025/135, словари 4317/4317. |
| 2026-09-18 | Этап 2, шаги 2.4–2.5: блок «Требует внимания» (ждут разноски, кассовый разрыв, просроченная дебиторка, заявки на оплату) — только непустые строки, каждая ведёт к действию, сбой источника главную не роняет. Сторож зависимостей расширен на новую ручку. Приёмка этапа: не закрыты строка о превышении бюджета (нагрузка на показ) и замер скорости с Redis-кешем. Прогон: витрина 1020/134, сервер 2326/348. |
| 2026-09-18 | Этап 2, шаг 2.3: на главной появились полоса показателей, график «Деньги по месяцам», остатки по счетам, топ статей расходов и переключатель периода (запоминается вид, а не даты). Пустая организация видит приглашение подключить счёт. Попутно: в `DashboardSummary` семь строк были вписаны по-русски в разметку — переведены на словарь. Прогон: витрина 1020/134, словари 4305/4305. |
| 2026-09-18 | Этап 2, шаг 2.2: заведена ручка `GET /dashboard/overview` — всё для главной одним ответом (плитки с изменением к прошлому периоду, 12 месяцев для графика, остатки по счетам, топ-5 статей расходов). Доходы, расходы и прибыль берутся из узлов отчёта ОПиУ — второго способа считать те же суммы нет. Тест на 7 проверок, проверен мутацией. Прогон: сервер 2321/348. |
| 2026-09-18 | Этап 1 «Навигация»: меню перестроено с 14 пунктов до 8 (подгруппы «Новые задачи» убраны — панель формы создания и так отбрасывает), глубина 2 уровня, ни один адрес не потерян (сторож `navigationReachability` прошёл без правок). Заведён флаг `documents`, выключенный по умолчанию: первичка уходит под него девятым пунктом. Режим «Бизнес» прячет 13 адресов вместо 6. Прогон: витрина 1013/133, сервер 2314/347, словари 4282/4282. |
| 2026-09-18 | Этап 3, шаг 3.7 и приёмка: сторож телефона расширен на примитивы дизайн-системы (раньше экраны на `DataTable` не проверялись вовсе), в шапку добавлены «Импорт выписки» и «Экспорт», заведён отбор по сумме. Починен мигающий тест двухфакторного входа (bcrypt не укладывался в 5 с). Таблица приёмки — в плане этапа: не закрыты отбор по контрагенту и статье, проект и юрлицо (зависят от чужих этапов). Прогон: витрина 1006/133, сервер 2313/346, словари 4271/4271. |
| 2026-09-18 | Этап 3, шаг 3.6: массовые действия — выделение строк в режиме разноски, «назначить статью выделенным» одним запросом и «исключить из учёта». Смешанное выделение (приход + расход) разом не разносится, вместо списка статей выводится объяснение. Прогон: витрина 1005/133, сервер 2313/346, словари 4268/4268. |
| 2026-09-18 | Этап 3, шаг 3.5: разнос одним движением — статья выбирается прямо в строке, сторона операции определяет список статей и тип, подсказка правила применяется одной кнопкой. Логика вынесена в `categorizeInline.ts` (7 тестов). Прогон: витрина 1001/133, сервер 2313/346, словари 4263/4263. |
| 2026-09-18 | PR #425 влит в `develop` (`1d2d0de8`): шаги 3.1–3.4 этапа 3. Вливание промежуточное — чтобы работа была видна и не копилась в ветке; оставшиеся шаги 3.5–3.7 пойдут следующим PR. |
| 2026-09-18 | Этап 3, шаг 3.4: полоса «N операций ждут разноски» на экране «Операции» + режим их показа с подсказкой от правила разноски. Сервер: ручка `GET /banking/uncategorized` — ждущие разноски по всем счетам (раньше только по одному), сторож на 3 проверки. Прогон: сервер 2313/346, витрина 991/132, словари 4259/4259. |
| 2026-09-18 | Этап 3, шаг 3.3: новый экран «Операции» (`/cashflow-accounts/transactions`) — список по всем счетам на новой дизайн-системе, период (по умолчанию текущий месяц), отборы по счёту, направлению денег и поиску, отборы в адресной строке, подгрузка по кнопке. Пункт меню добавлен в группу «Банк». Сервер дополнен счётом, контрагентом и назначением в строке. Прогон: витрина 988/132, сервер 2310/345, типы чисты, словари 4250/4250. |
| 2026-09-18 | Этап 3, шаг 3.2: серверный список операций по всем счетам — счёт стал необязательным, добавлены отборы (период, направление, контрагент, сумма, поиск), входящий остаток без счёта не считается. Сторож `allAccountsTransactions.spec.ts` (6 проверок, проверен мутацией). Попутно починена красная проверка типов сервера с `develop` (тест уборки демо передавал 7 доводов вместо 8). Прогон: сервер 2310/345, типы чисты, словари 4238/4238. |
| 2026-09-18 | Решения владельца: (1) сначала этап 3 «Экран операций», меню — после; (2) раздел «Проекты» оживлять, а не убирать из меню; (3) блок «Документы» выключить всем строго по ТЗ. Очередь исполнения изменена: 3 → 1 → 2 → 4 → 9 → … |
| 2026-09-18 | Этап 1, шаг 1.1: разобрано текущее меню (1001 строка, 117 записей, 14 пунктов), составлено сопоставление в 8 пунктов, сверены 27 адресов целевого меню с таблицей маршрутов — четырёх не существует. План: `docs/superpowers/plans/2026-09-18-tz-01-navigation.md`. Кода не трогали. |
| 2026-09-18 | Стабилизация, источник №5 (долг по `@ts-nocheck`): слой «семь замечаний» — слепая зона 134 → 102 файла, замечаний 1810 → 1571, хуже никому. 20 настоящих поломок, из них поле «Ссылка» в формах входящего платежа и кредит-ноты не сохранялось никогда. Два сторожа. Карта `docs/superpowers/specs/2026-09-17-roadmap-actualization-v88.md`. |
| 2026-09-18 | ТЗ заведено в репозиторий: `MASTER.md`, этот файл состояния, алгоритм `CONTINUE.md`, команда `/tz`, точка входа в `CLAUDE.md`/`AGENTS.md`. Работы по этапам не начинались. |
