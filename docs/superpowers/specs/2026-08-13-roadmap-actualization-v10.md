# Bigfin — Дорожная карта v10 (после фазы «Кто что видит») — 2026-08-13

Предыдущие карты: v6 «Достоверность учёта», v7 «Надёжность записи», v8 «Кто
что может», v9 «Кто что видит» — все закрыты (у v7 и v9 общий хвост — оплата
GitHub для автопроверок).

---

## 1. Приёмка фазы v9

Стенд собран со всеми срезами фазы, пробы — по каждому шагу. **11 проверок из
11 зелёные:**

| Шаг | Проба | Итог |
|---|---|---|
| В1 | оборотно-сальдовая ведомость сотруднику | 403 ✅ |
| В1 | остатки по покупателям сотруднику | 200 ✅ |
| В1 | баланс владельцу | 200 ✅ |
| В2 | письмо по счёту-«призраку» | 404 ✅ |
| В2 | письмо по оплате-«призраку» | 404 ✅ |
| В3 | роль «только поставщики» в подсказке | только поставщики ✅ |
| В4 | покупатель без входа видит счёт по ссылке | 200, сумма верна ✅ |
| В4 | выдуманная ссылка | 404 ✅ |
| — | повседневная работа сотрудника | цела ✅ |

Новых дефектов приёмка не нашла.

---

## 2. Что дала разведка этой итерации

Прошлые фазы отвечали: можно ли верить цифрам (v6), факту сохранения (v7),
границам записи (v8) и границам показа (v9). Следующий вопрос — **можно ли
верить тому, что уходит в фон**: письма, уведомления, пересчёты.

Разведка велась по живым данным очередей стенда, и они красноречивы:

**В очередях стенда молча лежат 22 упавшие почтовые задачи** — 12 писем по
счетам, 4 по оплатам, 4 по чекам, 2 по сметам. Никто о них не узнал: ни
отправитель, ни владелец.

Причин две, и обе показательны:

1. **`NotFoundError`** — исторические следы класса В2 до починки: письма по
   несуществующим документам, которые «отправились» с ответом 200.
2. **Почтовый сервер не настроен** — и это будни не только стенда: неверные
   реквизиты почты на проде дадут ровно ту же картину. Пользователь жмёт
   «отправить», видит успех, письмо не уходит — теперь уже не из-за документа,
   а из-за доставки.

Устройство фона это гарантирует:

- **Повторов нет вовсе.** Глобальные настройки очередей задают только очистку
  истории; ни одна очередь не задаёт попыток и затухания. Временный сбой
  почты = письмо потеряно навсегда с первого раза.
- **Падение молчит.** Обработчики очередей ловят ошибку, пишут её в консоль
  сервера и перебрасывают — задача помечается упавшей в Redis, и на этом всё.
  В ленту уведомлений (модуль ㉒ есть и работает!) ничего не попадает.
- **Сбой постановки в очередь местами глотается** — найдено ревью В2 в
  подписчике писем подтверждения: очередь недоступна → клиенту всё равно
  «успех».
- **Bull Board (/queues)** существует, но это техническая панель за отдельным
  паролем — владелец-предприниматель туда не смотрит.

---

## 3. Долг, вскрытый этой разведкой (новое в v10)

| № | Что | Почему важно | Объём |
|---|---|---|---|
| **Ф1** | **Упавшее письмо невидимо для человека** | Пользователь уверен, что письмо ушло; узнаёт от контрагента через неделю | Уведомление в ленту (модуль ㉒) при окончательном падении почтовой задачи + место, где можно повторить |
| **Ф2** | **Ни одного повтора ни у одной очереди** | Временный сбой почты/сети превращается в потерю навсегда | Попытки с затуханием для почтовых очередей (общая настройка) |
| **Ф3** | **Сбой постановки в очередь глотается** | Redis недоступен → «успех» без задачи вообще | Пройти подписчиков с try/catch вокруг queue.add |
| **Ф4** | **Счётчик «не отправлено» для владельца** | 22 упавшие задачи нашлись только прямым запросом в Redis | Строка в «Качестве данных» или плитка: сколько упало за 7 дней |
| **Ф5** | **Стенд: почта и рендер PDF не настроены** | Письма и PDF нельзя проверить вживую даже владельцу | Стендовая задача: локальный SMTP-двойник уже был в приёмке ㉒; рендер PDF — разобраться, чем рендерится и чего не хватает |

### Чего в списке НЕТ

Фоновый пересчёт себестоимости и разовые перестроения при включении складов
намеренно глушат ошибки (решения #209/#210) — их не трогаем: сбой пересчёта
не должен отменять сохранение документа.

---

## 4. Предлагаемая фаза: «Фон, которому можно верить»

Порядок — от дешёвого к заметному:

1. **Ф2 — повторы.** Самое дешёвое и сразу спасает от временных сбоев.
2. **Ф1 — видимость.** Уведомление в ленту при окончательном падении: модуль
   уведомлений уже умеет письма/телеграм/ленту — нужен новый тип события.
3. **Ф3 — не глотать постановку.**
4. **Ф4 — счётчик для владельца.**
5. **Ф5 — стенд** (после Ф1–Ф4, чтобы было чем проверять вживую).

**Что НЕ делать сейчас** (без изменений): тяжёлые банковские интеграции,
нативный клиент, консолидация мультиюрлица.

---

## 5. Открытые вопросы владельцу

Остаются из v9 (номера прежние): 1 ребрендинг; 2 ЭДО; 3 перенос месяца;
4 резервный канал Telegram; 5 НДС/МойСклад по запросу; 6 SMS-провайдер
дебиторам; 7 регламент перепроведения НДС; 8 невозмещаемый НДС по умолчанию;
9 **оплата GitHub** (единственный общий хвост v7–v9 — все сторожа трёх фаз
гоняются только вручную).

Новые:

10. **SMS по смете — ручка-пустышка** (найдено в В2): отвечает 201, вся
    служба закомментирована. Вернуть SMS или убрать ручку?
11. **Прод-почта.** После Ф1–Ф2 стоит проверить реквизиты почты на проде:
    сейчас сбой доставки невидим, и сколько писем уже потеряно — неизвестно.

---

## 6. Что дальше

Фаза v10 «Фон, которому можно верить» предлагается к исполнению в порядке
Ф2 → Ф1 → Ф3 → Ф4 → Ф5.
