/**
 * Заглушки для пакетов, у которых в проекте нет объявлений типов.
 *
 * Без такого объявления каждый ввоз пакета — замечание «не найден файл
 * объявлений», и файл не может выйти из-под пометки «не проверять типы».
 * Так было в 28 файлах витрины (Д3 карты v66).
 *
 * Это **заглушка, а не типизация**: содержимое пакета становится `any`.
 * Настоящие типы у большинства из них есть в `@types/*` — их надо поставить
 * зависимостями. Этого здесь намеренно не сделано: установка правит
 * `pnpm-lock.yaml`, а файл замков затрагивает стенд и в этом репозитории
 * защищён отдельным хуком. Установка типов вынесена в задел карты v66.
 *
 * Как убрать отсюда пакет: поставить его `@types/…` и вычеркнуть строку.
 *
 * Образец приёма взят у соседнего `flat.d.ts`.
 */

// `react-router` и `history` заглушать НЕЛЬЗЯ: `@types/react-router-dom`
// ссылается на них изнутри, и заглушка перекрывает настоящие типы — сразу
// ломается `useParams<{…}>()` в проверяемых файлах. Проверено правкой:
// с заглушкой проверка типов падает в трёх местах. Их 12 замечаний остаются.

// Здесь остался один пакет: у остальных восьми настоящие типы поставлены
// зависимостями (карта v74), и заглушки убраны.

// У этих пакетов объявлений нет.
//
// `react-use-context-menu` — нет ни своих, ни в @types.
//
// `react-scroll-sync` — свои типы библиотека завела только в первой версии, а
// у нас стоит 0.7.1. Пакет `@types/react-scroll-sync` ставить бесполезно: он
// помечен устаревшим и внутри пустой — лишь сообщает, что типы есть в самой
// библиотеке (карта v74). Переход на 1.x — отдельный разговор.
declare module 'react-use-context-menu';
declare module 'react-scroll-sync';

/**
 * Виджет оплаты Lemon Squeezy подключается тегом `<script>` и живёт в глобальном
 * объекте окна. Объявлено то, что витрина действительно у него зовёт
 * (Д7 карты v83).
 */
interface Window {
  LemonSqueezy: {
    Setup: (options: {
      eventHandler: (event: { event: string }) => void;
    }) => void;
    Url: { Open: (url: string) => void };
  };
}
